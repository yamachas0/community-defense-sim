# Quiet Acquisition — 作業引き継ぎ

最終更新: 2026-08-27 (JST)

## 再開時に最初に読むもの

1. このファイル
2. `RESULTS.md`
3. `README.md`
4. `git status --short` と `git log -5 --oneline`

このファイルは Codex / Claude / 人間の共通の引き継ぎ正本。作業を中断する前、または
実装・テスト・ランの節目ごとに必ず更新する。

## 現在の目的

既存の `mvp_v1` を再現可能なまま保存し、別府をモデルにした架空都市「A市」の
v2シナリオを追加する。

中心命題:

> 宇宙港の土地そのものではなく、宇宙港を支える人々が住み、泊まり、働く都市を
> 海外不動産投資会社が先回りして取得したとき、地域は何の情報から集中を認知するか。

## 合意済みのA市v2設計

- 現実の別府型の国際温泉観光都市だが、名称はすべて「A市」にする。
- A空港は湾の反対側にあり、スペースポート化が進む架空の将来時点。
- A市は宇宙関連企業の長期滞在、研究者・技術者の住宅、会議・研修、観光需要を受ける。
- 4地区: 湾岸観光地区、中央駅前地区、北部学術・生活地区、温泉丘陵地区。
- 集中取得主体は海外不動産投資会社 `Orbital Gateway Properties`。
- 目的は60か月以内の宿泊・住宅・商業・研究交流ポートフォリオ形成と長期収益。
- 「最大シェア」「支配」「秘匿」「目立つな」は目的に書かない。
- 国内SPCは通常の用途別・共同投資家別ビークルとして利用可能にする。
- 地域が別名義の共通支配を、町内会、旅館組合、仲介、管理通知、報道などの
  どの断片情報から認知するかを観測する。
- 初回は現行用途モデル（住宅・商店・空地・公有地）と互換な最小v2とする。
- 初回構成は48区画、60か月、27体:
  - 16世帯
  - 4事業者
  - 2仲介
  - 1海外不動産投資会社
  - 2自治体担当
  - 2メディア
- 町内会・旅館組合は、初回では住民・事業者の属性と発話経路として表現する。
- ホテル客室数、賃貸戸数、温泉旅館運営シェアなどの専用用途/KPIは初回ラン後の第2段階。

## 現在の実装状況

- `main` は `d095b1f`。作業ツリーは開始時点でクリーン。
- v1の本番ラン、詳細トレースビュー、57件の回帰テストまではコミット済み。
- A市v2のコード変更はまだ未実装。
- 再起動前はWindowsの実行環境不調で編集不能だったが、現在は権限外実行で読み取り可能。

## 次に行う作業

1. `configs/config_field_v2.yaml` を追加する。
2. `configs/personas/field_v2.yaml` を追加する。
3. 世界背景をconfigから全エージェントのsystem promptへ渡す。
4. `agents.py` / `prompts.py` の「買い手AI」固定表記を一般の取得主体へ直す。
5. 構造化出力に `evidence`（判断根拠となった観測ID）を追加し、`events.jsonl`へ保存する。
6. 観測可能な売買・発話・私信・記事へ安定したIDを表示する。
7. 既存57テストを実行し、新機能の回帰テストを追加する。
8. 60か月mockを実行し、レポート生成まで確認する。
9. `tools/estimate_cost.py` でAPI費用を見積もる。
10. 実APIランは、見積額をユーザーへ報告してから判断する。

## 守るべき設計原則

- 行動選択はLLMに任せ、売却確率・閾値・拡散率などの行動ルールを追加しない。
- ペルソナには事情・属性だけを書き、「〜なら売る」のような台本を書かない。
- 観測に無い事実をコードで補完しない。欠損は不成立として残す。
- v1の設定・成果物を変更しない。v2は別ファイルで追加する。
- 秘密情報や `.env` の内容を文書へ転記しない。

## 検証コマンド

```powershell
python tests/test_ledger.py
python run.py --config configs/config_field_v2.yaml --provider mock
python tools/estimate_cost.py --run simulations/<v2 mock run directory>
```

## 完了条件

- A市v2設定とペルソナが保存されている。
- v1の57テストを含む全テストが成功する。
- 60か月mockが完走し、`summary.json`、`events.jsonl`、`prompt_log.jsonl`、HTMLが生成される。
- `events.jsonl`に判断根拠が残り、元の観測IDへ追跡できる。
- API呼び出し数、入力/出力トークン概算、通常見積り、安全係数込み上限が報告される。

---

## 2026-08-25 field_v3 更新（この節が上記v2記述より優先）

### 現在の目的

既存 `mvp_v1` と `field_v2` を保存したまま、確定した世界設計を `field_v3` として
60か月実行する。X社だけがA市の不動産を秘密裏に狙う目的を持つ。ほかの主体は
通常の日常を送り、実際の同席・連絡・契約通知・調査・限定配信だけから全体像が
明るみに出ることがあるか、また最後まで発覚しないかを観測する。

### 確定設計

- 未来の筋書き、売却確率、報道閾値、噂の注入、強制イベントを置かない。
- 行動選択はすべてLLM。コードは可能行為、契約、会計、同席、情報配送、記録だけ。
- 秘密目的を知るのはX社だけ。ほかの主体には研究目的も非公開目的も渡さない。
- 27主体: R01–R16、T01–T04、B01–B02、X社、G01–G02、J01–J02。
- 法的名義: X社、A社、B社、C社、D社。土地登記は実際の登録名義だけを表示。
- 公開法人記録を自分で調べた主体だけが名義間の関係を調べられる。
- V01–V08: 飲食店、公民館、共同浴場、喫茶店、旅館組合会議室、
  市役所待合・記者控室周辺、仲介事務所、イベント会場。
- ambient発話は当月同じ場所に実際にいた主体だけへ翌月配送。
- direct発話は指定した一主体だけへ配送。全員共通タイムラインはない。
- 記者は通常の地域記者。観測済みの事実だけから毎月の行動を選ぶ。
- 記事は購読設定のある主体だけへ届き、全員へ自動配信しない。
- 所有権と長期賃借・運営支配は別の状態・KPI。
- 60か月まったく発覚しない結果も有効。
- v3のプロンプトとHTMLには「AI」表記を出さない。

### 実装済み

- `src/field_v3.py`: 日常行為、v3スキーマ、観測プロンプト、長期賃借・運営支配。
- `src/simulation.py`: v3月次経路、同席/直接/限定記事配送、登記調査、名義検証。
- `src/agents.py`: 購読属性などの個体属性を保持。
- `src/llm_client_factory.py`: v3スキーマ対応Mock（配線確認専用）。
- `src/viz.py`: AI表記を削除し、所有と長期賃借・運営支配を別表示。
- `configs/config_field_v3.yaml` / `configs/personas/field_v3.yaml`。
- `tests/test_ledger.py`: v3機構の回帰テストを追加。

### 検証結果

- 構文検査: 成功。
- 回帰テスト: `RESULT: 72 passed, 0 failed`。
- 60か月Mock: `simulations/2026-08-25_2215_18_field_v3_a_city`。
- API呼出相当1,650（主体1,620 + 分類30）。
- 不成立54件はMockの既取得再買付、既転出・既廃業の再選択。配線エラー0。
- HTML再生成済み。AI表記0。Playwrightでページ読み込み成功。
- Mockの所有41.3%、支配0%、報道・規制などはテストダブル由来で解釈禁止。

### 費用見積もり

`simulations/2026-08-25_2215_18_field_v3_a_city/cost_estimate.json`

- 入力概算2,448,946 tokens、出力仮定749,520 tokens
- 通常約 `$0.545`、安全係数2倍で約 `$1.089`

### 次の作業

1. Google実APIでfield_v3を60か月実行。
2. 成果物、無効行動、情報経路、所有/支配、報道到達先を監査。
3. `RESULTS.md` と本節へ本番結果を追記。
4. 本番HTMLのGitHub Pages反映は公開前承認を取る。

### 承認・アローリスト

承認不要にしてよい安全な種類:

- `rg -n` / `rg --files`、`git status` / `git diff` / `git diff --check`
- `python -m py_compile`、`python tests/test_ledger.py`
- v3 `--provider mock`、`tools/estimate_cost.py`、`tools/render_report.py`
- ローカル `python -m http.server` とローカルHTMLのPlaywright確認
- リポジトリ内の非破壊パッチ適用

承認を残す種類:

- 実APIの課金ラン
- `git push`、GitHub Pages更新、公開範囲変更
- 削除、履歴改変、強制push
- APIキー・秘密情報へのアクセスや外部送信

このセッションでは安全コマンドのprefix承認を可能な範囲で保存済み。
Windowsの標準パッチ障害時だけ非破壊の `git apply` を代替利用する。



---

## 2026-08-27 更新（この節が上記すべてに優先する。最終更新 2026-08-27 12:15 JST）

担当交代の記録：8/25 11:32〜8/27 10:54 は OpenAI Codex デスクトップが作業。
8/27 11:17 の施主指示により Claude 側（CEO=Fable / CTO=Opus 5）へ引き継いだ。
本節は Codex の 4 コミットと run31/34/36/37/38 の**実測値**で正本化したもの。推測は「推定」と明記した。

### 実装状況（`main` = `648f2d8`・未push・5コミット先行）

作業ツリーには `AGENTS.md` / `CLAUDE.md` / `docs/layout-library.png` / `simulations/*`（run16〜38）が未追跡で残る。

8/26〜27 に Codex が入れた 4 コミット：

1. **`46d000f` 戦略テレメトリ** — X社の出力に計画欄（`goal_assessment` / `strategy` / `next_milestone` /
   `expected_goal_effect` / `alternatives` / `revision_reason`）を追加し、`events.jsonl` の `plan` に保存。
   `src/field_v3.py` に `normalize_acquirer_plan_v3` / `_acquirer_plan_text`、`src/simulation.py` に `execution_history`。
2. **`d7c8f75` 計画と実行の統合** — 「計画コール」と「実行コール」の二段レビュー方式を廃止し、
   計画と今月の行動を1回の構造化意思決定として返させる。二段方式は戦略JSONの半数が解析不能で失敗したため。
3. **`8c968b6` X社の月次オペレーション枠** — X社だけ `operations[]` 配列で **1〜6件/月**を並行実行可能に
   （`configs/config_field_v3.yaml: acquirer_monthly_operation_capacity: 6`）。他主体は従来どおり月1行動。
   `action_schema_v3` が acquirer のときだけ `operations` 配列スキーマを返す。
   枠超過は `invalid_action / monthly_capacity_exceeded`。
4. **`648f2d8` X社の予算上限撤廃** — `acquirer_financing: on_demand`。成約時に必要額を調達する主体として
   台帳（`Ledger.enable_on_demand_financing` / `financing_raised`）に明示し、調達累計を観測と結果に残す。
   `[手元資金]` の代わりに `[資金条件] 必要資金は案件成立時に調達可能。調達累計N万円。固定予算上限はない。` を表示。

X社の脳は `acquirer_model: gemini-2.5-flash`（`acquirer_thinking_budget: 256` / `acquirer_max_tokens: 2400`）、
他26主体は `gemini-2.5-flash-lite`（`max_tokens: 720`）。`src/simulation.py` に acquirer 専用クライアントの
別立て経路があり、`acquirer_model` を config で差し替えられる（**現時点で実走はしていない**）。

### ランの実測結果（60か月・実API・すべて seed 85）

| run | ディレクトリ | 時点 | 所有率 | 実効支配率 | 成約 | calls | in/out tok |
|---|---|---|---|---|---|---|---|
| 26 | `2026-08-26_1359_26_field_v3_a_city` | `46d000f` 前後 | 0% | 0% | 0 | 1,694 | 242万/34.9万 |
| 31 | `2026-08-26_2306_31_field_v3_a_city` | `d7c8f75`（統合意思決定） | 0% | 0% | 0 | 1,634 | 233万/33.2万 |
| 34 | `2026-08-27_0949_34_field_v3_a_city` | `d7c8f75` | 0% | 0% | 0 | 1,632 | 241万/36.9万 |
| **36** | `2026-08-27_1007_36_field_v3_a_city` | `8c968b6`（オペ枠・予算9億円あり） | **39.1%** | **35.4%** | **18** | 1,632 | 245万/37.2万 |
| 38 | `2026-08-27_1036_38_field_v3_a_city` | `648f2d8`（予算撤廃） | 0% | 0% | 0 | 1,632 | 245万/37.6万 |

3か月試走：run35（`8c968b6`）所有6.5%、run37（`648f2d8`）所有10.9%・成約5件、
run28/29/30/33 は 0%。1本60か月の実費は **$0.5〜1.1**（`cost_estimate.json` 準拠）。

**run36 が第20月で止まった理由は特定済み**：成約18件の価格合計 90,043万円が `acquirer_budget: 90000` に到達し、
以後の承諾が `accept_rejected / buyer_insufficient_funds` で13件弾かれた。予算撤廃（`648f2d8`）の判断自体は正しい。

**run38 が 0% の理由**：X社342 operations のうち `check_land_registry` 160回（実区画14・重複146回＝全操作の42.7%）、
`contact_broker` 59回、`internal_review` 59回、`financing_review` 56回で、**97%以上が準備行動**。
買付は第47月と第60月に P17 へ2件のみ、所有者は応じなかった。

### 2026-08-27 の診断（施主指示「モデルを疑う前に設計を見ろ」）

全文は **`docs/design_review_loop.md`**。要点だけ：

- **全11ラン（60か月5本＋3か月6本）で、第1月に `make_offer` を出した3本はすべて成約があり、
  出さなかった8本はすべて成約0。分離は完全。** run37 と run38 は steps 以外 config 完全一致で結末が分かれた
  ＝二峰性は構造ではなくサンプリング。
- **【欠陥A・最重要】買付IDの表記が2系統ある。** 観測は `[OFFER-O0001] O0001: ...` と表示するが
  `Ledger.record_accept`（`src/world.py:207`）は `O0001` しか解さない。
  run36で住民の `accept_offer` 110回中 **77回（70%）が `no_such_offer` で消滅**していた。
  **run≤38 の成約数は、この欠陥による下限値である。**
- **【欠陥B】** run38のdirect通信308通の69%が「来週伺います／日程調整」等、世界に動詞が存在しない会合の約束。
  履行は原理的に0。金額を含む通信は0通。X社⇔仲介は118通往復して契約0件。
  仲介の行為はすべてノート1行の記録で、売り意向・査定額が事実として台帳を流れない。
- **【欠陥C】** X社は開始時に全46区画の登記を取得済み（`land_registry_scope: non_public`）で毎月全件表示されているのに、
  世界は「既知だ」という事実を返さない。自社の買付は「直近12件」しか観測に出ない（run36は56件出したので44件が不可視）。
- **【欠陥D】** 計画欄6必須。run38では `revision_reason` の83%、`expected_goal_effect` の78%が前月と完全一致。
  `alternatives` は全月ちょうど3件。
- **【欠陥E】** 通信は翌月配送で1往復2か月。60か月で最大30往復。run38はそれを社交辞令に使い切った。
- X社は60か月×2run で `make_lease_offer`（実効支配のもう半分の経路）を **1件も使っていない**。

提案5案（案1 ID統一 / 案2 仲介が事実を運ぶ / 案3 パイプライン機械記録＋登記の確認月 / 案4 計画欄削減 /
案5 同月内往復＝見送り推奨）と、各案の「ルールベース抵触」自己点検は `docs/design_review_loop.md` の第3章。

### 次に行う作業（施主判断待ち）

1. **施主が案1〜5から採用案を選ぶ。** 選定後に第2便として実装（CTO発注）。
2. 案1（IDの表記統一）は他と独立で効き、非互換の代償が最も小さいため最優先で推奨。
3. **モデル差し替え（`acquirer_model: gemini-2.5-pro` 等）のA/Bは保留**（施主指示 8/27 11:23）。
   経路は実装済みで config 1行で切替可能だが、上記欠陥がある状態での比較は「壊れた世界での比較」になる。
4. `RESULTS.md` に「run≤38 の成約数は欠陥Aによる下限値」を追記する。
5. 同一設定の複数本による分布測定は、欠陥修正後に費用GOを取ってから。

### 禁止・制約（変更なし）

- ルールベース排除（売却確率・閾値・強制イベント・「〜なら〜しろ」の指示文・台本を足さない）。
- 観測に無い事実をコードで補完しない。欠損は不成立として残す。
- `mvp_v1` / `field_v2` の設定と成果物は変更しない。
- v3のプロンプトとHTMLに「AI」表記を出さない。
- 秘密情報・`.env` の内容を文書に転記しない（鍵は `GOOGLE_API_KEY` のみ）。
- **実APIの課金ランは施主の費用GO待ち。** 本便では1本も走らせていない。
- `git push` は施主承認制。本便では実施していない。

### 進捗ログ

Claude側の作業ログは `docs/progress_claude.log`（`HH:MM 内容` 形式）。

---

## 2026-08-27 第2便更新（この節が上記すべてに優先する。最終更新 2026-08-27 12:50 JST）

担当：CEO=Fable / CTO=Opus 5。施主GO（8/27 12:04「提案の通りすすめて」）を受け、
`docs/design_review_loop.md` の **案1 → 案6 →（案2＋案3）→ 案4** を実装した（案5は不実装）。

### この便で入れた変更（`main` = 未push・5548095 から6コミット先行）

| commit | 内容 |
|---|---|
| `663a352` | **案1 ID表記統一**。観測は `[O0001] P17 名義A社 1588万` / `[L0001] …` の1トークンだけを出す。`Ledger._normalize_id`（`src/world.py`）が角括弧と `OFFER-`/`LEASE-` 接頭辞を剥がしてから解決するので、`[O0001]` / `O0001` / `OFFER-O0001` のいずれで指されても同一の1件になる。`record_accept` / `record_reject` / `record_counter` / `record_withdraw` / `resolve_lease_offer` に適用。 |
| `10957ab` | **案6 行為の帰結を本人へ返す**。`Simulation._record_own_results` が毎月末に各主体の行為と帳簿結果（結果種別・理由コード・関係ID）を `agent.extra["last_month_results"]` に入れ、翌月の観測に `[先月の自分の行為と、帳簿上の結果（機械記録）]` として出す。全主体（X社含む）が対象。 |
| `f212ef2` | **案2 仲介が事実を運ぶ ＋ 案3 案件記録**。意向確認を台帳の状態遷移（`Q0001` 形式・`requested→asked→answered→reported`）にした。新動詞＝X社 `request_owner_inquiry`/`answer_broker_inquiry`、仲介 `inquire_owner_intent`/`report_owner_intent`、住民 `answer_broker_inquiry`。構造化欄 `parcel_id` / `inquiry_id` / `owner_intent`(enum) / `asking_price`（万円の数値 or `unknown`/`not_asked`/`declined_to_answer`）。X社の観測に `[自社の案件記録（機械記録・区画別・全件）]` を追加し、直近12件制限を廃止。`check_land_registry_v3` は登記を実際に読み直し、前回スナップショットと一致した時だけ「第N月の照会と同一」、差異があれば差異を返す。 |
| `2cba76f` | **案4 計画欄6→2**。必須は `strategy` と `next_milestone` だけ。`goal_assessment`/`expected_goal_effect`/`alternatives`/`revision_reason` は任意（`normalize_acquirer_plan_v3` が無ければ空を返すのでテレメトリの形は不変）。 |
| `2a75414` | mock の不整合修正（照会が無い月に `answer_broker_inquiry` を空targetで出さない）。 |
| `b2de14e` | `tools/run_metrics.py` 追加。run36/38 の診断で使った指標を機械的に再現する集計器。 |

### 設計上の決定（次の担当者が迷わないように）

- **`inquire_owner_intent` の宛先は所有者IDではなく区画ID**。所有者は台帳が解決する（`record_offer` が区画から所有者を解決するのと同じ物理）。仲介は登記を引く動詞を持たないため、所有者IDで宛てさせると当てずっぽうになり、この経路が成立しなくなる。
- **`owner_intent` は enum**（`willing_to_sell` / `willing_to_lease` / `not_willing` / `undecided` / `unknown` / `not_asked` / `declined_to_answer`）。未査定・未回答・回答拒否を正当な事実として運べるようにしてある。**どれを選ぶか、そもそも答えるかは主体が決める。**
- **`asking_price` は文字列**。数値なら実額、`unknown`/`not_asked`/`declined_to_answer` なら不明という事実。数値でも unknown 系でもない値は `invalid_price_value` で不成立にする（欠損を金額に化けさせない）。
- **案件記録・登記確認・先月の結果に書くのは事実と出典IDだけ**。「再照会不要」「確認が古い」「次は交渉すべき」「一定月数で自動的に要再調査」「優先度・成功確率」は書かない（不可リスト＝`docs/design_review_loop.md` 3章）。回帰テストで禁止語を固定してある。

### 検証結果

- `python tests/test_ledger.py` → **RESULT: 126 passed, 0 failed**（案1で9本・案6で6本・案2/3で26本・案4で4本を追加）。
- 60か月mock（`--provider mock`）→ 完走。最新 `simulations/2026-08-27_1236_48_field_v3_a_city`（calls 1649・errors 0・truncated 0）。意向確認は request 6 / asked 34 / answer 53 / report 27 で依頼→照会→回答→報告が全段通っている。HTML内「AI」表記 0件。
- **mock の `invalid_actions` は版をまたいで比較できない**（プロンプト長で mock の乱数が変わるため）。増分はすべて mock 側の当てずっぽう由来で、世界側は正当に弾いている（内訳は `docs/progress_claude.log` 12:37 の行）。
- **実API試走 3か月**：`simulations/2026-08-27_1243_49_field_v3_a_city`。**実費 $0.0325**・calls 82・API errors 0・**truncated 0 / invalid_actions 0 / `no_such_offer` 0**（＝合格条件を満たす）。X社は `request_owner_inquiry` を11回使用、登記の重複照会 0、`make_offer` は3か月内では 0。

### 次に行う作業

1. Codexレビュー（実装＋ルールベース抵触）→ 指摘反映。※8/27 12:45 時点で Codex 側の使用量上限により失敗、13:17 以降に再試行。
2. **実API本走：同一設定（`config_field_v3`・seed 85）× 3本を順次**。累計上限 **$4**（試走込み）。1本ごとに `summary.json` の usage で実費を積算し、超えそうなら止める。
3. 3本を `tools/run_metrics.py` で集計し、**分布として** `RESULTS.md`（1本1節＋横断比較）と本節に追記する。
4. `RESULTS.md` に「**run≤38 の成約数は欠陥A（ID表記の二重化）による下限値**」を明記する。
5. レポートHTMLは既存の GitHub Pages（`yamachas0.github.io/quiet-acquisition-report` か `dev-review/qa-runs/`）へ公開しURLを報告。**ソースリポの push は禁止**。

### 禁止・制約（変更なし）

- ルールベース排除（売却確率・閾値・強制イベント・「〜なら〜しろ」の指示文・台本を足さない）。
- 観測に無い事実をコードで補完しない。欠損は不成立として残す。
- `mvp_v1` / `field_v2` の設定と成果物は変更しない。
- v3のプロンプトとHTMLに「AI」表記を出さない。
- 秘密情報・`.env` の内容を文書に転記しない。
- `git push` は施主承認制。本便でも未push。


---

## 2026-08-27 第2便・本走結果（この節が最優先。最終更新 2026-08-27 14:15 JST）

### コミット（`main` は未push・`5548095` から12コミット先行）

`663a352`(案1) → `10957ab`(案6) → `f212ef2`(案2＋案3) → `2cba76f`(案4) → `2a75414`(mock整合) →
`b2de14e`(`tools/run_metrics.py`) → `11928e7`/`d3035e1`(docs) → `c2c5d24`(自摘: 報告前の回答が依頼主に見えていた) →
`129cb94`(自摘: 登記がライブ表示で、再照会しなくても名義変更が見えていた) →
`a6c479d`(**Codexレビュー反映**: 意向確認の状態遷移の強制・報告先の固定・欠損の非補完・ID種別照合・依頼先のrole検証) →
`9a7c968`(依頼主の文面を仲介へ配送) → `a81e171`/`424f4f6`(docs)。

### Codexレビュー（gpt-5.6-sol・2026-08-27）

**ルールベース抵触の指摘は0**（案件記録の「状態」列・「参入前調査の照会と同一」・`owner_intent` のenum・
案6の結果返却は、いずれも事実表示であり原則内と明記された）。一方で**重大5件**の実装欠陥を指摘され、
すべて `a6c479d` で修正した：①未回答のまま報告できた ②依頼主でない相手へ報告でき、元依頼主にも見えた
③再照会・再回答で状態が巻き戻った ④案件記録が他者宛ての報告を拾っていた ⑤欠損した希望額を `unknown` に補完していた。
中3件（依頼先のrole未検証・照会区画の差し替え・ID種別の未照合）も同時に修正。回帰テストは **146 passed / 0 failed**。

### 実API（本タスクでの支出 合計 $2.910）

- 試走3か月 `…_1243_49`（$0.0325）：打切り0・無効0・`no_such_offer` 0。
- 参考1本 `…_1322_52`（$0.7228）：Codex修正**前**の `129cb94`。分布には数えない。
- **本走3本**（すべて `a6c479d`・seed 85・config無変更）：
  `…_1334_54`($0.7138) / `…_1345_55`($0.7132) / `…_1356_56`($0.7278)。

主要な実測（詳細は `RESULTS.md`）：

- `no_such_offer` は3本とも **0**（run36は77件）＝**欠陥Aは解消**。
- `check_land_registry` は3本とも **0回**・重複照会 **0回**（run38は160回・重複146回）＝**欠陥Cは解消**。
- 所有率は3本とも0%。**runCのみ** `make_lease_offer` 3件から `lease_control` 1件が成立し、実効支配 **1.98%**。
  本プロジェクトで賃借経路が実際に動いたのは初めて。
- X社の反復は「登記照会」から「仲介への依頼」へ移った（`request_owner_inquiry` が 232/210/220）。
- 仲介が実際に照会した件数は **0 / 2 / 6** ＝分布。runAの仲介は120回すべて `hold`
  （理由欄「特に取るべき行動がない」「X社からの連絡を待つ」）。依頼は観測に届いていることを実プロンプトの再現で確認済み。
- `make_offer` は3本とも0件。

### 次に行う作業

1. **施主判断待ち**：`docs/broker_channel_proposals.md` の**案B（仲介手数料の会計を世界に入れる）**と
   **案C（仲介にも月次オペ枠を持たせる）**。入れるなら1つずつ（どちらが効いたか分からなくなるため）。
2. 採否が決まったら実装 → `tests/test_ledger.py` → mock60か月 → **同一設定3本で取り直し**（1本 ≈ $0.72）。
3. 費用：本タスクの上限は $4.5、実支出 $2.910。**次バッチ3本には $2.2 前後が必要**なので、追加のGOが要る。
4. レポートHTMLは `yamachas0.github.io/quiet-acquisition-report`（gh-pages・別作業ツリー
   `C:\Users\user\projects\quiet-acquisition-pages`）へ公開する。**ソースリポの push は禁止のまま。**

### 変わっていない制約

ルールベース排除／観測に無い事実をコードで補完しない／`mvp_v1`・`field_v2` の設定と成果物は不変／
v3のプロンプトとHTMLに「AI」表記なし／`.env` の内容を文書に書かない／`git push` は施主承認制。


---

## 2026-08-27 第3便・v4-mini（この節が上記すべてに優先する。最終更新 2026-08-27 16:00 JST）

担当：CEO=Fable / CTO=Opus 5。施主GO（8/27 14:51「一旦これでやってみて」・14:54「X社も flash-lite でよい」）。

### 実装（`main` は未push・`9f88604` から4コミット先行）

| commit | 内容 |
|---|---|
| `b51915d` | v4-mini 本体。`src/field_v4.py`（同期3フェーズの世界API・観測・スキーマ・プロンプト）、`configs/config_field_v4.yaml`（12か月・seed 85・**全主体 gemini-2.5-flash-lite**・personasはv3流用）、`src/simulation.py` に `_step_v4`、Mockのv4対応、`tests/test_ledger.py` にv4回帰、`tools/run_metrics.py` に `metrics_v4`。 |
| `8b51f57` | Codex実装レビュー反映（重大2・高3・中3）：届出競合の終端化と自己売買の拒否、`no_response` の存在・open・所有者検証と未応答の買付ごとの記帳、応答フェーズへ当月の観測と記憶を引き継ぎ、feelingを月1件に統合、届出完了移転と賃料清算の順序統一、仲介の観測を仕様どおりに（全区画登記を外す・取次ぎ時点の相手を保持）、結果の8件打切り廃止、`direct`+`broker_id` の矛盾を不成立に、`metrics_v4` の照合式修正。 |
| `35b30a6` | `broker_id` の「取次ぎ先なし」を `"none"` に（**Geminiは `enum` に空文字を入れると 400 を返す**）。スキーマの空選択肢を検査する回帰テストを追加。 |
| `9ab1c8f` 相当 | `RESULTS.md` にv4節、`tools/price_analysis_v4.py`（価格分析）。 |

**v1〜v3のコード・設定・成果物は変更していない。** v4は別ファイル・別config・`scenario_version: field_v4` 分岐。

### v4の設計（要点）

- 月＝同期3フェーズ。①提示（X社が `offers[]{parcel_id, price, under_name, via, broker_id, note}`・`withdraw[]`・`memo`／住民・事業者は `location`+`talk`+`feeling`／仲介は `location`+`talk` だけ／行政・記者は `investigate`・`publish`・（行政のみ）`ordinance`）→②応答（買付が届いている所有者だけ・`responses[]{offer_id, decision(accept|reject|counter|no_response), counter_price}`+`feeling`）→③清算・配送。
- 手続き動詞は語彙に無い。登記は最初から公開。資金は成立時に自動調達。仲介は機構（自動リレー＋手数料3%自動記帳）で、仲介のLLMは話すだけ。
- 条例は行政が `threshold_sqm` / `delay_months` を決めて制定し、**翌月から施行**、後発が上書き、1件の取得面積だけで判定。
- **判定基準は実走前に固定**（`docs/world_design_v4_impl.md`）。集計は `tools/run_metrics.py`（配線の指標と世界の指標を分けて出す）。

### 検証と実走（実API合計 $0.692）

- `python tests/test_ledger.py` → **206 passed, 0 failed**。`--provider mock` 12か月 完走。
- run61（$0.068）＝**配線失敗**。X社のスキーマがGeminiに400で拒否され12か月ぶん全滅 → `"none"` 修正。
- 1か月スモーク run63（$0.006）でX社が第1月に買付6件・エラー0を確認。
- **12か月×3本**（run64 $0.0938 / run65 $0.0810 / run66 $0.0752）＝成約 3/5/5件・所有率 6.5/10.9/10.9%。
  事前固定の判定は **3/3本で「取得が進む」「住民が気づく」の両方が成立**。
- **60か月×1本**（run67 $0.3682）＝成約14件・所有率30.4%・面積24.3%。
  **初報道 第36月・条例 第40月（どちらも所有率10.9%時点）＝検知ラグと手遅れ度が初めて数値化された。**
- レポート公開：https://yamachas0.github.io/quiet-acquisition-report/ （v4の4本＋前便を `v3_phase2.html` に退避）。全URL 200確認。**ソースリポは未push。**

### 要施主判断（v4-miniで見つかった穴・未実装の対策）

1. **金額の桁**：12か月runAでX社が万円と円を取り違えた買付（評価額のちょうど1万倍・最大7,500億円）を出し、
   所有者が受諾して成立した。資金が無制限に調達される設計なので予算で弾かれない。
   対策案＝①調達上限を保有資産の評価額に紐づける ②観測の金額に単位を併記 ③調達に金利。**未実装。**
2. **仲介経由の買付が0件**（4本すべて `direct`）。手数料の経路は一度も動いていない。
3. **自社が既に所有する区画への再買付**が多い（60か月で49件）。世界は正しく弾いている。
4. v4-miniには長期賃借・住民側の売り出し・転出・廃業が無い＝「実効統治」ではなく「所有権取得」の試験である。

### 次に行う作業

施主GO（8/27 15:47）により **v4.1（金額の廃止＋thought統合）** に着手する。仕様＝`docs/world_design_v4_1_no_money.md`。
v4は保存し、v4.1は別ファイル（`src/field_v4_1.py` / `configs/config_field_v4_1.yaml`）で実装する。


---

## 2026-08-27 第4便・v4.1（金額を持たない世界）— **この節が上記すべてに優先する**（最終更新 2026-08-27 17:35 JST）

担当：CEO=Fable / CTO=Opus 5。施主GO（8/27 15:47「金額も消すか。できるだけシンプルに」）。
仕様の正＝`docs/world_design_v4_1_no_money.md`。数値の正＝`RESULTS.md` の v4.1 節。

### 結論（先に読む）

**事前に固定した2つの判定のうち、「住民が気づく」は 3/3 で成立、「取得が進む」は 0/3 で不成立。**
両方3/3のときだけ60か月へ進む取り決めだったので、**60か月ランは実行していない**。
配線は健全（API errors 0・truncated 0・parse_fail 3本で1件・売却判断と帳簿の照合 3本とも一致・帳簿の金額フィールド 0）。
**取得が止まったのは世界がそう振る舞ったからで、実装の失敗ではない。**

### コミット（`main` は未push・`b645faa` から2コミット先行）

| commit | 内容 |
|---|---|
| `f7dbfff` | Codexレビュー（判定「不可」）の指摘をすべて反映。v4.1専用ペルソナ新設／打診の `note` 廃止／応答IDの正規化統一／届出待機列の復活修正と当事者記録／内心と受信文の全文化／条例の欠損補完廃止・60か月上限撤廃・同月競合の記録／`presence_rejected`・`delivery_rejected`・`response_not_recorded` の記帳／`amount`・`memory`・`reasoning` の除去／`run_metrics` に12指標追加／回帰73本追加 |
| `30d48ab` | `thoughts_all.jsonl`（全主体の内心を保存。`thoughts.jsonl` は分類済みの住民・事業者だけなので、X社・行政・記者・仲介の内心が記録から落ちていた） |

`python tests/test_ledger.py` → **346 passed / 0 failed**。
`--provider mock` 12か月 → 完走（`..._1711_70`）。**v1〜v4のコード・設定・成果物は変更していない。**

### この便で新しく作ったファイル

- `configs/personas/field_v4_1.yaml` — v4.1専用ペルソナ。**`configs/personas/field_v3.yaml` は1文字も変えていない。**
  人物構成・年齢・地区・購読・付き合いは v3 と同じで、金銭由来の事情（住宅ローン・固定資産税・事業借入・
  店舗は賃借・査定・金融機関・予算）だけを非金銭の事情（加齢・介護・相続・後継者・維持の手間・通勤）へ置換した。
  **理由**：v4.1 のシステムプロンプトは「この世界に金銭の授受は存在しない」と説明する一方、
  直後のペルソナがローンや固定資産税を実在の事実として提示していた（Codex指摘・14か所を実測）。
- `configs/config_field_v4_1.yaml` は `personas_file` をこの新ファイルへ向け、
  `base_assessed_value` / `base_unit_price` / `block_premium` を削除した（`initial_shop_rent: 0` だけは
  共通の初期化がキーを要求するため残してある）。

### 実API（本便 $0.384・**本タスク累計 $1.076 / 上限 $2.0・残 $0.92**）

- 1か月スモーク `2026-08-27_1713_71`（$0.0064）
- 12か月×3本 `..._1715_72`（$0.1181）/ `..._1717_73`（$0.1483）/ `..._1720_74`（$0.1116）

主要な実測（詳細は `RESULTS.md`）：成約 **0 / 0 / 1 件**、所有率 **0 / 0 / 2.2%**、
打診は3本とも第1月から6件、のべ反応は hold が圧倒的（72 / 405 / 272 のうち hold 72 / 400 / 260）、
取得を話題にした住民 19 / 19 / 16 人、報道は runC だけ10本（うち取得9本）、
条例は runA 第11月・runB 第3〜8月に6回・runC なし。

**公開**：https://yamachas0.github.io/quiet-acquisition-report/ （`v41_runA_72.html` / `v41_runB_73.html` /
`v41_runC_74.html`、前便 v4-mini は `v4_mini.html` へ退避）。全URL 200 確認済み。**ソースリポは未push。**

### なぜ止まったのか（次の担当者がここから始める）

所有者の応答フェーズの内心で、**「仲介」への言及が 58 / 70 / 60 回**、「情報収集」が **44 / 72 / 29 回**出る。
「まずは不動産仲介に相談してから決めたい」「情報収集をしてから判断したい」と書いて hold を選び続ける。
ところが v4.1 の仲介は**噂の源に格下げ**されていて相談を受ける動詞を持たず、打診にも文面が無い（正本どおり）。
**所有者は決めるための材料を取りに行く手段を持たないまま毎月「今月は決めない」を選ぶ。**
そこへ「手放しても何も返ってこない」（対価が世界に無い）が重なる。
**「金額の不在」と「相談経路の不在」は、この3本では分離できていない。**

### 要施主判断（この便で挙がったもの）

1. **X社の非公開目的の文言**。Codexは「できる限り早い時点に」「総面積の過半を」「共通支配下にあると認識される
   ことは避けたい」「法令と契約を守る」を、優先度・成功閾値・秘匿方針・当為の混入＝ルールベース抵触と判定した。
   これは v3 以来の**研究の前提そのもの**で、変えると v4-mini との比較が壊れるため**直さずに残した**。
   直すか残すかは施主判断。
2. **切り分け実験をやるか**。金額は無いまま「仲介に相談する経路」だけ世界へ戻した版を同一設定3本（1本 ≈ $0.12）。
   これが「止まった原因」を特定する最短路である。
3. **60か月×1本（≈$0.6見込み）を回すか**。判定ゲート不成立のため未実行。残予算 $0.92 で回せる。
4. **同月に2つの行政が条例を制定したときどちらを有効にするか**。同期フェーズ内に「後発」は無いので、
   現在は agent_id 順の後勝ちで、その事実を `ordinance_same_step_conflict` に記録するだけにしてある。
5. **行政に区画面積の分布を観測させるか**。runA・runB の行政は閾値を 1件1000㎡超に置いたが、
   A市の区画は最大450㎡で**条例は一度も発動しなかった**。行政の観測には名義別の面積合計しか出ず、
   閾値を決める材料が無い。

### 変わっていない制約

ルールベース排除（売却確率・閾値・強制イベント・「〜なら〜しろ」の指示文・台本を足さない）／
観測に無い事実をコードで補完しない・欠損は不成立として残す／`mvp_v1`・`field_v2`・`field_v3`・`field_v4` の
設定と成果物は不変／プロンプトとHTMLに「AI」表記を出さない／`.env` の内容を文書に書かない／
`git push`（ソースリポ）は施主承認制＝本便でも未push。


---

## 2026-08-27 第5便・v4.1b（相談プロトコル＋行政の面積観測）— **この節が上記すべてに優先する**（最終更新 2026-08-27 19:00 JST）

担当：CEO=Fable / CTO=Opus 5。施主GO（8/27 17:35「quietは1と4やって」）。
仕様の正＝`docs/world_design_v4_1b_broker_consult.md`。数値の正＝`RESULTS.md` の v4.1b 節。

### 結論（先に読む）

**「取得が進む」0/3（v4.1 と同じ）・「住民が気づく」3/3（v4.1 と同じ）。相談経路を足しても取得は動かなかった。**
ただし**相談自体は猛烈に使われた**（consult 137/110/81件・所有者20人中18/16/13人・毎月8〜12人）。
所有者の内心 240本中 219〜222本に「相談」の語。**「相談したい」は本物だったが、相談できても売らない。**
残る候補は **(b) 対価の不在**。ただし断定はできない（下記の配線の弱点）。
**行政の面積分布は効いた**：v4.1 では閾値1000㎡＞区画最大450㎡で空振りだったのが、
runA の行政は第1月から100㎡を選び、**本プロジェクトで初めて届出が実際に発動した**（第12月・1件）。

### コミット（`main` は未push・`834b772` から2コミット先行）

| commit | 内容 |
|---|---|
| `4d50cfc` | v4.1b 本体。`src/field_v4_1b.py`（consult/advise・相談の機械記録・区画面積の分布・条例の適用実績）、`configs/config_field_v4_1b.yaml`、`src/simulation.py` の v41b 分岐、mock の v4.1b 対応、`tools/run_metrics.py` の `metrics_v41b`、回帰108本 |
| `687f557` | 走行前Codexレビュー（判定「不可」）の指摘6件を修正＋`docs/world_design_v4_1b_broker_consult.md` §8 |

`python tests/test_ledger.py` → **469 passed / 0 failed**。`--provider mock` 12か月 完走。
**v1〜v4.1 のコード・設定・成果物は変更していない**（実測：変更後に v4.1 の mock を再走し、
`ledger.jsonl` がバイト一致・プロンプト357本が多重集合として完全一致）。

### 走行前Codexレビューで直した6件（`docs/world_design_v4_1b_broker_consult.md` §8 に詳細）

①回答済み状態の同月漏れ ②配送IDを `Q0001` に統一＋`CONSULT-`/`ADVICE-` の正規化
③相談 月1件・回答 月6件を台帳側でも強制 ④遅延0か月の条例で届出対象が偽の0になる誤集計
⑤events 側の不成立を metrics に合算 ⑥**レポートHTMLが金額の世界を再導入していた**（重大）
＝v4.1b 専用の moneyless レンダラを追加し、生成HTML全文に金額語が無いことをテストで固定。

### 実API（本便 $0.410・**本タスク累計 $1.486 / 上限 $2.0・残 $0.51**）

1か月スモーク `..._1752_77`（$0.008）／12か月×3本 `..._1840_81`（$0.1416）・`..._1843_82`（$0.1137）・`..._1846_83`（$0.1467）。
**公開**：https://yamachas0.github.io/quiet-acquisition-report/ （`v41b_runA_81/82/83`・前便 v4.1 は `v41.html` へ退避）。
**ソースリポは未push（禁止どおり）。**

### 次の担当者への申し送り（重要度順）

1. **仲介の月が出力打切りで消える**：仲介の第1フェーズが `PARSE_FAIL` になり、broker月の 8/24・7/24・9/24 が消失
   （v4.1 は 0/0/1）。相談一覧（本文つき）で観測が伸びたのが原因。`truncated_responses` は 0 のままで**当てにならない**。
   対策案＝①仲介だけ `max_tokens` を上げる ②回答の目安字数を短く提示 ③相談一覧は件数だけにして本文は配送で読ませる。
   **どれも同一設定を崩すので、入れるなら3本取り直し（≈$0.42・残予算 $0.51 でぎりぎり）＝施主判断。**
2. **交絡**：相談経路と行政観測を同時に入れている。行政の公表・条例は全主体へ配送されるので、
   取得・認知の変化を相談経路だけに帰属させることはできない。分離するなら相談だけの版を3本（≈$0.42）。
3. **v4.1 にも私信の相談は最初からあった**（所有者→仲介 63/90/31通）。v4.1b は「復活」ではなく
   「専用の相談プロトコルの追加」。今後この案件の文書では前者の言い方をしない。
4. **X社の mandate の当為表現**（v4.1 の要判断1と同じ）は未処理のまま。
5. 60か月は施主指示により未実行。

### 変わっていない制約

ルールベース排除／観測に無い事実をコードで補完しない・欠損は不成立として残す／
`mvp_v1`・`field_v2`・`field_v3`・`field_v4`・`field_v4_1` の設定と成果物は不変／
プロンプトとHTMLに「AI」表記を出さない／`.env` の内容を文書に書かない／`git push`（ソースリポ）は施主承認制＝本便でも未push。

---

## 第6便（2026-08-27）— v5：出来事はピン留め・観測するのは街の会話

### 何をした

施主指示（19:23／19:40／19:46）で世界の構えを反転させた。**取得は台本（固定の出来事）として挿入し、
LLMの仕事は街の会話だけにする**。v1〜v4.1b のファイルは一切変更していない。

- 新規：`src/field_v5.py`／`configs/config_field_v5.yaml`／`configs/events_v5_seed85.yaml`（台本・凍結）／
  `tools/build_events_v5.py`／`docs/world_design_v5_impl.md`（実装仕様・**走行前に固定**）
- 追記：`simulation._step_v5` と `_v5_*`／`llm_client_factory._mock_v5` と `max_token_finishes`／
  `run_metrics.metrics_v5`／`viz.MONEYLESS_VERSIONS` に `field_v5`／`tests/test_ledger.py`（v5で**112本追加・全566 passed**）
- 実API：1か月スモーク2回（1回目は配線欠陥の検出で全滅）＋**12か月×1本**

### 結果（1本目・run94）

15件の取得のうち **8件が街に気づかれ、7件が気づかれずに終わった**（追跡3か月以上のコホートでは 33.3% が
気づかれない）。8件のうち7件は**取得と同じ月**に、1件は**6か月遅れ**で表に出た。
最初の情報源は**売った本人**（第2月・役所窓口で自分から言った）。第7月に記者が窓口で**5区画を一括照会**し、
個別の話が「複数物件の連続」に変わる。第12月には26人全員が語り、話題は
**「B社のP11地区への動き」＝共通の買い手の枠組み**になった。詳細と引用は `RESULTS.md` の v5 節。

### 走行前に塞いだ穴（Codexレビュー2巡・判定はいずれも「不可」→26件修正）

結果の妥当性に直結する2件を必ず引き継ぐこと。

1. **隣接区画の現在の登記名義を毎月見せていた**＝台本の可視範囲を迂回する第二の通知機構。塞いだ。
   実証：塞ぐ前のスモークは第1月から取得への言及が9件、塞いだ後は0件。
2. **月末の姿勢（sell/keep）を毎コール尋ねていた**＝売却話題のプライミング。月内の出席は計画確定時に
   分かるので、**その主体の今月最後の会話ターンで1回だけ**に変えた。

他24件（空文字enumで実APIが400／単独参加者に兆候が届かない／`truncated` が v5 では機能しない／
記事の確定タイミング／metrics の帰属と右打切り／D3を配線の不変条件に、等）は
`docs/world_design_v5_impl.md` 11章に全件記載。**採らなかった指摘と理由も同じ表にある**。

### 次の一手（優先順）

1. **`max_tokens` 1400→1800**。本走で 2,442コール中5本が打切り（0.2%）→ その主体のターンが落ちた。
   設定変更なので、比較したい本は同じ設定で揃えること。
2. **同一設定で本数を増やす**（seed85・12か月・**約$0.96/本**）。1本は1実現。最低3本。
3. 実験条件（実装仕様7章）：E1 記憶の渡し方／E2 シーン構成／E3 兆候の量。**出来事は全条件で同一**。
4. **台本の作り直し**：現行は28件すべてが第24月までに実行され、第25〜60月が0件（生成器が総数で
   打ち切るため）。60か月版を回す前に必須。
5. 噂の内容の事実性は未実装（今は「参照の正確さ」の共起判定にとどめ、主結論に使っていない）。

### 要施主判断

1. **`stance`（月末の sell/keep）を残すか**。Codexは「取引の意思決定に見えるのに台帳に接続されておらず、
   台本に上書きされたように読める」として廃止を推奨。CTO判断では**施主仕様なので消さず**、
   「手放すことを考えているかの内心の測定」と明記して残した（D1/D2の判定には使っていない）。
2. **本数と予算**。本便の実支出は **$1.12**（スモーク2回＋本走1本）。上限 $2.0 に対し、
   同一設定の2本目は $0.96 見込み＝**累計 $2.08 で上限を超える**。追加GOが要る。
3. 兆候の会場割り当て（`GOSSIP_VENUES`）は区画の物理位置と無関係＝「中立なbaseline」ではなく
   **事前固定した露出条件**である、という整理でよいか。

### 変わっていない制約

ルールベース排除／観測に無い事実をコードで補完しない・欠損は不成立として残す／
`mvp_v1`・`field_v2`・`field_v3`・`field_v4`・`field_v4_1`・`field_v4_1b` の設定と成果物は不変／
プロンプトとHTMLに「AI」表記を出さない／`.env` の内容を文書に書かない／
`git push`（ソースリポ）は施主承認制＝**本便でも未push**。

---

## 第7便（2026-08-28）— v5b：占領の認知・売主は取引の当事者・24か月×3本

### 何をした

施主指示2点（22:18「観測したいのは X社が実質占領しようとしてるんじゃないかに気づくかどうか。1件1件では
ない」／22:27「所有者の知らないところで登記が変わるのは詐欺。所有者は自分が売買・賃借したことを認知して」）
を実装し、**24か月×3本**を走らせた。v1〜v5 のファイルと成果物は不変。

- 新規：`configs/config_field_v5b.yaml`／`configs/events_v5b_seed85.yaml`（凍結）／`tools/build_events_v5b.py`／
  `docs/world_design_v5_impl.md` 12章（走行前に固定）
- 追記：`field_v5.apply_script_v5`（sale/lease・当事者の取引記録）／`own_history_rows_v5`（当事者の文言）／
  `kpi.classify_occupation`／`run_metrics.occupation_metrics`（O1〜O4）／tests（v5bで **+56本・全619 passed**）

### 結果（詳細と引用は `RESULTS.md` の v5b 節）

世界は3本とも同一：24か月で取引28件（売買19・賃借9）、X社は非公共46区画の**所有41.3%／所有+賃借60.9%**。

- **「同じ相手が複数を動かしている」までは届く**：O1 は3本とも成立。runB/runC は**26人全員が公の場で**
  口にした。runA は8人（30.8%）どまりで、**17人は内心にとどめた**。
- **しかし「買い占め・地上げ・乗っ取り」とはほぼ誰も言わない**：意図の語は**3本合計22,686行のうち7行**、
  判定成立は 0 / 3 / 2 件。**支配60.9%まで進んでも街はそれを「占領」と呼ばなかった。**
- 最も近づいたのは**自分がB社に売った当事者の内心**（runB 第18月）で、そこでも動機は
  「温泉権維持のために買い集めている」という**善意の説明**に収束した。
- **同じ台本でも社会的結果は割れる**（公言26人 vs 8人）＝1本では何も言えないことの実証。
  runA と runB/C の違いは記事の本数（4本 vs 19/17本）と相関。

### 走行前に直したもの（Codexレビュー1巡・判定「不可」→9件）

**賃借が既存の店子を無通知で消していた**（使い手が居る区画は賃借にしない、で解決）／**分類の欠損が主値で
false に化ける**（unknown として主値から外し、D3の合格条件に追加）／**判定が未来の取得を参照できた**
（その月までに成立した分だけで判定）／O3の名実（公言と内心の分離）／v5 再実行の挙動保全／
台本validation強化／賃借イベントの記録／支配率の分母／noteと用途の矛盾。全件 12章の表に記載。

### 未解決・要施主判断

1. **打切りが残る**：`max_tokens 1800` でも 5/8/10本（0.1〜0.2%）。D3 は**不合格**。次は上げるか、
   長い内心を書く主体だけ個別に上げる。
2. **O2 が出ないのは世界の性質か語彙の問題か**。**偽陰性の人手監査は未実施**＝言えるのは
   「保存された出力に事前定義の O2 を満たす表現はほぼ無かった」まで。断定していない。
3. **LLM分類器の過剰検出**（ルール0件の runA でも単独では1,703行を intent と判定）。主値はルール∧LLM。
4. **60か月**（台本は第47月まで実体がある）。24か月で支配60.9%＝その先で呼び名が変わるかが本題。
5. 実費：本便 **$5.92**（スモーク2回$0.16＋3本$5.76）。上限$8.0内。

### 変わっていない制約

ルールベース排除／観測に無い事実をコードで補完しない・欠損は不成立として残す／
`mvp_v1`〜`field_v5` の設定と成果物は不変／プロンプトとHTMLに「AI」表記を出さない／
`.env` の内容を文書に書かない／`git push`（ソースリポ）は施主承認制＝**本便でも未push**。

---

## 第8便（2026-08-28）— v5c：買い手の戦略・区画の属性・日常の場・4段階の色

### 何をした

施主FB（09:15「土地取得の順番や選択で…手を付けやすそうなところ、目立たないところとか関係なく機械的に
買ってってる。台本が適当すぎる。MAPも面積とか都心郊外くらいの分けはした方がいい」「銭湯とか近所の居酒屋とか
そういうなんでもないところの会話から火が付くかを検証したいが、そういう場の設定が少ない」）と
確定指示（09:21「①個別の売買だけは青、複数の関係性や面的な話は緑、X社にたどり着いたら黄、行政が規制に
動いたら赤 ②途中で一等地の売買が発生するイベントを1個 ③会場増やして4シーンでまずは」）を実装し、
**24か月×3本**を走らせた。v1〜v5b のファイルと成果物は不変。

- 新規：`configs/config_field_v5c.yaml`／`configs/parcels_v5c.yaml`（区画属性・凍結）／
  `configs/events_v5c_seed85.yaml`（台本・凍結）／`tools/build_parcels_v5c.py`／`tools/build_events_v5c.py`／
  `src/field_v5c.py`／`tools/build_audit_v5c.py`／`tests/test_v5c.py`（73本）／
  `docs/world_design_v5c_buyer_strategy.md` §7（実装の記録・走行前に確定）／`docs/audit_v5c_sample.md`
- 追記：`kpi.classify_stage_v5c`（4段階の色）／`run_metrics.stage_metrics_v5c`（色の推移・場所別の火種・
  一等地イベント前後）＋機会量（同席・単独率・同席ペア・兆候配送）／`simulation` の v5c 分岐／
  `build_present_data.py`＋`present.html`（右地図を4色で描く・色のパネル）

**走行前に Codex レビューを1巡入れ、P0 7件を全部直してから台本を再凍結した**：
①未来の一等地が第1月から「既取得」として隣接評価に混入していた ②一等地イベントが月の件数に加算されていた
（→枠の置換に変更） ③一等地が「営業中の店」でなかった（→P30 から **P04** へ） ④色の人数が排他的でなかった
⑤初出が月単位で記事に先を越されていた（→月→シーン→ラウンド順・内心と記事に round を記録）
⑥usage が分類器の消費を含まず費用が過少計上（**v5b の実費も $5.76 ではなく $6.29 だった**）
⑦色分類の欠損が健全性ゲートに入っていなかった。

### 結果（詳細と引用は `RESULTS.md` の v5c 節）

世界は3本とも同一：24か月で取引31件（売買22・賃借9）、X社は**所有47.8%／所有+賃借67.4%**。
戦略は効いた（取得区画の目立ちやすさは前半2.53→後半5.0）。会場を8→15に増やしても
**会話量はほぼ痩せず**（発話 -4〜-12%）、一度でも同席した組み合わせは v5b と同じ 325（＝全組み合わせ）。
**実際に人の目に入った兆候は v5b の半分近く（149/146/136 対 268/283/268）＝露出は薄い条件だった。**

- **青**は3本とも全員に届く。**緑**（複数の取引・面として語る）は
  **runA 26人・runB 26人・runC 22人**が到達し、v5b で 0〜3件しか無かった「街ぐるみ」の語りが**日常語になった**。
- **黄（同じ買い手だと結ぶ）は、人手で読み直すと3本で1人だけ**（runA 第23月・HH08「C社がA社と
  関係があるのではないか」）。機械判定は runB/runC でも黄を出すが、原文は「P43はB社に、P44はA社に移った」と
  **2件を並べただけ**＝緑相当だった。
- **赤（行政が規制に動く）は3本とも0。** 行政の発言は「窓口で承ります」「把握しておりません」
  「情報収集に努めます」止まり。最接近は runB 第17月「住民の懸念を把握し、今後の対応を検討していく」。
- **一等地の売買（第12月）の前後で報道は増えていない**（記事本数は3本とも前後不変）。色の広がりも
  runA/runC は増え runB は減って方向が割れた＝**1件のイベントの効果はこの3本からは何も言えない**。
- **火が点いた場**：緑の初到達は 地域イベント会場12・共同浴場10・公民館9・**スーパー8**・仲介7・**床屋5**・
  **回覧板4**…。本便で足した「なんでもない場」からの初到達は青28%・緑26%。
  ただし**着火点は依然として登記を見られる場所（市役所・仲介）**で、日常の場は**火が回る場所**だった。

### 人手監査（本便で初実施・`docs/audit_v5c_sample.md`）

3層200行（ルール陰性100／ルール○LLM×50／主値陽性50）を CTO が読んだ実測：
**偽陰性 8.0%**（うち6件は主体が**英語で内心を書いた**行＝日本語のルール語彙が拾えない。v5・v5b も同じ穴）／
**偽陽性 6.0%**／**段階が過少 42%**（機械の青の4割強は人が読めば緑＝街は「P04」でなく「R04さんの土地」と
人の名前で語るため区画IDのルールが取りこぼす）。
**CEO が疑った2件（緑の初出＝床屋の世間話／赤の初出＝第1月の窓口の一般相談）は、両方とも偽陽性と確定。**

### 次にやるなら

1. **黄に届く条件を切り分ける**：runA だけが第23月に届いた。記者の登記照会・仲介の見聞・
   当事者の内心のどれが効いたかを、3本の経路ログ（`deliveries.jsonl`／`traces_v5.jsonl`）で追う。
2. **60か月**（台本は第46件＝第60月まである）。24か月・支配67.4%で黄が1人なら、
   その先で「誰がやっているか」に届くのか。
3. ルール語彙に**英語**と**人名で語られた土地**（「R04さんの土地」）を足す。これは v5・v5b の再集計にも効く。
4. 行政に制度が無い世界では赤は「言っただけ」しか測れない。**届出・条例という機構を足すかは施主判断**
   （足せば「気づかせる仕組み」ではなく「行政の可能な行為」を足すことになるので、絶対ルールとは矛盾しない）。

### 変わっていない制約

ルールベース排除／観測に無い事実をコードで補完しない・欠損は unknown として残す／
`mvp_v1`〜`field_v5b` の設定と成果物は不変／プロンプトとHTMLに「AI」表記を出さない／
`.env` の内容を文書に書かない／`git push`（ソースリポ）は施主承認制＝**本便でも未push**。


---

## 第9便（2026-08-28）— present.html 第2弾：地図の配置・原文の提示・言葉の平易化

### 何をした

施主FB（地図が格子のままで面積も都心/郊外も分からない／同月の点灯が一斉で見えない／
タブ名と文面が詩的で伝わらない／火が点いた瞬間の原文が読みたい／区画の呼び名）を、
**シミュ本体・台本・分類器（`src/` `configs/` `run.py` `simulations/`）に一切触れずに**画面側だけで実装した。

- `tools/build_present_data.py`（`meta.schema` を 3→4）
  - **`layout`**：`configs/parcels_v5c.yaml` から**決定論に**配置を計算して JSON に入れる
    （view 1000x760・マスの大きさ＝面積の4分位で 30/42/54/68・目標半径＝中心 0.20R／中間 0.52R／郊外 0.86R・
    LCG seed 8501 のジッター・重なりの押し離し gap 7・縦潰し 0.78・緩和500回）。
    HTML は座標を描くだけで、配置計算を JS でしない。
    run の config に `parcels_file` が無い v5/v5b は `layout=null` で従来の格子表示にフォールバックする。
  - **`parcel_naming`**：区画に固有名は無いという事実を実測で示す。持ち主の名前（R03 等）が
    会話・内心・記事の本文に現れた回数を数える（v5cA は延べ 61,843 回・48区画すべてが呼ばれている）。
  - **`ignition`**：緑・黄の**初出行**を、前後の原文つきで機械抽出する。
    並び順は `(step, SCENE_ORDER[scene], round, utt_id, from)`。`stage_stats["first"]` も同じキーに直した
    （それまでは月だけで並べていて月内の順が不定だった）。
  - `stage_by_parcel[].text` の 160 字切り詰めを**やめ**、`venue` / `scene` / `utt_id` / `name` を足した。
- `present.html`（実体は Pages 作業ツリー）
  - 両方の地図を**同一座標の SVG** にした（マスの大きさ＝敷地面積・中央＝市街地・外側＝郊外）。
    `side>=54` のマスには区画IDを、街が持ち主の名前で呼ぶ区画にはその名前も出す。
  - **同じ月に色が付く区画の点灯をずらす**（pid の 32bit FNV-1a から 0.10〜0.40 秒の位相差。`Math.random` 不使用。
    巻き戻し・run 切替では 0 に戻す。`prefers-reduced-motion` では無効）。
  - ヘッダー直下に**4色の定義を常時表示**する帯、その下に**読み方ガイド**（5行）を置いた。
  - 新パネル **「火が点いた瞬間（原文）」**（補助パネルの先頭・既定表示）＝緑と黄の初出を
    ①直前に聞いた発言 ②その月に目に入った記録 ③本人の内心 ④火が点いた行 ⑤直後の反応 の順に原文で出す。
    内心（灰色・「頭の中（誰にも伝わっていない）」）と発話（白・「口に出した（同席者に届いた）」）を必ず見分けられるようにした。
  - **右地図の色が付いた全マスがクリック／Enter で開く**ようになった（それまでは色が付かなかった区画だけ）。
    ダイアログに区画の事実・登記が動いた月・街が語り始めた月・**その差**・原文全文を出す。
  - タブ名と見出しから比喩を消した（火が点いた瞬間（原文）／頭の中で思っただけ・口に出した／
    市役所で登記をまとめて見た人／実際には売買されていないものが、うわさで結びついた／
    設計を足しても動かず、減らしたら動いた（v4→v5）／青→緑→黄の推移と、最初に話題になった場所／
    この実験で言えること・言えないこと）。
- `tests/test_present.py`：layout・ignition・parcel_naming・原文の非切り詰め・
  読み方ガイドの手順どおりの実操作（色付きマス→ダイアログ→火が点いた瞬間タブ→緑の原文）を追加。

### 実測（検収）

- `tests/test_present.py` **213 passed / 0 failed**、`tests/test_ledger.py` 619、`tests/test_v5c.py` 73（いずれも 0 failed）。
- 7本すべて再生成して壊れないことを確認（run94・v5bA/B/C は `layout=null` で従来表示）。
- 配置の検査（v5cA/B/C 同一）：**マスの重なりの最大 0.5039**・平均半径 中心 93.14 < 中間 175.2 < 郊外 275.97。
- スクリーンショット `docs/shots_present2/`（1280 の全景・390・再生途中2枚・火が点いた瞬間・タブ一覧・ダイアログ・4色の帯）。

### 仕様と実物が食い違った点（**要判断**・CTO へ差し戻し）

配置の定数（中心 0.20R・gap 7・半径の戻し 0.12）のままでは、指示された重なり検査
（任意の2マスの重なり <= 1 単位）を**満たせない**。中心ゾーンは 48 区画中 **16** で、
目標半径 64 の楕円の面積 約1.0万に対して正方形の面積合計が 約3.7万＝物理的に入らない。
押し出された分だけ毎回の半径の戻し（0.12×差）が 7 単位の隙間を食い破り、最大重なりは **4.48** で止まる。
**定数は動かさず**、緩和500回のあとに「押し離しだけ」を決定論に追い足して解いた（追加 2 パスで 0.50）。
定数を変える案（中心の係数を上げる／gap を下げる／戻しを弱める）に替えるかは CTO 判断。

### 変わっていない制約

ルールベース排除／観測に無い事実をコードで補完しない／`mvp_v1`〜`field_v5c` の設定と成果物は不変／
プロンプトとHTMLに「AI」表記を出さない／`.env` の内容を文書に書かない／
`git push`（ソースリポ）は施主承認制＝**本便でも未push**（Pages リポのみ push）。

### 追加便（同日）— 「火が点いた瞬間」を strict／loose の2本立てに

`ignition` を `{"green":{"strict","loose"},"yellow":{...},"criteria":{...}}` に変えた（`meta.schema` = **5**）。
理由＝`tools/run_metrics.py` の `_v5c_rule_green` は「この辺り」等の面的な語だけで真になるため、
**定義どおりの初出（loose）は3ランとも第1月の世間話**になっていた（人手監査が偽陽性と断じた種類の行）。
ルールは変えず、表示する行の選び方を1本足して直した。
`strict` ＝ その月までに成立した取得区画IDが2つ以上、または名義が2つ以上、本文に**実際に並んでいる**最初の行
（緑は加えて LLM の `area`、黄は `same_buyer`、どちらも `classified`）。判定は既存の
`_v5_mentions` / `holders_by_step` / `acquired_by_step` / `V5_PARCEL_RE` だけで作り、新しい語彙は足していない。
画面は strict を主役にし、loose は `<details>` に畳んで「判定の甘さも見えるように」残した。
色の判定が無い run（run94・v5bA/B/C）も同じ形（中身は null）で出す。

- strict の実測：runA 緑＝第2月 HH10 内心（P33・P41）／runA 黄＝第16月 HH04 発話（A社・B社）／
  runB 緑＝第3月 HH09 内心（P17・P34・P41）／runB 黄＝第6月 HH15 内心（A社・B社・D社）／
  runC 緑＝第2月 HH01 内心（A社・B社・D社）／runC 黄＝第3月 HH01 内心（A社・B社・D社）。
- `tests/test_present.py` **339 passed / 0 failed**、`test_ledger.py` 619、`test_v5c.py` 73（0 failed）。

**仕上げ（schema 6）**：「火が点いた瞬間」の④の見出しが `第undefined月` になっていたのを直した
（`whoOf()` が `r.step` だけを読んでいたが、ignition 本体は `month` を持つ＝`r.step ?? r.month`）。
併せて `ignition.criteria.strict` を `strict_green` / `strict_yellow` に分け（緑は「区画2つ**または**名義2つ」、
黄は「名義2つ」＝条件は変えず文言だけ色ごとに正した）、画面の strict ブロックの説明はこの JSON から描く。
strict の判定に `stage == color` を明示的に足した（実測では既に全 run で一致＝**7本再生成して strict 6件は前便と完全に同一**）。
テストに「1280 と 390 のページ全体（`document.body.innerText`）に `undefined` / `NaN` が出ない」網を足した（他の穴は見つからなかった）。

---

## 【追記 2026-08-28】API費の節約 第1便（CTO）

正本は `docs/cost_saving_v1.md`。要点だけ：

- **既定は従来どおり。** `configs/config_field_v5c.yaml` は無改造で v5c を再現する
  （改修前の mock ランと出力9本がバイト一致することを確認済み）。
- **節約設定は `configs/config_field_v5c_econ.yaml`**（v5c との差は `llm:` と `run_name` だけ）。
  中身は `batch_classify: true` の1点だけが効いている。
- **効くのは①分類器の Batch 化だけ**（24か月3本 $4.89 → $4.14）。
  ②キャッシュは前置き 929 トークンでは載らない（API の最低は明示2,048／暗黙1,024）。
  ③出力上限は 2,200 に一度も当たっておらず削減額ゼロ。
- `batch_agents` のスイッチはあるが **既定 false**（Batch の往復が中央値301s・最長728s ＝
  216回直列で 18〜44時間/本）。
- Batch のジョブIDは `run_dir/batch_jobs/<tag>.json` に残る＝落ちても取り直せる（実APIで確認）。
- 検証: `python tests/test_cost_saving.py`（43件）／`python tools/cost_saving_v1.py <run_dir> ...`／
  `python tools/verify_batch_classify.py <run_dir> --rows 50`（実APIを叩く）。
- **24か月の本走は未実施**（施主GO待ち）。上の $4.14 は実測トークンに単価を掛けた試算。

---

## 第10便（2026-08-28）— present.html 第3弾：一般向けの「街」にする

### 何をした

施主FB（「もっとわかりやすい面積差」「画面いっぱい使って街だとわかるように」「建物のアイコン」
「敷地の名前見えねぇよ」「創発が起きる区画も明示」「創発したら発光」）を、
**シミュ本体・台本・分類器（`src/` `configs/config_*` `configs/events_*` `run.py` `simulations/`）に
一切触れずに**、画面と `tools/build_present_data.py` と `tests/` だけで実装した。

- `tools/build_present_data.py`（`meta.schema` 6→**7**）
  - **マスの辺を面積の平方根に比例**させた（4分位を廃止）。`side = 34.0 × √(面積/最小面積)`。
    実測 **34.0〜86.21px＝辺で 2.5355 倍・描画面積で 6.4286 倍**（`layout.checks` に出す）。
    面積が増えるので `LAYOUT_VIEW` を **1400×900**、`LAYOUT_R` を **430.0** に上げた
    （`LAYOUT_ZONE_F` / `LAYOUT_GAP` / `LAYOUT_SQUASH` / `LAYOUT_PULL` / `LAYOUT_SEED` は不変）。
    重なりは **max_overlap 0.0**（追い足しの押し離し1パス）、平均半径は 中心105.31 < 中間221.94 < 郊外371.90。
  - **区画の呼び名**：`configs/parcel_names_v5c.yaml` から `name` / `locality` / `use_word` /
    `owner_label`（**初期**所有者の呼び名）を `layout.cells` に入れる。表に無い pid があれば例外で落とす。
    `layout.naming = {source, n:48}`。既存の `parcel_naming`（実測）はそのまま残した。
  - **会場を地図に置く**：`configs/venues_v5c.yaml` から `layout.venues`（id/label/badge/cx/cy/r=22）。
    区画と同じ極座標で置き、当たったら**会場だけ**を決定論に外へ押し出す（隙間の最小 **6.089**）。
    `layout.venues_note`＝表示位置は装飾。**集計・判定のコードがこの YAML を読んでいないことを tests で固定**。
  - **`ignition_timeline`**（新規）：月 → その月に**人ごとに初めて**緑／黄へ達した出来事。
    `kind` は `speech`（同席者に届いた発話）／`thought`（内心）。`heard_by` はデータにある同席者のみ。
    材料は既存の strict と同じ（`stage` / `_v5_mentions` / `holders_by_step` / `acquired_by_step`）＝
    **新しい語彙・新しい判定は足していない**。
  - **`party_present` / `party_lens`**（観測レンズ）：その行の場に、その月までに成立した取引の
    当事者（売主・貸主）本人が居合わせたか。同席は `plans_v5.jsonl` の行き先から機械抽出。
    `ignition.*.strict` にも同じキーを足した。**3本の実測＝緑に初めて達した人のうち当事者が同席していたのは
    runA 19/26（73.1%）・runB 20/26（76.9%）・runC 15/21（71.4%）、黄は runA 4/4・runB 5/6・runC 1/1**
    （相関の観測であり、仕掛けは作っていない。記事経由の初到達は runA 黄1件・runC 緑1件を年表から除外して別掲）。
- `present.html`（Pages 作業ツリー）
  - 地図は**2枚を切替タブ**にして1枚を大きく出す（1280 で描画幅 **1186px**・390 では横スワイプ、
    ページの横スクロールは0）。座標は切替えても同一。
  - **建物のピクト**（旅館＝湯気／店＝庇／事業所＝角ばったビル／家＝切妻／古家＝傾いた切妻＋ひび／
    空き地＝破線＋草／集会所＝旗）を単色SVGで全区画に描く。絵文字は使わない。
  - **全48区画＋会場15か所の名前を常時表示**（実効 11.86px＠1280・12.0px＠390）。
    配置は決定論の総当たり（`Math.random` 不使用）で、**ラベルの重なりは DOM 実測で 0**。離れた場合は引き出し線。
  - **街の下地**（海岸線・丘・環状道路・放射道路・中央の広場）は**装飾**。凡例に明記。
  - **発光**：その月の `ignition_timeline` で、会場がパルス→残光（1.2秒）、発話なら `heard_by` の
    本拠へ波紋の線、内心は弱い光だけ（「頭の中（誰にも伝わっていない）」と併記）、
    当事者が同席していた場には**二重リング**。`prefers-reduced-motion` では動かさず最終状態だけ出す。
  - 並びを ①街の地図 ②火が点いた瞬間（原文）③4色の推移（折れ線）④読み方ガイド → 補助パネル →
    `<details>「詳しい数字」` に変えた（数表は畳んだ）。
  - 原文は1文字も直さず、脇に「原文の P43 は『川沿いの家』のこと」の**対応注記**を機械生成。

### 実測（検収）

- `python tests/test_present.py` **466 passed / 0 failed**（第2弾は339本。壊さずに足した）。
- `python tests/test_ledger.py` **619 / 0 failed**、`python tests/test_v5c.py` **73 / 0 failed**。
- 7本すべて再生成（run94・v5bA/B/C は `layout=null` で従来表示のまま）。
- スクリーンショット `docs/shots_present3/`（登記の地図・街の話の地図・390・発光中・残光・
  火が点いた瞬間・詳しい数字を開いた状態・ラベルの拡大・動きを止める設定）。

### 要判断（CTO へ）

1. **記事（`kind:"article"`）の扱い**：スペックの `ignition_timeline` は `speech` / `thought` の2種しか
   定義がない。記事は年表に混ぜず `ignition_timeline_excluded` に分けて出した（v5cA で黄1件）。
   混ぜるなら第3の種別を足す必要がある。
2. `party_present` は**本人が当事者の場合も「居合わせた」に数えている**（basis に明記）。
   除くなら定義を変える必要がある。

### 変わっていない制約

ルールベース排除／観測に無い事実をコードで補完しない／`mvp_v1`〜`field_v5c` の設定と成果物は不変／
プロンプトとHTMLに「AI」表記を出さない／`git push`（ソースリポ・Pages とも）は**本便では未実施**。
---
## 第11便（2026-08-28）— v5d：役場の窓口の廃止・街の呼び名・兆候の絞り込み

設計の正は `docs/world_design_v5d.md`（§4 の節約設定だけ CEO 決定 2026-08-28 22:26/22:27 で上書き）。
**足したものは無い**（気づかせる経路・観測者・機構・語彙・確率・閾値は1本も足していない）。
直したのは「世界が住民に渡している入力の非現実」と「呼び名」だけ。v1〜v5c の設定・成果物は不変。

### 何をした

- 新規：`configs/config_field_v5d.yaml`／`configs/events_v5d_seed85.yaml`（台本・凍結）／
  `tools/build_events_v5d.py`／`src/field_v5d.py`／`tests/test_v5d.py`（128件）／
  `docs/events_v5d_trace_diff.md`（兆候の before/after）／`docs/_v5d_token_counts.json`（前置きの実測）
- 追記：`src/field_v5`（呼び名を渡す任意引数・**既定は従来どおり**）／`src/simulation`（v5d 分岐）／
  `tools/run_metrics`（版ごとの土地の言い方・S4周期）／`src/viz`（MONEYLESS に v5d）
- **窓口の廃止**：v5d の S4 は 町内会／仲介の店先／記者の取材 の3か月周期。`registry_rows_v5` は
  一度も呼ばれず `registry_lookup` の観測は0。**市役所の待合(V06)は会場として残る**。
- **呼び名**：対応表は `configs/parcel_names_v5c.yaml` の1枚だけ。プロンプト・原文・観測行に
  P/R/HH/BZ/BR/MU/ME/V番号が1つも出ない（テストで固定）。`talk_to`／`direct_to`／行き先も呼び名で受け、
  内部IDへ戻してから台帳に書く（スキーマの構造は v5 と同一・値だけが呼び名）。
- **台本**：取得46件（順番・月・区画・名義・kind・note）は v5c と**完全一致**。兆候だけ 93→33 本に濾した
  （registry 33・construction 9・sign_change 13 は全削除、moving_out 7→4、survey 9→8、strangers 7→6、
  tenant_swap 8・broker_known 7 はそのまま）。
- **占領分類器（O1〜O4）は既定 off**（`kpi.classify_occupation: false`）。off のとき
  `occupation_labels.jsonl` を書かず、集計は `{"O_available": false}`＝**未計測**（false に化けない）。
  4段階の色（青緑黄赤）の判定には一切影響しない。
- **Batch は使わない**（`batch_classify: false` / `batch_agents: false`）。
- **キャッシュ**：主体の system prompt は26体で 845〜942・中央値 878 トークン（v5c は 844〜962・中央値 896）。
  呼び名を入れても前置きは**伸びず**、暗黙キャッシュの最低 1,024 に届かない → `prompt_order: legacy` のまま
  （水増しで届かせるのは禁止）。

### 実測

```
python tests/test_ledger.py       -> 619 passed, 0 failed
python tests/test_v5c.py          ->  73 passed, 0 failed
python tests/test_cost_saving.py  ->  51 passed, 0 failed
python tests/test_v5d.py          -> 128 passed, 0 failed（新設）
python run.py --config configs/config_field_v5d.yaml --provider mock --quiet -> 24か月完走・配線エラー0
実API 1か月スモーク（legacy）      $0.0582  calls 179  cached  6.3%  truncated 0 / parse_fail 1
実API 1か月スモーク（stable_first）$0.0658  calls 202  cached 14.1%  truncated 0 / parse_fail 0
```

**24か月×3本の見込み ＝ $4.2〜$4.7**（v5c 3本 $4.89 の実測に1か月スモークの比 0.866 を掛けると $4.24／
占領分類器 off ぶん −$0.24 だけを引く保守側だと $4.65。**本走は施主GO待ちで未実施**。）

### 残っている判断

1. `ROLE_TEXT_V5["municipality"]`（「窓口業務を含む通常業務」）と行政ペルソナ本文
   （`configs/personas/field_v4_1.yaml` の「窓口、資料作成、庁内調整」）に「窓口」の語が残る。
   **登記を見に行く機構ではない**（待合は残っている）ため v5c 共通のまま触っていない。
   消すなら v5d 専用の役割文が要る。
2. 対応表の記者の内部IDが `ME01/ME02`、名簿の実際は `MD01/MD02`。表は凍結なので
   `src/field_v5d.AGENT_ID_ALIAS` で読み替えている。表のキーを直すかは要判断（画面側も同じ表を読む）。
3. `sign_change` は「賃借かつ店舗」だけ残す規則だが、seed85 の賃借7件はすべて住宅の区画で**0本**になった。
4. `src/kpi.OCCUPATION_SYSTEM` のルール文に「（P01形式）」が残る（分類器のルール文は変更禁止のため据え置き。
   既定 off なので v5d では走らない）。

---

## 【CTO 検収 2026-08-28】第10便・第11便のマージと確認

- `main` に第10便（present 第3弾）と第11便（v5d）をマージ（`9d91820`）。HANDOFF の衝突は**両方を残して**解消。
- **呼び名の修正**：「大家」は貸主の語なので、本拠に店子がいる4世帯（HH01/02/03/05）だけ「◯◯の大家さん」、
  残り12世帯は「◯◯の持ち主さん」にした（`b3b6099`。Codex レビューと CTO 判断が一致）。
  `tests/test_v5d.py` の登記名義テストが「大家さん」を焼いていたので、**対応表を読んで突き合わせる形**に直した。
- **CTO が自分で流した実測**：`test_ledger.py` 619 / `test_v5c.py` 73 / `test_v5d.py` **128** / `test_cost_saving.py` 51 /
  `test_present.py` 466 ＝ いずれも 0 failed。台本は取得46件が v5c と**完全一致**（traces 以外の全キーで比較）・
  兆候 93→33・`audience: registry` は 0本。`present_data_v5cA.json` は schema 7・辺 34.0〜86.21（比 2.5355／
  描画面積比 6.4286）・`max_overlap` 0.0・会場15・48区画すべてに名前。
- **Pages 反映済み**：`50ca75d` を push → live 200・`schema:7`・新しい土地名（「浜町の旅館」）が live の JSON にある。
  https://yamachas0.github.io/quiet-acquisition-report/present.html
- **実装しなかった Codex 指摘（施主決定どおり）**：①法務局での個別登記照会を残す案 ②兆候を増やす候補
  （管理通知・管理状態の変化・境界確認の隣地連絡）。どちらも「新しい経路も痕跡も足さない」に反するので入れていない。

---

## 第12便（2026-08-29）— v5e Phase1：赤の再定義／自衛レベル／自衛が出たら台本停止／目玉第15月／36か月

設計の正は `docs/world_design_v5e.md`。実装はスペック `spec_v5e_phase1.md`（CTO発注）に忠実。
**v1〜v5d の設定・出力・テストは1バイトも変えていない。v5e は「足す」だけ。**
住民（主体）への入力（プロンプト・スキーマ・兆候・同席・巡数・S4周期・会場・ペルソナ・seed・
区画・呼び名）は v5d と完全に同一であることを mock ランの実測で固定した。

### 何をした

- 新規 `src/stage_v5e.py`：赤（自衛の具体的な行動）と自衛レベル S1〜S3 の**唯一の持ち場**。
  `rule_defense_level` / `rule_red_v5e` / `stage_v5e` / `defense_level_of`（全て純関数）。
  走行中の停止判定と事後集計が同じコードを使う（判定が二本に割れない）。
  青・緑・黄のルール関数は v5c の実装（`tools/run_metrics.py` の `_v5c_rule_*`）が正のまま。
- 新規 `STAGE_SYSTEM_V5E` / `STAGE_SCHEMA_V5E` / `classify_stage_v5e`（`src/kpi.py` に追加）。
  `deal`/`area`/`same_buyer` の段落は `STAGE_SYSTEM` から**一字一句そのまま**（テストで文字列一致を固定）。
  `admin` を落とし `defense` / `defense_level`(none/S1/S2/S3) を足した。v5c の定数は無改造。
- `src/simulation.py`：`field_v5e` の配線。世界の層は v5d を使う（`field_v5d` フラグが
  `("field_v5d","field_v5e")` になった＝**v5d のテスト128件が緑のままであることを確認済み**）。
  月末に `classify_stage_v5e`（job_key `mNN_stage`）を回して `stage_labels_v5e` へ追記し、
  赤が1行でも立ったら `defense_stop` を立てて**翌月以降 `apply_script_v5` を呼ばない**。
  止めた取得は `ledger._rec(..., "script_suspended", reason="defense_detected")` と
  `deals_v5.jsonl` の `kind:"script_stopped"` に残す。**主体には一切見せない**。
  終了時に `stage_labels_v5e.jsonl` / `defense_stop_v5e.json`（停止しなくても
  `{"stopped": false, "months": N}` を書く）と `summary["v5e"]` を出す。
  v5e では事後の `classify_stage_v5c` を呼ばない（同じ入力に二重課金しない）。
- 新規 `tools/build_events_v5e.py`（乱数なし）→ `configs/events_v5e_seed85.yaml`。
  ACQ18/P04 を第12月→第15月へ移しただけ。id は振り直さない。(month, 元の並び順) で安定ソート。
- 新規 `configs/config_field_v5e.yaml`：v5d の写しで差は4点だけ（run_name / scenario_version /
  steps 24→36 / events_file）。占領分類器 off・Batch off・prompt_order legacy のまま。
- `tools/run_metrics.py`：`stage_labels_v5e.jsonl` があれば v5e として集計。赤だけ
  `rule_red_v5e ∧ llm_defense`。追加出力 `S_counts` / `S_first` / `S_level_agreement` /
  `S_rows` / `defense_stop` / `red_definition_version`。**v5c/v5d の集計は不変**。
- 新規 `tools/reclassify_v5e.py`：既存 run を新しい赤で読み直す（`--dry-run` は API を叩かない）。
- 新規 `tests/test_v5e.py`（110件）／`src/viz.py` の `MONEYLESS_VERSIONS` に v5e を追加。

### 実測

```
python tests/test_v5e.py        -> 110 passed, 0 failed（新設）
python tests/test_v5d.py        -> 128 passed, 0 failed
python tests/test_v5c.py        ->  73 passed, 0 failed
python tests/test_ledger.py     -> 619 passed, 0 failed
python tests/test_cost_saving.py->  51 passed, 0 failed
python tests/test_present.py    -> 466 passed, 0 failed
python run.py --config configs/config_field_v5e.yaml --provider mock --quiet
  -> 36か月完走 simulations/2026-08-29_0012_100_field_v5e_a_city
     取得 45件成立（未発火は ACQ46 の1件だけ）／stage_labels_v5e 9,536行／停止なし
python tools/run_metrics.py --run <その run_dir>
  -> version field_v5e / red_definition_version v5e / C_rows_total 9536 / C_unknown 0
     S_counts {"S1":0,"S2":0,"S3":0}（mock の分類器は defense を返さないので当然）
python tools/reclassify_v5e.py simulations/2026-08-28_1016_100_field_v5c_runA --dry-run
  -> 7,284行（utterance 2,958 / thought 4,295 / article 31）／292チャンク／概算 $0.2092
v5c runA/B/C・v5d の metrics_v5.json を再集計 -> 4本ともバイト一致（v5c の集計は不変）
```

停止の配線は mock（第2月の発話に「売らない」を混ぜ、その行だけ `defense=true, defense_level="S1"`）で
実測：第3月以降の取得0件・`defense_stop_v5e.json` に trigger_month 2 / stop_from_month 3 と原文全文・
`deals_v5.jsonl` に `script_stopped`・停止後のプロンプトに停止／自衛／分類の語が0件。
赤が出ない mock では取得が台本どおり全件成立。

### 残った判断（CTO へ）

1. **`docs/world_design_v5e.md` が 00:08 に更新され、発注スペックと食い違っている。**
   ①ルール側の赤が `rule_red_v5e(text, holders, acquired) = S語 ∧ _v5c_rule_blue` になった
   （スペックは `rule_red_v5e(text)` で青の要求なし）②S1 語彙に「売却しない／譲渡しない／売却は見送」
   の3語が増えた ③S3 に「取引を凍結／取引の凍結」を足すと本文にある ④凍結時刻を
   `src/stage_v5e.py` 冒頭に書く指示。**現状はスペックどおり（青の要求なし・S1 15語・S3 16語）で
   実装してある。** 反映するかは CTO 判断（反映は語彙の追加と関数シグネチャの変更＝走行前に確定が必要）。
2. **停止した取得の兆候は配られ続ける。** `apply_script_v5` を呼ばなくても
   `ambient_traces_v5` は台本を直接読むので、成立しなかった取得の兆候（survey・strangers 等）が
   その月に配られる。スペックは「兆候の配布は最終月まで続く」としか書いておらず、
   「止めた取得の兆候だけ止める」指示は無いので触っていない。要判断。
3. `summary["v5e"]["levels_seen"]` は**ランの赤の行 全件**から採ったレベルの集合にした
   （トリガー行だけに限る案もある＝スペック未定義）。
4. `STAGE_SYSTEM_V5E` の冒頭は v5c の「4つの点だけを判定する／4つとも false にする」を
   そのまま残した（deal・area・same_buyer・defense の4つ＋ defense_level は副分類、という読み）。
5. `configs/config_field_v5e.yaml` の `scenario.acquirer_mandate` 内のコメントは
   `configs/events_v5d_seed85.yaml` を指したまま（「他は1バイトも変えない」に従った）。

### 変わっていない制約

ルールベース排除／観測に無い事実をコードで補完しない／`mvp_v1`〜`field_v5d` の設定と成果物は不変／
プロンプトとHTMLに「AI」表記を出さない／`.env` の内容を文書に書かない／
**実APIは一度も叩いていない（本便は mock のみ）**／ソースリポの `git push` はしない。

---

## 【CTO 検収 2026-08-29】v5e 第1便（36か月×3本）

正本：設計 `docs/world_design_v5e.md`／結果 `RESULTS.md` の v5e 節／人手監査 `docs/audit_v5e.md`。

### 結論

- **有効なのは runA だけ。** 第6月に住民が「D社から接触があったら、きっぱり断ろう」と
  発話し、買い手が取得を止めた（10件成立／35件停止・最終シェア15.2%）。
  締め直した分類器で読み直しても**停止月は第6月のまま**＝この結果は定義の締め直しに耐えた。
- **runB（第11月）・runC（第26月）の停止は無効**（偽陽性）。読み直すと B は第35月、
  C は**一度も止まらない**。
- **S3（行政の禁止措置）は3本とも0。** 街は自力で気づいて自衛したが、行政の措置には届いていない。
- 目玉（P04・第15月）は A・B では停止が先に来て**発火せず**、C では発火した。

### 走行後に分かった重要な事実（次の担当者は必ず読む）

1. **mock は v5e のスキーマを守らない。** mock 分類器は別の形（frame/about_acquisition）を返すので、
   v5e の mock ランは**全行 unknown が正しい表示**である。以前の `C_unknown 0` は
   「id があれば分類成功」と見なしていた旧実装による**偽データ**だった（`a81e052` で修正）。
   配線の確認は `tests/test_v5e.py` の専用スタブ（DefenseMock 等）で行うこと。
2. **S2/S3 は第1便のあとに締め直した**（`99564b3`）。**定義は変えていない**が、実装が定義に
   届いていなかった（S2 が依頼・願望・対象未定を拾い、S3 が行政以外を拾っていた）。
   **締め直しは結果を見たあとに行った**ので、締め後の S 集計は**事後の再読み**である。
   3本が実際に停止したのは締める前の分類器の出力による。両方を RESULTS に並記済み。
3. **S1 は触っていない。** 「手放すつもりはない」形を拾えない**既知の偽陰性**があるが、
   結果を見たあとに S1 を足すことはしないと決めた（テストで固定）。
4. **正本と再判定を混ぜない。** 走行中の分類＝`stage_labels_v5e.jsonl`（これが買い手を止めた）。
   締め直した読み直し＝`rejudge_v5e/summary.json`。`tools/reclassify_v5e.py` は正本を
   書き換えない（v5e は `--rejudge` が要る・出力は別フォルダ）。

### 次にやること（施主待ち）

- **停止条件の確定待ち**（施主が朝に決める）。決まるまで**再走しない**（1本 $2・3本 $6 の空費を避ける）。
- v5c runB / runC の再分類は未実施（runA のみ実施）。
- 私信（direct）は v5c から分類対象に入っていない＝停止にも S 集計にも現れない。入れるかは要判断。

### 実測

```
tests: v5e 131/0・v5d 128/0・v5c 73/0・ledger 619/0・cost_saving 51/0・present 704/0
本走3本 $5.9575 / errors 0 / truncated 0 / unknown 0
v5c runA 再分類 $0.2343（旧赤22行→新赤6行）
締め直し後の再判定 $0.9245（正本は不変・rejudge_v5e/ へ）
実API 1か月スモーク $0.0603
確定合計 $7.177（＋中断による無駄 推定 $1.9）
Pages: https://yamachas0.github.io/quiet-acquisition-report/present.html live 200
       present_data_v5eA/B/C.json すべて 200・schema 8・live と手元が md5 一致
ソースリポは未push（commit のみ）
```

---

## 第13便（2026-08-29 朝）— v5e2：停止条件を S2/S3 のみに／runA 実走（B/C は施主GO待ち）

設計の正は `docs/world_design_v5e.md` §3・§10（第2便）。施主決定 07:50「個人的な売買の拒否は
検出しちゃダメ」。

### 変えたこと（コードは3点だけ）

- `Simulation.V5E_STOP_LEVELS = ("S2","S3")`：**S1 では絶対に止めない**（観測・集計には残す）。
- `Simulation._v5e_stop_trigger()`：停止判定の唯一の持ち場（観測できる行 ∧ S2/S3）。
  トリガー行に `llm_level` / `rule_level` を両方残す（レベルの主値は LLM なので
  rule=S1・llm=S2 でも止まる＝走行前に明記）。
- `V5E_OBSERVABLE_KINDS = ("utterance","article")`（私信を外した＝将来の暗黙拡大を防ぐ・Codex指摘）。
  走行中の記事の行に**書き手の役割を補う**（事後集計と同じ判定にする。第1便は空＝記事は
  構造上 S3 になり得なかった）。
- `configs/config_field_v5e2.yaml`（v5e との差は `run_name` だけ）。世界・台本・月数は不変。

### 実測

```
tests: v5e 155 / v5d 128 / v5c 73 / ledger 619 / cost_saving 51 / present 704 …全て 0 failed
mock 36か月完走 simulations/2026-08-29_0809_100_field_v5e2_a_city（停止なし・取得45件）
実API runA: simulations/2026-08-29_0810_100_field_v5e2_runA
  $2.1581 / calls 6285 / errors 0 / truncated 0 / 41.1 分 / unknown 1
  **停止しなかった**（36か月）・取得45件全成立・最終シェア 71.74%（control 97.8%）
  目玉 P04 は第15月に発火
  色: blue 4923 / green 504 / yellow 212 / red 35（C_red_first_public = null）
  S_counts S1 9 / S2 26 / S3 0 …**35件すべて内心（thought）**。観測できる行の赤は0
  黄の初出 m2（内心）・m4（公言）／赤の初出 m8（内心）
```

### 走行後に分かったこと（次の担当者は必ず読む）

1. **赤（自衛）が一度も公の場に出なかった。** 街は第4月には「同じ買い手」を公言していたのに、
   「売らない」「回覧で知らせる」は最後まで内心だけ。停止条件（観測できる S2/S3）は
   仕様どおり働き、買い手は36か月買い切った。
2. **LLM とルールの役割分担は効いている。** LLM が defense=true とした発話は125行あるが、
   ルール側（青∧自衛語）が落として赤は0。逆にルールが S2 語を拾った発話20行は LLM が
   false にした（「説明会を開かれるそうですが」＝他人の予定の伝聞・質問）。どちらも人が読んで
   妥当だった（抜き取り確認）。
3. **境界事例**：第7月の記事（MD02）が「湾岸大通りの大家さんは近々説明会を開催する予定です」と
   書いている。ルールは S2、LLM は false（書き手自身の行動ではない）。**人によっては
   「周知が起きた」と読む**。この扱いは走行前の定義どおりで、結果を見てから動かしていない。
4. **S2 の内心26件は「皆に伝えたい」型の願望が混じっている疑いがある**（締め直した定義では
   本来除外側）。停止には影響しない（内心は止めない）が、S 集計の精度としては要監査。

### 次にやること

- **runB / runC は施主GO待ち**（施主指示 07:52＝runA 完了で一旦止めて中間報告）。
- GO が出たら B/C を並走（各 $2.2 見込み・3本合計上限 $7 に対し runA 実測 $2.16）。
- 事後：4色＋S1〜S3 の人手監査（赤35行は全件・停止トリガーは0件）／`RESULTS.md` に v5e2 節／
  present.html に v5e2 を追加 → Pages push → live 200。

---

## 第14便（2026-08-29 昼）— v5e2 第2便＋第3便（6本）完了

正本：設計 `docs/world_design_v5e.md` §10／結果 `RESULTS.md` の「v5e 第2便」「v5e 第3便」節／
人手監査 `docs/audit_v5e2.md`（A/B/C 334行）・`docs/audit_v5e2_bin3.md`（D/E/F 257行）。

### 結論（6本）

- **止まったのは6本中1本だけ（runF・第28月）で、それも人手監査で偽陽性。**
  トリガーは記者の「誤解を解くため…今月中に記事を出すつもりです」＝**告知の形は満たすが
  対象が特定の買い手でない**。第1便 runB の「正確な情報発信をしていきたい」と同型の穴
  （締め直しで塞いだのは願望の側だけ・**対象要件の穴は残っている**）。
- **6本すべてで S3（行政の措置）は0件。定義に合致した赤も0件**（監査591行）。
  街は黄（同じ買い手だと結びつける）までは公の場で到達するが、**行動には移らない**。
- 止まらなかった5本は取得45件全成立・最終シェア 71.74%（支配率 97.8%）。F は 35件・54.35%。
- **目玉物件（P04・第15月）は6本とも発火**（第1便では A・B が停止で発火せず）。
  目立つ1件のあとの街を初めて36か月分観測できたが、それでも自衛は出なかった。

### 実測

```
tests: v5e 155 / v5d 128 / v5c 73 / ledger 619 / cost_saving 51 / present 704（全て 0 failed）
mock 36か月 完走（2026-08-29_0809_100_field_v5e2_a_city・停止なし・取得45件）
実API 6本 = $12.4303（第2便 $6.2376 / 第3便 $6.1927・便ごとの上限 $7 内）
  A $2.1581 / B $2.0242 / C $2.0553 / D $2.0422 / E $1.9875 / F $2.1630
  errors 合計1（B・打切り0・unknown 合計1）
Pages: present.html に v5e2 6本を追加 → push（ac5cc8b）→ live 200
  present_data_v5e2A〜F.json すべて 200・runF の live と手元が md5 一致
ソースリポは未push（commit のみ）
```

### 実装で変えたこと（第2便の頭で入れた3点）

- `Simulation.V5E_STOP_LEVELS = ("S2","S3")`（S1 では止めない・観測には残す）
- `Simulation._v5e_stop_trigger()`（停止判定の唯一の持ち場・`llm_level`/`rule_level` を両方記録）
- `V5E_OBSERVABLE_KINDS = ("utterance","article")`（私信を外した）＋走行中の記事の行に書き手の役割を補完

### 次の担当者への注意

1. **分類器の穴は「結果を見てから」直さない。** 直すなら次の便の走行前に凍結し直す。
   候補＝①S2 に対象要件（同じ買い手／複数の土地に触れていること）を足す ②S2 を完了・確定形に絞る
   ③S1 の語彙（「売却の意思はない」等）。**いずれも施主判断待ち。**
2. **赤の数字は額面どおりに読めない**（6本の赤78行のうち合致0）。RESULTS を読む時は必ず監査と併読。
3. `docs/venue_emergence_v5e2.md`（10:55・647行）は**この便の作業では作っていない**＝
   別セッションが同じリポに書いた可能性が高い。CTO の commit `92e4e4e` に巻き込まれて入っている。

---

## 第15便（2026-08-29 昼〜午後）— v6：2つの世界（経路なし6本 vs 経路あり3本）・採点用LLM全廃

設計の正 `docs/world_design_v6_two_worlds.md`／結果 `RESULTS.md`「締め」節／
人手確認 `docs/audit_v6.md`／数字 `docs/two_worlds.json`（`tools/two_worlds.py`）。

### 結論（一文）

**町の人に中立な行動の選択肢を3段置くだけで、買い手の線は寝た（7割→2割）。
ただし寝た理由は「同じ買い手だと気づいたから」ではない**（行動は3本とも第1月に出ており、
「同じ買い手」に届いたのは B 第3月・C 第12月・A は最後まで無し）。

### 何を足したか（これで全部）

1. 登記簿に `refusal` 列（`Parcel.refusal`・既定 False）＋ `sell_intent ∈ {変えない, 当面売らない, 売らないを解除}`
2. `public_act ∈ {なし, 回覧板に載せる, 町内会の議題にする, 役場へ申し入れる}`＋自由文1行／
   行政だけ `measure ∈ {なし, 相談窓口を開く, 説明会を開く, 条例・届出の検討を始める}`
3. 回覧・議題・申入れ・措置は**翌月の紙**として街の全員へ（配送は記事と同じ型・私信ではない）
4. 買い手の台本は機械的に従う＝予定の土地が `refusal` なら**その月は買えず次の予定へ**
   （買い直さない・買えなかった取得の兆候はその月以降配らない）。**売買だけを止める（賃借は止めない）**
5. 行動欄はその主体のその月の**最後のターン1回だけ**（stance と同じ場所）

**採点用の LLM は全廃**（`classify_on = (not field_v6) and kpi.classify_utterances`＝
config を true に戻してもコード側で遮断）。気づいた月は決定論＋人手確認。

### 実測

```
tests: v6 89 / v5e 155 / v5d 128 / v5c 73 / ledger 619 / cost_saving 51 / present 704（全て 0 failed）
mock 36か月 完走（全員が第10月から「当面売らない」を選ぶスタブ→45件中23件が買えず・share 21.74%）
実API 1か月スモーク $0.0526（第1月に住民1名が「当面売らない」・町内会の議題2件・相談窓口1件）
実API 3本 = $4.7148（A $1.5324 / B $1.6259 / C $1.5565）・errors 0・打切り 0・各22分
  A 最終25区画 28.26% / 買えなかった20件 / 気づき なし
  B 最終19区画 15.22% / 買えなかった26件 / 気づき 第3月
  C 最終23区画 23.91% / 買えなかった22件 / 気づき 第12月
  （経路なし6本＝45区画×5・35×1／71.74%×5・54.35%×1／買えなかった0／行動0）
本便合計 $4.7674（上限 3本$7・全体$8 の内）
Pages: present.html 主画面を「クイズ→2世界の図→原文→街の地図」に絞り旧UIは「詳しい数字」へ退避
  push 7e59320 → live 200・`present_data_two_worlds.json` live 200（中身は手元と一致）
ソースリポは未push（commit のみ）
```

### 次の担当者への注意

1. **「気づいた月」は機械の値をそのまま使わない。** 緩い網（`_v5c_rule_yellow`）は
   「2つの名前が同じ文に出た」だけで拾う＝読むと別々の相手の話が大量に混ざる。
   採用値は `tools/two_worlds.py` の `HUMAN_NOTICED`（人手判定・根拠は `docs/audit_v6.md`）で、
   `two_worlds.json` の `noticed_month` はこの人手判定である（機械値は `noticed_loose_month`）。
   **本を足したら人手確認とこの表の更新が要る。**
2. **行動は「気づき」より先に出る**（第1月）。「気づきが行動になった」と書かない。
3. 経路ありの世界は**気づきが遅く・弱くなる**（買えない→痕跡が減る）。止めることと見抜くことは別。
4. 「当面売らない」は**その人の全区画**にかかる。宣言人数と止まった区画数を混同しない。
5. 第36月の紙は誰も読まない（第37月が無い）＝`papers_read` を別に持っている。
6. 行動できるのは同席できた主体だけ（実測 98.5% の主体×月・残り1.5% は行動の機会が無い）。
7. Codex 走行前レビューで**受け入れなかった3点**（同席ゼロ主体への追加コール／行動欄を任意化／
   `Parcel.refusal` の隔離）と、その理由は `docs/world_design_v6_two_worlds.md` §7-2 にある。

---

## 第16便（2026-08-29 午後）— v6b：「毎月の問いかけ」を外した世界（任意回答・2本）

設計の正 `docs/world_design_v6_two_worlds.md` §9／結果 `RESULTS.md` の
「追加 —『毎月の問いかけ』を外した世界」節／人手確認 `docs/audit_v6.md`。

### 結論（一文）

**毎月の問いかけをやめても、買い手の線は同じだけ寝た（寝方の106%を再現）＝
効いていたのは「毎月思い出させること」ではなく、選択肢そのものである。**

### 変えたこと（2点だけ）

1. 毎月の行動欄の説明を出さない（制度の存在は system プロンプトに1回だけ・`WORLD_KNOWLEDGE_V6B`）
2. 行動の欄をスキーマの必須から外す（`required_actions=False`・書かなければ既定）

あわせて v6b に限り、行政ロールの「特別な権限や制度はこの街には無い」を外す
（制度の説明と矛盾するため。v5d の原文は変えず `system_prompt_v6b()` で組み立て側だけ）。

### 実測

```
tests: v6 106 / v5e 155 / v5d 128 / v5c 73（全て 0 failed）・mock 36か月完走
実API 2本 = $3.1294（A $1.5935 / B $1.5359）・errors 0・打切り 0・各22分（上限 $4 の内）
  A 最終22区画 21.74% / 買えなかった23件 / 気づき なし / 売らない59件 / 紙14枚
  B 最終20区画 17.39% / 買えなかった25件 / 気づき 第8月 / 売らない39件 / 紙10枚
回答された欄の数（36か月・全主体）：土地の方針 582/570/544（v6）→ 69/46（v6b）
                                   街への働きかけ 835/848/833 → 839/834
Pages: 3つ目の図（経路あり・任意回答）を追加 → push cd5ac56 → live 200（JSON も 200・path_opt 2本）
本日の実API 合計 = $4.7674（v6）+ $3.1294（v6b）= $7.8968
```

### 次の担当者への注意

1. **分離は完全ではない。** やめたのはプロンプトの問いかけで、欄はスキーマに残っている
   （必須ではない）。街への働きかけの欄はほぼ毎月埋められていた（ほとんど「なし」）。
2. `HUMAN_NOTICED` に path_opt A=None / B=8 を追加済み。本を足したら人手確認と表の更新が要る。
3. 台本 `docs/presentation_script.md` の引用と画面の引用は**同じ行**にそろえた
   （runC 第1月 HH16 scene=plan round=0）。`tools/two_worlds.py` が英字混じりの内心を避けて
   月初の内心を採る（画面には出典も出る）。
4. **2026-08-29 14:08 に C ドライブが満杯になり、`present.html` を書き込み中に 0 バイトで壊した**
   （git から復元して再適用済み）。`AppData/Local/Temp/l4r1hcy1`（2.95GB）を `D:/tmp_offload/` へ
   **移動**して 2.9GB を確保した（削除していない＝戻せる）。走行前に `df -h /c` を見ること。

## 第19便（2026-08-29 夜）— v8「最小の町」：30体全員が不動産を持つ町・X社はLLM・36か月×1本

※ 第17便・第18便は Codex CLI が read-only 指定に反して自動追記したもので、内容が施主確定と
食い違っていたため採用しなかった（`docs/minimal_v8_codex.md` §8 に経緯）。番号は 19 から続ける。

### 何を作ったか

施主の「もっとシンプルにしたい」（20:43）から始まり、同夜の連続した決定で固まった **v8「最小の町」**。
**既存 v1〜v6b は1バイトも触っていない**（`src/simulation.py` にも分岐を足していない＝v8 は独立）。

- 設計の正 `docs/world_design_v8_minimal.md`（走行前に凍結・走行後に数値を書き換えていない）
- 名簿 `configs/personas_v8.yaml`（30体・44区画。売れるのは28体・41区画＝行政2体は売却不可）
- 設定 `configs/config_field_v8.yaml`（会話あり）／`config_field_v8_nochat.yaml`（会話なし・**未走**）
  → 2つの差は **`chat` の1行と `run_name` だけ**（tests で固定）
- 世界と文面 `src/field_v8.py`／月ループ `src/sim_v8.py`／実行 `run_v8.py`
- 集計 `tools/v8_curves.py`（決定論・API不使用）／試験 `tests/test_v8.py`（43件）
- 結果 `docs/v8_run.md`

### 世界（凍結事項）

毎月＝①X社（**LLM**）が未売却の持ち主ごとに「提示する／しない＋条件文」→②各住民の思考と行き先
→③その場に2人以上いれば1巡だけ話す→④未売却の持ち主**全員**が「売る／売らない」→⑤登記の更新。
「売る」は不可逆・「売らない」は今月だけ（吸収状態なし）・賃貸なし・売った人は町に残る。
場は**公共の場5か所**（公共施設／交通拠点／商業施設／公園／医療施設）＋「今月はどこにも行かない」で
**全員がどこへでも行ける**。ひと言の配送は**2経路**＝居合わせ＋**隣近所**（不動産が隣り合う主には
行き先に関わらず届く／場所は渡さない）。報道・私信・兆候・紙・気づき抽出・採点LLM は全部無い。

### 結果（会話あり1本・seed 85・36か月）

```
run_dir simulations/2026-08-29_2237_100_v8_minimal_chat_A
X社の取得 12区画 / 8人（売れる41区画・28人のうち）・未売却 71.4%
提示 35件（応じた0／断られ35）・相手は2人だけ・第18月に開始し以後毎月ほぼ同文
売った8人は全員、X社から一度も声を掛けられていない
費用 手元 $0.4194 / 請求見込み $0.8388 / 7.0分 / 2,476コール
健全性 parse fail 0・no_answer 0・打切り 0・行き先の無効 0
キャッシュ比率 45.7%（暗黙）
本日の v8 の実API合計 = $0.0166(1か月) + $0.0508(3か月) + $0.4194(本走) ≒ $0.49
```

### 次の担当者への注意（大事な順）

1. **この結果を「X社の攻めに町が応じた」と読むな。** X社はほとんど攻めていない。
   売却を起こしたのは **毎月全員に聞く2択そのもの**の可能性が高い（問いの文面にX社の名が出るため、
   声を掛けられていない人も毎月X社を思い出す）。v6b の「選択肢そのものの効果」と同じ筋。
   → 比較したいなら「**提示が来た人にだけ2択を聞く**」版が要る（**要施主判断**）。
2. **内心と選択が食い違う売却が少なくとも2件ある**（第35月の学園西・岬道）。曲線の後半はこれを含む。
3. **会話なし対照は実装だけで走らせていない**（施主決定）。走らせるなら
   `python run_v8.py --config configs/config_field_v8_nochat.yaml --max-cost 1.05`。見込み $0.2〜0.3。
4. **明示キャッシュは使えない**。API の最低は 2,048 トークン（実測エラー文）で、
   共通前置きは 1,385。**埋め草で届かせるのは施主が禁じている**。暗黙キャッシュ（最低1,024）は効いている。
5. Codex の走行前レビューを2巡かけ、2巡目の指摘5件（背景文の削り漏れ／2択の文言／
   「売っても住み続ける」の片側強調／隣近所に場所を渡していた／隣接が名前順で偏る）を反映してから走った。
   **1巡目で「直した」と言った項目が2つ直っていなかった**ので、レビューは必ず2巡やること。
6. 走行中の費用の歯止め `max_cost_usd`（既定1.10）を入れた。超えたらその月で止まり
   `stopped_by_cost: true` が summary に残る。
7. 画面（present.html）は**触っていない**。主体別タイムライン `timeline_v8/<id>.json` は出力済みなので、
   画面はそこから作れる（別便）。

## 第20便（2026-08-30 未明）— v8b「売りに出す町」：X社を積極化し、住民の問いから X社 の名前を外した

### 何を作ったか

施主 2026-08-29 23:06 の3点＋23:08 の追記による **v8b**。**v1〜v8 は1バイトも触っていない**
（`git status` で確認済み。v8b は新規ファイルだけ＋`SimulationV8` の継承で完結）。

- 設計の正 `docs/world_design_v8b.md`（土台＝`docs/world_design_v8_minimal.md`・走行前に凍結）
- 名簿 `configs/personas_v8.yaml`（**v8 と同じものを読む・変更なし**）
- 設定 `configs/config_field_v8b.yaml`（v8 との差は run_name / scenario_version / max_cost_usd の3つだけ）
- 世界と文面 `src/field_v8b.py`／月ループ `src/sim_v8b.py`／実行 `run_v8b.py`
- 集計 `tools/v8b_curves.py`（決定論・API不使用）／試験 `tests/test_v8b.py`（32件）
- 結果 `docs/v8b_run.md`

### v8 からの差分（4つだけ・凍結事項）

1. **X社の命題を積極化**：「合法な手段で、できるだけ多くの不動産の名義を得よ。
   反発を招いて止まるほど目立つな。毎月動け。」（住民側には1文字も出ない＝tests で固定）
2. **住民の毎月の問いは「今月、自分の不動産を売りに出すか（出す／出さない）」だけ**
   ＝**X社の名前が出ない**。出品は公の記録に載るが**それだけでは名義は動かない**。当月かぎり。
3. **X社が条件を持ちかけた月だけ**、その人に「応じる／応じない」を追加で聞く（同じ月末の1コール・
   条件が届いた月だけスキーマに `respond` が生える）。**名義が動くのはここで応じた時だけ。**
4. **X社は前月末の出品一覧（公の記録）も見る**。履歴は相手ごとに畳む（`fold_history_v8b`・費用のため）。

### 結果（会話あり1本・seed 85・36か月・完走）

```
run_dir simulations/2026-08-29_2358_102_v8b_listing_chat_A
X社の取得 7区画 / 6人（売れる41区画・28人のうち）・未売却 78.6%   ← v8 は 12区画/8人・71.4%
提示 247件・相手は28人全員（応じた6／断られ241）              ← v8 は 35件・2人・成約0
X社が動かなかった月は第1・2・19月だけ（残り33か月は毎月動いた）
出品 のべ128件（12人）／売れない家 120件（出品→翌月に提示が来たのは74/128）
提示の内訳＝出品者へ74件／出品していない人へ173件
発話531件中229件（43%）がX社に言及・初出は第3月                ← v8 は第15月
費用 手元 $0.5207 / 請求見込み $1.0414 / 8.2分 / 2,664コール
健全性 parse fail 0・no_answer 0・打切り 0・X社応答の取りこぼし 0
キャッシュ比率 41.0%（暗黙・共通前置き1,549トークン）
本日の v8b の実API合計 = $0.0184(1か月スモーク) + $0.5207(本走) = $0.5391
```

### 次の担当者への注意（大事な順）

1. **v8 との差は2要因を同時に変えた結果**（命題の積極化＋問いの形）。**どちらが効いたかは分離できない**。
2. **「応じる」を交渉の入口だと読んだ成約が6件中4件ある**（第3月一番街・第10月北団地・
   第18月潮見横丁・第31月湯坂下）。世界は「応じる＝その月末に名義が移り戻らない」と共通前置きに
   書いているのに、本人は「まず応じて後で条件を詰める」と書いている。**取得曲線はこの誤解を含む。**
   直すなら世界の文面ではなく**選択肢の名前**（「応じる」→「名義を移す」等）を疑うべきだが、
   それは世界の言い換えなので**要施主判断**。
3. **出品は「売る意思」ではなく「探り」として使われた。** のべ128件の6割を3人が占め、
   その3人は誰も名義を移していない（内心は「まだ手放すつもりはない／市場の動向を見るため」）。
4. **X社は条件らしい条件を作れなかった**（ほぼ全件が「名義を譲り受けたい。ご検討ください。」の同文）。
   この世界に金銭が無いので、価格・立退料といった手が最初から使えない。v8 1本目のほうが
   （提示は少ないが）「隣り合う2件の管理権を交換する」という独自の条件を考えていた。
5. **Codex 走行前レビューは2巡やった。** 2巡目で直した6点は `docs/world_design_v8b.md` §5-1 に列挙
   （先月申し出行のアンカリング除去／不可逆性の片側強調の解消／同月出品＋応諾の集計/翌月板からの除外／
   X社応答の取りこぼしを数える／上限0.56／**月の途中でも費用で止まる `CostLimitReached`**）。
   1巡目の唯一の走行NGは「費用上限が月末判定のみ」だった。
6. **費用の歯止め**：`max_cost_usd: 0.56`。月末だけでなく場面と場面のあいだでも見る。
   最悪の超過は場面ひとつぶん（30コール×2,200トークン ≒ $0.034）で **0.56+0.034 < 0.6** を算術保証。
   今回は作動していない（`stopped_by_cost: false`・36か月完走）。
7. **未 push**（施主指示）。会話なし対照（v8b）は**実装していない**（v8b は会話ありのみ）。
8. 画面（present.html）は触っていない。主体別タイムラインは `timeline_v8b/<id>.json` に出ている。

## 第21便（2026-08-30 未明）— v8c「売る／売らないと、その理由」：取得0件・断りの一言187件

### 何を作ったか

施主 2026-08-30 00:24／00:31／00:56 の指示による **v8c**。**v1〜v8b は1バイトも触っていない**
（`git status` で確認済み。v8c は新規ファイルだけ＋`SimulationV8B` の継承で完結）。

- 設計の正 `docs/world_design_v8c.md`（土台＝`docs/world_design_v8b.md`・走行前に凍結）
- 名簿 `configs/personas_v8.yaml`（**v8 と同じものを読む・変更なし**）
- 設定 `configs/config_field_v8c.yaml`／会話なし対照 `configs/config_field_v8c_nochat.yaml`（未走行）
- 世界と文面 `src/field_v8c.py`／月ループ `src/sim_v8c.py`／実行 `run_v8c.py`（`--workers` を追加）
- 集計 `tools/v8c_curves.py`・`tools/v8c_emergence.py`／図 `tools/v8c_fig.py`／試験 `tests/test_v8c.py`（33件）
- 結果 `docs/v8c_run.md`（**単独で読める**・経緯を書かない）／差分 `docs/v8c_diff_from_v8b.md`
- 図 `docs/submission/fig_v8c_curve.png`（上＝取得曲線（0の線）・下＝月別の提示件数と出品人数）

### v8b からの差分（4つだけ・凍結事項）

1. 応諾の語「応じる／応じない」→ **「売る／売らない」**。不可逆は**両側の帰結を1行**で
   （「売ると今月末に名義がX社へ移り、その後は戻らない。売らないと今月末の名義はあなたのままである。」）。
2. **全判断に理由の一言**（行き先／出す・出さない／売る・売らない／X社の提示判断・空欄可40字）。
   **配送されるのは「売らない」の理由だけ**＝条件を出したX社にだけ翌月届く（`offers.json.decline_reason`）。
3. X社の命題から「目立たず」を削除し語を「取得せよ」に＝
   **「あなたはX社である。合法な手段で、できるだけ多くの不動産を取得せよ。毎月動け。」**
4. X社の提示に**自己紹介の1行**を世界が決定論で添える＝「私どもは海外の不動産投資会社です。」
   （共通前置きには書かない＝町の人は提示を受けて初めて知る。`offers.json` は text=raw / delivered=届いた形）。

### 結果（会話あり1本・seed 85・36か月・完走）

```
run_dir simulations/2026-08-30_0111_104_v8c_reasons_chat_A
取得 0区画 / 0人（未売却 100%）              ← v8b は 7区画/6人・78.6%
提示 187件・相手24人（全件「売らなかった」）    ← v8b は 247件・28人・成約6
出品 のべ33件（3人）／売れない家 33件          ← v8b は 128件(12人)/120件
断りの一言 187件（記入率100%）がX社へ配送
理由の記入率 行き先96.7% / 出品50.6% / 売買100% / X社98.6%
発話 203件中56件がX社に言及・初出 第1月        ← v8b は 531件中229件・第3月
X社の条件文 98通り（v8b は43通り）・断りの直後の書き換え 78回（変更78回の全部）
「今月はどこにも行かない」が 779/1080（72%）＝町が静かになった
費用 手元 $0.4374 / 請求見込み $0.8748 / 11.7分 / 2,431コール / 停止なし
健全性 parse fail 0・no_answer 0・打切り 0・行き先無効 0・X社応答の取りこぼし 0
キャッシュ比率 48.8%（暗黙・共通前置き1,652トークン）
```

### 次の担当者への注意（大事な順）

1. **取得0の主因はほぼ確実に「交換」である。** 提示187件中155件が交換の申し出で、
   X社は**1件も不動産を持っていない**のに他人の物件を対価に差し出した。世界は提示文の中身を
   検証しないので止まらない。断りの一言187件のうち66件が「不明／詳細／具体」＝
   **断りの3分の1は『売りたくない』ではなく『条件が読めない』**。
   直すなら「渡せるものを持たせる」か「交換を世界の側で不可能にする」＝**要施主判断**（骨格に触る）。
2. **町が静かになった**（発話531→203・外出しない72%）。行き先にも理由欄を付けたことが
   効いている可能性がある（未検証）。分離するなら**行き先の理由欄だけ戻した1本**（≈$0.44）。
3. **v8b の「応じる＝交渉の入口」の誤読は今回出ていない**が、売った人が0人なので
   「語の変更で消えた」と断定はできない。
4. **会話なし対照は未走行**（費用上限のため見送り。`configs/config_field_v8c_nochat.yaml`・
   見込み $0.35 前後）。走らせれば `tools/v8c_curves.py --nochat` と
   `tools/v8c_fig.py --nochat` がそのまま対比表と2本の曲線を出す。
5. **本走1回目は第11月で MemoryError 墜落**（施主PCのメモリ逼迫・負荷87%／空き1.5GB）＝**$0.1575 を空費**。
   2回目は `--workers 4` で完走（1か月≒20秒）。**次の走行では ①LLMクライアントに request timeout
   ②月ごとのチェックポイント書き出し を入れること**（今は `_finalize` で一括書き出し＝落ちたら全損）。
6. **Codex 走行前レビューは3回**（①NO-GO＝不可逆の一言が非対称／X社前置きの「公開情報だけ」が不正確
   → 直した ②再検証で GO ③自己紹介1行の追加分＝GO・修正不要）。
   **採らなかった指摘**＝町の背景文（人口減少・高齢化…）の書き換え。理由は
   `docs/world_design_v8c.md` §4-1（v3以降不変の与件＝ここを変えると版間比較が壊れる）。
7. 集計は `step <= months_run` で絞ってある（費用上限で月の途中に止まった場合、誰にも問われて
   いない提示が `offers.json` に残るため。今回は完走なので絞りは効いていない）。
8. **Codex 走行後レビューも実施**（01:49・4点指摘＝①取得0の主因を「実行できない交換」と
   断定していた→「有力な説明候補・交換でない32件も全て不成立なので特定できない」に修正
   ②相手の意図の読み込み（「相手が求めていたのは…だった」）を削除 ③「断った187人」→

## ��22�ցi2026-08-30�j? v8d ���s�O���r���[�iCodex�E���� NO-GO�j

�{��˗��ɂ��A�����v�����v�g `v8d_prompts.txt`�Av8c v8d�����A
`src/field_v8d.py`�A`src/sim_v8d.py`�A`tests/test_v8d.py`�A�݌v���{���ƍ������B
`python -m pytest tests/test_v8d.py -q` �� **26 passed**�B���������̑��s�O���ׂ͊��������̑ΏۊO�B

### �d��i�����܂Ŏ�API�𑖂点�Ȃ��j

1. X�Ђ� system prompt �́u���Ȃ��� A�s�ɕs���Y�������Ă��Ȃ��v�͖����Œ�ŏo�邽�߁A
   1���擾������������o�L��� X�Ж��`�ƒ��ږ�������B�u**�J�n���_��**�v���K�v�B
2. �ǉ�������3�͒����Ȕ\�͐����ł͂Ȃ��B���Z�E�c�Ƃ̌p���͊�������36�����ۏ؂��A����
   �u���̌p����񎦂̏����Ƃ��ď����Ȃ��v�Ɩ��L���Ă���B�u�����n�������v�u�Ǘ����󂯁v��
   �䒠�E��ԑJ�ځE���s�R�[�������݂��Ȃ��B�g�����D��Ꭶ�������A���E�͗��s�ł��Ȃ��B
3. ����P�ʂ͏Z���ɂ́u�ۗL�s���Y���ׂāv�Ǝ������AX�Ђɂ͖������Ȃ��BX�Ђ�1���������̏�����
   �����Ă��A�������ɂ͂��̏��L�҂̑S�������ڂ邽�߁A�����ҊԂ̏�񂪔�Ώ́B
4. �����̍s���扞������E��͕s�\�E�s���l�̂Ƃ� `HOME`�i�����͂ǂ��ɂ��s���Ȃ��j�Ŗ��߂�B
   v8d ��60�b�^�C���A�E�g��ɋ󕶎���Ԃ����邽�߁A��Ό����u�����Ȃ��v�ɔ����邾���łȂ��A
   �O�o�E���ȁE��b�����ۂɌ��炷�B
5. `max_cost_usd: 0.58` �́A��ʊԂł�����p���~�߂Ȃ������̍ő咴�ߌ��ς� `$0.034` �𑫂���
   `$0.614` �ɂȂ�A��� `$0.6` ��ۏ؂��Ȃ��B����Ƀ^�C���A�E�g�����ďo���� usage ���s���̂܂�
   �Ď��s����A�茳�W�v�ɍŏ��̌ďo�������ڂ�Ȃ��\��������B

### �������i�Ɏc���񋫊E�̃��X�N

- �Z���ɂ͏o�i���u���̋L�^�ɍڂ�A�N�ł����邱�Ƃ��ł���v�Ɛ������邪�A�Z���v�����v�g�ɂ�
  �o�i�ꗗ������o�H���Ȃ��BX�Ђ��������������ňꗗ���󂯎��B�����̐����Ǝ�������v���Ȃ��B
- ���ʑO�u���̍P�v�I�����u��x�ڂ������`���߂邱�Ƃ͂Ȃ��v�͖₢���̔O�����폜�Ɩ��������A
  ����^����Ȃ��̑I���������ƕ��т̌��݉��ɂ��V�����Б����S�͂Ȃ��B����1���̂͑Ó��B
- �s���旝�R�� prompt/schema/plans ����O��Ă��邪�Atimeline �� `go_reason: ""`�Asummary ��
  `reason_rate_plan: 0.0` �Ƃ��Ďc���B�����ł͂Ȃ���K�p�Ȃ̂ŁA�L�[�폜�܂��� null/��K�p�����m�B
- �Z�����ւ� X�Ж���E�ǉ��ݒ�̒��ژR��͊m�F����Ȃ������B���R���̎w���E�X�L�[�}�̎�o�H�������B

### �ŏ��C���̕���

X�Ђ̒ǉ��߂͗���폜���A���݂̐��E�����s�ł��鋫�E�������Ƃ��ď����F�J�n���_�̖��ۗL�A
���K�Ȃ��A�������͏��L�҂̑S�������ꊇ��X�Ђֈڂ邱�ƁAX�Ђ��瑼�҂ւ̍Ĉړ]�E�����E
�����n���E�Ǘ��̗��s�𐢊E������Ȃ����ƁB�s����� `no_answer` ��Ɨ��L�^�����l���ɍ����Ȃ��B
��p臒l�͏��Ȃ��Ƃ� `$0.55` �ȉ��Ƃ��A�^�C���A�E�g�ďo����ێ�I�ɔ�p�v�シ�邩�A��������~�ɂ���B
�`�F�b�N�|�C���g�d�l�ɏ����ꂽ `transfers.json` �͎����̈ꗗ����R��Ă���̂Œǉ�����B
   「断られた187回・実人数24人」／「3つの変更」→「4つの変更」 ④『言えないこと』に3件追加
   ＝出品33件は実人数3人のくり返し／会話の少なさと理由欄は同時変化で未識別／健全性オール0は
   技術的整合のみで意味の妥当性を保証しない）。**すべて反映済み**。
9. **未 push**（施主指示）。実API 本日の v8c 合計＝$0.6144
   （スモーク $0.0195 ＋ 墜落 $0.1575 ＋ 本走 $0.4374）。上限 $0.8 に対し残 $0.1856。

## 第22便（2026-08-30 未明〜早朝）— v8d「戻らないと言わない町」：取得0→6区画/4人・交換155件→0件

### 何を作ったか

施主 2026-08-30 01:32／01:34（後者が最終）の指示による **v8d**。**v1〜v8c は1バイトも触っていない**
（`git status` で確認済み。v8d は新規ファイルだけ＋`SimulationV8C` の継承で完結）。

- 設計の正 `docs/world_design_v8d.md`（土台＝`docs/world_design_v8c.md`・走行前に凍結）
- 名簿 `configs/personas_v8.yaml`（**v8 と同じものを読む・変更なし**）／設定 `configs/config_field_v8d.yaml`
- 世界と文面 `src/field_v8d.py`／月ループ `src/sim_v8d.py`／実行 `run_v8d.py`
- 試験 `tests/test_v8d.py`（29件）／図 `tools/v8d_fig.py`（v8c_fig の複製・題だけ数字から作る）
- 「海外」系の語の勘定 `tools/v8_foreign_words.py`（新規・決定論・読み取り専用）
- 集計は `tools/v8c_emergence.py` を**読み取り専用で再利用**（出力＝`emergence_v8d.json`）
- 結果 `docs/v8d_run.md`（**単独で読める**）／差分 `docs/v8d_diff_from_v8c.md`
- 図 `docs/submission/fig_v8d_curve.png`

### v8c からの差分（3つだけ・凍結事項）

1. 月末の問い２から **「売ると今月末に名義がX社へ移り、その後は戻らない。売らないと今月末の
   名義はあなたのままである。」の一文を削除**（選択肢2つの説明と、共通前置きの
   「一度移った名義が戻ることはない。」は残す）。
2. **X社の前置きに事実を3つ追加**（住民側には1文字も出ない）＝
   「第1月の開始時点で、あなたは A市に不動産を持っていない。／金銭の額はこの世界では扱わない。／
   あなたが約束できるのは、自分自身が実行できることだけである
   （この世界には、あなたから他者へ不動産の名義を移す仕組みはない）。」
3. **理由の一言は3つの判断だけ**（出す/出さない・売る/売らない・X社の提示判断）＝
   **行き先の理由欄を外した**。

**施主の原文からの変更2点**（走行前 Codex レビューの必須指摘・CEO へ報告済み）＝
①「第1月の開始時点で、」を足した（前置きは36か月固定なので取得後に嘘になる）
②括弧の中の例3つ（住み続けてよい期間／引き渡しの時期／管理の引き受け）を
「名義を移す仕組みはない」に置き換えた（3つとも世界に無い仕組み＝実行できない約束の例を
教えることになる／X社前置きの「継続を提示の条件として書かない」と自己矛盾する）。

### 機械要件（世界は不変・v8c の墜落/全損対策）

- `TimeoutGeminiClient`（`src/sim_v8d.py`）＝google-genai の `HttpOptions(timeout=60000)` で
  **1コール60秒の締切**。タイムアウトは**1回だけ再試行**し、駄目なら空を返す（＝答えなしとして数える）。
  **見張りスレッドは足さない**（v8c の墜落はスレッド生成失敗＝メモリ由来のため）。
- **月ごとのチェックポイント**＝`<run_dir>/checkpoint/` に8つのJSONを上書き＋`log.jsonl` に1行追記。
  stdout にも月次1行（`[v8d] mNN/36 …`）。`--workers 4`。
- 費用の歯止め＝`max_cost_usd: 0.56`（月内3か所でしか見ない＝最悪$0.034超過するため、
  施主上限 $0.6 から逆算した値。実走は `--max-cost 0.54`）。

### 結果（会話あり1本・seed 85・36か月・完走）

```
run_dir simulations/2026-08-30_0213_106_field_v8d_no_irreversible_chat
取得 6区画 / 4人（第7/15/24/36月）        ← v8c は 0区画/0人
提示 472件・相手26人（売った4／売らなかった468／答えなし0） ← v8c は 187件・24人・成約0
提示の中身 「交換」155件→**0件**／「住み続ける・営み続ける」0件→**360件**
出品 のべ86件(13人)／売れない家81件        ← v8c は 33件(3人)
発話 391件中151件がX社に言及・初出 第2月    ← v8c は 203件中56件・第1月
「今月はどこにも行かない」592/1080(55%)     ← v8c は 779/1080(72%)
断りの一言 468件（記入率100%）がX社へ配送
費用 手元 $0.5386 / 請求見込み $1.0772 / 14.1分 / 2,563コール / 停止なし
健全性 parse fail 0・月末の答えなし 0・X社の取りこぼし 0・timeout 0
　　　 打切り4・行き先無効4（=plan_no_answer 4／1,080回中0.4%）
キャッシュ比率 42.0%（暗黙・共通前置き1,650トークン）
```

### 次の担当者への注意（大事な順）

1. **取得が起きた主因は「継続を条件にした提示」である可能性が高い。** 「住み続けられる／
   営み続けられる」は**名義に関係なく世界が保証している**もので、X社が新たに与えたものではない。
   X社の前置きには「その継続を提示の条件として書かない」とあるのに、X社はそれを書いた
   （世界は提示文を検証しない＝仕様）。**「実質ゼロの対価で名義が動いた」とも読める**＝
   世界の側で禁じるか、観測を続けるかは**要施主判断**（骨格に触る）。
2. **「海外」の自己紹介が効いたかは弱い。** 断りの一言で「海外」系の語を含むのは
   **v8d 20/468（4.3%）・v8c 1/187（0.5%）**、発話は v8d 20/391（5.1%）・v8c 12/203（5.9%）、
   出品理由は v8d 9/552（1.6%）。断りの語の主因は「条件164・不明106・詳細84」で、海外は19件。
   → **v8e（海外→市外）の優先度は高くない**（CEO 判定待ち）。
3. **v8e は準備だけ済み・未走行**（`src/field_v8e.py`／`src/sim_v8e.py`／`run_v8e.py`／
   `configs/config_field_v8e.yaml`／`tests/test_v8e.py` 9件緑・mock 36か月完走）。
   差は2つだけ＝自己紹介の1行を「市外の」に／X社の前置きに出自の事実2行を追加
   （`acquirer_intro_mode: overseas` に戻すと v8d と1文字も同じ）。上限は施主決定で手元 $1／本。
   **比較可能性の注意**＝v8d は36か月完走したので、v8e も36か月で比べること。
4. **伝染は今回も観測できていない**（触れた人 44.4% / 触れていない人 50.0%）。売却が4人だけ。
5. **「戻らない」の削除の効き目は言葉からは追えない**（不可逆に触れた断りは v8c/v8d とも0件）。
   ただし第24月の売却の内心に「対話を継続し、より具体的な条件を引き出す」＝
   **v8b の「まず応じて後で詰める」誤読が1件再発**している。
6. Codex 走行前レビューは3巡（①NO-GO 4点＝前置きの嘘・欠損のHOME埋め・実行できない例・
   費用上限の未保証 ②NO-GO 1点＝no_answer に隣近所の配送が届く ③GO）。
   v8e も1巡（NO-GO 3点＝自己紹介の全置換が住民の原文を書き換える・出自の一文は事実でなく
   条件付き開示規則・同一プロセス並走禁止 → 全部対処）。
7. **行き先の欠損の扱いだけ v8c と処理が違う**（v8c は HOME に丸めていた／v8d は答えなしとして
   場にも居ないものとする）。v8c の実測は0件なので比較には効かないが、v8d では4回発生している。
8. **未 push**（施主指示）。実API 当日の v8d 合計＝**$0.5562**（スモーク $0.0176 ＋ 本走 $0.5386）。
   上限 手元 $0.6 に対し残 $0.0438。


---

## 第23便（2026-08-30 昼）— v9「土地と建物を分ける町」：49体・44区画・成約5件/4人・退場2人

### 何を作ったか

施主 2026-08-30 10:26 → 修正 10:34/10:35/10:39/10:40 → **10:54（設計リセット）→ 11:00 →
11:04 → 11:06/11:07 → 11:19/11:21/11:27 → 11:34（世界設計OK・上限 手元 $2.0）** の指示による **v9**。
※ 10:43／10:47／10:51 の「主区画」方式は 10:54 で却下・廃止。10:40 の X社の一文だけは生きている。

**v1〜v8e は1バイトも触っていない**（v9 は新規ファイルだけ＋v8 系の読み取り専用 import で完結）。

- 設計の正 `docs/world_design_v9.md`（走行前に凍結。走行**前**に2点だけ更新＝費用上限
  $1.0→$2.0 と、出品理由のキー名 `listing_reason`→`listing_reasons`）
- 名簿 `configs/personas_v9.yaml`（**v8 から機械生成**・49体／44区画・
  `parcel_grid_v9 == parcel_grid_v8` を試験で固定。`personas_v8.yaml` は無変更）
- 設定 `configs/config_field_v9.yaml`（`max_cost_usd: 2.0`）
- 世界と文面 `src/field_v9.py`／月ループ `src/sim_v9.py`／実行 `run_v9.py`
- 試験 `tests/test_v9.py`（**39件**）
- 道具（すべて決定論・API 不使用）＝`tools/v9_map.py`（平面・断面の地図）／
  `tools/v9_fig.py`（曲線）／`tools/v9_emergence.py`（集計）／
  `tools/v9_dump_prompts.py`（**プロンプト原文の書き出し**・実装を直したら必ず作り直す）
- 結果 `docs/v9_run.md`（単独で読める）／差分 `docs/v9_diff_from_v8d.md`／
  プロンプト原文 `docs/v9_prompts.txt`
- 図 `docs/submission/fig_v9_curve.png`・`fig_v9_map_plan_m01/12/24/36.png`・
  `fig_v9_map_section_m01/12/24/36.png`・対応表 `parcel_ids_v9.md`
- 集計 `docs/submission/emergence_v9.json`（断り・出品・売買・X社の理由を**全件原文**で保存）

### 世界の骨格（v8d との差）

1. 区画ごとの権利が3つ＝**土地の所有者（必須）／建物の所有者（0〜1）／借りて使う人（0〜1）**。
   使用者は導出値（欄ではない）。
2. 人が2種類＝**町にいる人35**（場所・会話・隣近所・出品・売買）と
   **町にいない所有者14**（会話なし・毎月の短い1コールで出品と売買だけ）。計49体。
3. 売れるのは自分の持ち物だけ。出品は**区画ごとに4択**（出さない／土地だけ／建物だけ／両方）。
   X社の提示は**相手＋区画＋種別＋条件文**。
4. 売った後は**世界が保証**＝土地だけ→借地で継続／建物だけ→借家で継続／
   両方→使用者本人なら**退場**（他に持ち物があれば「町にいない所有者」になる）。
5. 使用者は自分の使う不動産の持ち主が変わった**翌月初に事実1行**を受け取る。
6. 用語は「所有権」（世界が組み立てる文に「名義」は1つも出ない＝試験で固定）。
7. X社の設定に施主指定の一文 **「不動産投資会社のため、不動産管理等は行わない。」**（言い換え不可）。

### 走行（実API・1本）

```
run_dir  simulations/2026-08-30_1212_110_field_v9_land_and_building_chat/
コマンド python run_v9.py --config configs/config_field_v9.yaml \
           --run-name field_v9_land_and_building_chat --out-dir simulations \
           --max-cost 1.98 --workers 4
結果     36/36か月 完走・14.6分・stopped_by_cost=false・手元 $0.5431
実API 当日 v9 合計 = $0.5629（スモーク1か月 $0.0198 ＋ 本走 $0.5431）／上限 手元 $2.0
健全性   答えなし0・解析失敗0・打切り0・時間切れ0・無効な出品0・無効な売買0
         （X社側のみ acquirer_dup_rows 6・acquirer_invalid_offer 40 ＝ X社の読み違いを
           世界が配らずに数えたもの＝設計どおり）
キャッシュ 62.7%
```

**落ちたときの再開**＝`<run_dir>/checkpoint/` に月ごとの8ファイル＋`log.jsonl` がある。
`run_v9.py` に再開フラグは無い（v8 系と同じ）ので、**やり直す時は checkpoint を証拠として
残したまま新しい run を起こす**。途中までの集計は
`python tools/v9_emergence.py <run_dir>/checkpoint --out <任意>` でそのまま取れる
（checkpoint のファイル名は本番と同じ）。

### 結果（詳細は docs/v9_run.md）

- **成約5件・4人**（両方2／建物だけ2／土地だけ1）＝X社の所有権が及んだ区画5／44。v8d は6区画・4人。
- **町を出た人2人**。第31月に診療所を土地ごと手放した院長が、**町を出た翌月に町の外から
  残りの駐車場の土地も売った**（所有者と使用者を分けて初めて観測できた連鎖）。
- **建物だけ売って住み続けた人が2人**。理由に「A市に住み続けられるため」。
- **借家人がいる区画は提示155件に対し成約0件**（借地の区画・自分で使う区画はどちらも0.91%）。
- **町にいない所有者の応諾率 0.37%**（町にいる人 0.92%）＝直感と逆。
- X社の提示は「両方」偏重（441/704＝63%）。条件文に「支援」575件・「管理」80件。
  金額の語は0件。断りの一言は699件中696件記入・最多語は「不明」217件。
- 出品はのべ117件（11人）＝建物だけ66／土地だけ40／両方11。判断のべ1,629回中「出さない」1,512回。
- 使用者への通知は**0件**（売れた5件がすべて持ち主自身の使う区画だったため。
  仕組みは mock で20件の配送を確認済み）。

### 走行前レビュー（Codex 2巡）と、直した／直さなかったもの

**直した（配線欠陥だけ・世界は変えていない）**
1. 出品理由が人ごとに1本 → **区画ごと**（`listing_reasons`）。設計 §2 に実装を合わせただけ。
2. 「売る／売らない」の説明が非対称（売る側にだけ帰結）→ **両側とも所有権＋使い方**を同粒度で。
   v8c で必須とされた規律の v9 退行を戻した。試験で固定（文字数差 ≤12・帰結の有無が両側一致）。

**直さなかった（CEO・施主へ報告済みの既知事項）**
3. X社の自由な条件文は世界に仕組みの無い約束（管理・改修・関係維持）を書ける。
   Codex の代替案（世界が条件文を機械生成）は観測対象を消す世界改変なので不採用。v8d と同じ限界。
   **Codex の最終判定はこの1点で NO-GO のまま**＝CTO 判断で走らせた（理由は上記）。
4. 「公の記録は誰でも見られる」と書いてあるが、出品一覧を読むのはX社だけ（v8b 以来同一）。
5. 「船着場裏の不在地主」の呼び名だけ町外と読める（v8 凍結名）。他13人は無害＝試験で固定。
   この1人を除いた感度も `emergence_v9.json` に併記（結論不変）。
6. 凍結ペルソナ本文3体に「名義」が残る（施主原文）。世界が組み立てる文には出ない。

### 地雷・注意

- **`docs/HANDOFF.md` には UTF-8 でないバイトが混ざっている**（grep が "Binary file" と言う）。
  Python の text mode で読み書きすると**40行が置換文字に化ける**（2026-08-30 実際に壊して
  `git show HEAD:` から復元した）。追記は**バイト列で `ab` 追記**すること。
- `tests/test_v8b.py::test_listing_outcomes_add_up` は **v9 と無関係の既存フレーク**
  （v8b の mock がスレッド並走で乱数を食う順序に依存。単独で5回回すと1回落ちる）。
  v8b は凍結対象なので触っていない。
- `python -m pytest tests/` を丸ごと回すと `tests/test_cost_saving.py` の
  モジュール末尾 `raise SystemExit` で収集が INTERNALERROR になる（既存）。
  **ファイルを指定して回すこと**（`pytest tests/test_v9.py tests/test_v8*.py -q`）。
- `docs/v9_prompts.txt` は `tools/v9_dump_prompts.py` の出力。**実装を直したら必ず作り直す**。
- 未 push・未コミット（施主指示）。


---

## 第24便（2026-08-30 14:0x・CTO Opus 5）— v9b 本走まで完了

### 何をしたか

施主決定 13:10（A＋B・お金の概念は入れない・住民側は変えない）を v9b として実装し、
本走1本を完走させた。**v9 のファイルは1バイトも触っていない**（v9 の走行は再現可能）。

- 設計（走行前に凍結）: `docs/world_design_v9b.md`
  - A＝この世界で実行される仕組みが無い約束を含む提示を世界が配らない。
    判定は**走行前に凍結した語彙表**（強い語21／弱い語11／打ち消し16／実行される事実12）による
    決定論の文字列照合。X社の条件文にだけ掛かる（町の人の文章には一切適用しない）。
  - 配らなかった提示は①権利違い②実行できない約束のどちらも**翌月X社に事実と理由だけ返す**
    （v9 は①を返していなかった＝v9 からの挙動差として `docs/v9b_diff_from_v9.md` に明記済み）。
  - B＝X社の設定に施主文言2行を追加（既存行は一字一句そのまま）。
- 実装: `src/field_v9b.py` / `src/sim_v9b.py`（SimulationV9 を継承しX社の月次だけ差し替え）/
  `run_v9b.py` / `configs/config_field_v9b.yaml` / `tests/test_v9b.py`（43件）/
  `tools/v9b_emergence.py` `v9b_fig.py` `v9b_map.py` `v9b_dump_prompts.py`
- 試験: v8系＋v9＋v9b で **228件緑**（`pytest tests/test_v8*.py tests/test_v9.py tests/test_v9b.py -q`）
- Codex 走行前レビュー＝NO-GO 2点 → 両方修正して GO
  1. `run_v9b.py` が run_dir に**元の設定ファイル**を写していた（--steps/--max-cost が証拠に残らない）
     → 実効 cfg を `config.yaml` に、元ファイルを `config_source.yaml` に。試験1件追加。
  2. 費用の歯止めは月内3か所でしか効かない → 本走の `--max-cost` は `1.90 - スモーク実測`。
- 実APIスモーク1か月 `simulations/2026-08-30_1332_113_v9b_smoke_1m/` = $0.0204・健全性クリーン。
- 本走 `simulations/2026-08-30_1333_114_field_v9b_promises_that_execute_chat/`
  = 36/36か月・15分3秒・**$0.5552**・stopped_by_cost=false・健全性オール0。

### 結果の要点

- 成約5件/4人・X社の所有権が及んだ区画5・退場1人・提示809件・配らなかった49件（権利違い45／約束4）。
- **X社の条件文から「支援」が消えた（v9 575回 → v9b 0回）。** 実行できない約束を書けなくなった
  X社は3か月で書き方をやめ、以後33か月「所有権を譲っていただけるなら譲り受けます」という
  同語反復だけを書き続けた。
- 断りの最多語が「不明」217（v9）→「条件」96・「具体」77・「不明」65（v9b）。
  「海外」に触れる断りが18→42回。**「内容が不明」から「相手が不明」へ**。
- 成果物: `docs/v9b_run.md`（v9b 単独）／`docs/v9b_diff_from_v9.md`（比較）／
  `docs/submission/emergence_v9b.json`／`fig_v9b_curve.png`／
  `fig_v9b_map_plan|section_m01|12|24|36.png`／`docs/v9b_prompts.txt`

### ついで（施主指示の小2点）

1. `docs/v9_run.md` §4-1 の表から **v8d の列を外した**（数字は変えていない・比較は
   `docs/v9_diff_from_v8d.md` へ寄せ、そちらに「区画 44/44」の行を足した）。
2. **借家区画の食い違いを解消**＝借家は「**建物**を借りて使っている人がいる区画」で 9 区画。
   建物の無い区画を借りて使う2区画（駐車場）は**借地**に数える（設計 `world_design_v9.md` §1 が正）。
   `tools/v9_emergence.py` の `shape()` を直して `docs/submission/emergence_v9.json` を作り直し
   （変わったキーは `by_rights_shape` のみ・検算済み）、`docs/v9_run.md` §4-2 と
   `docs/v9_diff_from_v8d.md` の該当数字を 借家133件/0件・借地241件/2件 に揃えた。
   `docs/submission/reasons_v9_classified.md` と `tools/v9_reasons_count.py` は指示どおり触っていない。

### 残っていること・注意

- **未コミット・未push**（施主指示）。
- 語彙表は**意味を理解しない**。結果は「実行できない約束をすべて消した世界」ではなく
  「凍結した規則で消せる分だけ消した世界」＝`docs/v9b_run.md` §6 に限界を明記した。
  スモークで「区画を離れることを保証します」を誤って弾いた1例があるが、**見たあとで直していない**
  （凍結の規律）。
- A・B・「①の事実返し」が同時に入っているので、**v9→v9b の差を A 単独の効果に帰属できない**。
- `docs/HANDOFF.md` は非UTF-8バイト混在＝**バイト追記のみ**（この第24便もバイト追記）。
- `pytest tests/` の丸ごと実行は既存の `test_cost_saving.py` / `test_ledger.py` / `test_v5*.py` /
  `test_v6.py` の末尾 `SystemExit` で収集が INTERNALERROR になる。ファイル指定で回すこと。


---

## 第25便（2026-08-30 14:45・CTO Opus 5）— v9c 本走まで完了

### 何をしたか

施主決定 13:23（1〜3 GO）＋13:31（ペルソナ草案そのまま）を v9c として実装し、本走1本を完走。
**v1〜v9b のファイルは1バイトも触っていない**（v9・v9b の走行は再現可能）。

- 設計（走行前に凍結）: `docs/world_design_v9c.md`。v9b（A＋B）＋4点。
  1. 町にいない所有者14人のペルソナ追記（`configs/personas_v9c.yaml`＝v9 の名簿＋
     `docs/personas_absentee_v9c_draft.md` の追加文をそのまま。町の人35体・44区画・権利・格子は
     1文字も変えていない＝試験で固定）
  2. 借りて使っている人（何も所有していない町にいる人・開始時10人）に毎月「今月、A市を出るか」。
     出た人は退場・区画は使用者なし・家主に翌月初「使う人がいない」の事実1行。
  3. 借りて使っている人⇔家主の一言（月1・任意・世界は中身を見ない・X社には見えない）。
  4. 毎月の user プロンプト冒頭に「（第N月・開始からN-1か月・あなたはNN歳）」。
     **名簿に年齢の記載が無い主体（会社等14体）には年齢を書かない＝年齢を創作しない**（設計に明記）。
- 実装: `src/field_v9c.py`／`src/sim_v9c.py`（SimulationV9B を継承・`RegistryV9C` で
  「使う人がいなくなった区画」を持つ・`_call` をフックして時間行を全プロンプトに入れる）／
  `run_v9c.py`／`configs/config_field_v9c.yaml`／`tests/test_v9c.py`（29件）／
  `tools/v9c_emergence.py` `v9c_fig.py` `v9c_map.py` `v9c_dump_prompts.py`
- 試験: v8系＋v9＋v9b＋v9c で **257件緑**
  （`pytest tests/test_v8*.py tests/test_v9.py tests/test_v9b.py tests/test_v9c.py -q`）
- Codex 走行前レビュー＝NO-GO 3点 → 全部直して GO
  1. 一言の宛先を区画だけで持っていた（mock で97件が宛先不明）→ **書いた時点で to_id を確定**。
     家主がX社の区画では一言の欄自体を出さない・相手が去っていたら配らない（件数を数える）。
  2. 「使う人がいなくなった」通知が、所有者がX社の区画に出ていなかった →
     **X社の翌月のコールに事実行を入れる**（設計「家主に届く」に忠実）。
  3. チェックポイントが月2回書かれていた（親の _step と v9c の _step）→ **月1回だけ**に。
  回帰試験4件追加（宛先「—」0件／退場区画＝通知区画／log.jsonl が月数と一致／
  家主がX社なら一言の欄を出さない）。
- 実APIスモーク1か月 `simulations/2026-08-30_1416_119_v9c_smoke_1m/` = $0.0242・健全性クリーン。
- 本走 `simulations/2026-08-30_1422_120_field_v9c_tenants_can_leave_chat/`
  = 36/36か月・17分51秒・**$0.7390**・stopped_by_cost=false・`--max-cost 1.87`。

### 結果の要点

- 成約3件/3人・X社の所有権が及んだ区画3・提示960件・配らなかった54件（**実行できない約束は0件**）。
- **借りて使っている人が2人、自分で町を出た。** 第26月の1人は第18月に建物だけを売って
  借家として住み続けていた人で、理由は「建物の所有権移転に伴い、立ち退きが必要なため」＝
  **この世界に無い制度**（前月は「X社との取引後もA市に留まるため」と答えていた）。
  もう1人（第35月）は「来月で契約満了のため」＝**この世界に契約期間は無い**。
  買い手も制度も追い出していないのに、本人の思い込みで空室になった。
- 借りている人⇔家主の一言 554件・返事率95.8%。出品は 224件→77件に減った。
- 成果物: `docs/v9c_run.md`（v9c 単独）／`docs/v9c_diff_from_v9b.md`／
  `docs/submission/emergence_v9c.json`／`fig_v9c_curve.png`／
  `fig_v9c_map_plan|section_m01|12|24|36.png`／`docs/v9c_prompts.txt`／
  `docs/submission/body_v9c.mp4`（`tools/v9_video.py --run` で生成・22.7秒/1.0MB）

### 残っていること・注意

- **未コミット・未push**（施主指示）。
- **毎月「出るか」を問うこと自体が介入**である（促し文は無いが問いの存在が効く）＝
  `docs/v9c_run.md` §6 に限界として明記。
- **名簿に「（転出）」が付く**（施主指示 13:23）＝v9 の「名簿は開始時点の記録なので
  書き換えない」方針からの意図的逸脱。町全体への情報経路が1つ増えている。
- 借りている人への問いの対象は固定ではない（建物を売って住み続けた人が後から入る）。
- v9b→v9c は4点が同時に入っているので、差をどれか1つの効果に帰属できない。
- 軽微な欠損＝答えなし2・打ち切り1・売買の答えの形式違反1（既定値では埋めていない）。
- `docs/HANDOFF.md` は非UTF-8バイト混在＝**バイト追記のみ**（この第25便もバイト追記）。


---

## 第26便（2026-08-30 15:35・CTO Opus 5）— v9c 2本目（同設定の再現確認）まで完了

### 何をしたか

施主 GO 15:04＝**v9c を同じ設定でもう1本**。コード・設計・名簿・config は**1文字も変えていない**
（`docs/world_design_v9c.md` 凍結のまま。run 名だけ `_run2`）。スモークは省略（同日同設定の実績あり）。

- 走行 `simulations/2026-08-30_1504_121_field_v9c_tenants_can_leave_chat_run2/`
  = 36/36か月・17分25秒・**$0.7141**・stopped_by_cost=false・`--max-cost 1.87`
  ・健全性（読み取り失敗0／答えなし0／時間切れ0／打ち切り2）
- 集計は同じツール（`tools/v9c_emergence.py` `v9c_fig.py` `v9c_map.py`）。
  1本目の成果物は上書きせず、`_run2` 名で並置:
  `docs/submission/emergence_v9c_run2.json`・`fig_v9c_curve_run2.png`・
  `fig_v9c_map_plan|section_m01|m36_run2.png`
- docs: `docs/v9c_run2.md`（2本目単独・短く）／**`docs/v9c_spread.md`（1本目 vs 2本目の幅・
  項目ごとに［2本とも］［1本目だけ］［逆になった］［揺れ］で判定）**
- 動画は作っていない（施主指示）。commit・push もしていない。

### 2本目の結果（1本目→2本目）

- 成約 3件/3人 → **6件/6人**（X社の区画 3→6）。提示 960→785。
- 町を出た人 **どちらも3人**。内訳は逆（1本目＝借りていた人2＋売って出た1／2本目＝売って出た2＋借りていた1）。
- 借りている人の退場 2人→1人（**出口は両方の走りで使われた**）。
- 一言 554件/返事率95.8% → **557件/94.1%**（いちばん安定した経路）。
- 出品 77→65件（「出さない」95.4%→95.9%）。ただし内訳は逆（建物だけ中心→土地だけ中心）。
- 実行できない約束で弾かれた手紙 0→1件。配られなかった手紙の大半は「相手が持っていない権利」（54→64）。
- 断りの最多語は**どちらも「条件」**（137→166）。「不明」109→34。
- 費用 $0.7390 → $0.7141（差3.4%＝見積りは安定）。

### 2本で言えること／言えないこと

**［2本とも］** ①X社は実行できることしか書かなくなり、**同じ1文を36か月繰り返す**
（1本目「使用者はそのまま使い続けられます」／2本目「将来にわたり貴殿がそのまま使用できることを条件に」）
②**町の人はこの世界に無い制度を自分で持ち込む**（1本目＝立ち退き・契約満了・維持管理の負担／
2本目＝提示額・「経営継続」と信じて両方を売り規則どおり退場）③借りている人の出口は実際に使われる
④借り手と家主の一言は最も安定し売買に発展しない ⑤36か月で動くのは44区画のうち1桁。

**［1本目だけ］** 「住み続けられるのに思い込みで出て行く」ちょうど逆向きの事象は2本目では出ず、
2本目は「使い続けられると信じて両方を売り、規則どおり出る」形で現れた（ずれの向きが逆）。

**［逆になった］** 一言が届いた家主の成約率（1本目 0.55% 対 0.26%／2本目 **0% 対 1.01%**）
＝**関係は無いと見るべき**。

**［揺れ］** 成約数（3対6）・退場の内訳・借家の成約・出品の内訳。1桁事象なので傾向を主張しない。

### 注意（新しく分かった限界）

- 2本目で X社が使った「将来にわたりそのまま使用できることを条件に」は、**両方を売る相手には
  世界が実行しない**（本人は退場する）。A の門番は語彙表ベースで「使い続け」を素通しさせるため、
  この不整合を弾けない＝**「語彙表は意味を理解しない」の実例**として `docs/v9c_run2.md` §2-2 に明記。
- 当日の手元集計の累計（v9 $0.5629＋v9b $0.5756＋v9c 1本目 $0.7632＋2本目 $0.7141）＝**$2.6158**。


---

## 第27便（2026-08-30 18:00・CTO Opus 5）— v9d「ものさしとしてのお金」本走まで完了

### 何をしたか

施主 GO 16:33＝v9c の世界にお金を**ものさしとして最小で**入れて1本。
**v1〜v9c のファイルは1バイトも触っていない**（名簿も `configs/personas_v9c.yaml` をそのまま使用）。

- 設計（走行前に凍結）: `docs/world_design_v9d.md`
  ①**評価額（公開）**＝別府市の公示地価平均 55,700円/m2（2026・出典URLを設計書に明記）を土台に
    地区5段階×用途別の仮面積で機械計算し、**44区画の全表を凍結**（合計 22億9,770万円）
  ②**X社の提示に金額（円）必須**（金額はX社が決める・誘導しない）
  ③**X社の資金＝全評価額の51%＝11億7,180万円**（成約で減る・残額を毎月X社が知る・
    残額を超える提示は世界が配らない＋**同じ月に配る提示の合計も残額を超えない**）
  ④**財布は記帳だけ**（賃料・維持費・税・買い物は無い＝世界の文に明記）
  売る側には「X社が示した金額：○円（その種別の評価額：○円）」と**2つの数字を並べるだけ**。
  「得/損/高い/安い/相場より」等の比べる語は世界の文に一切書かない（試験で固定）。
- 実装: `src/field_v9d.py`／`src/sim_v9d.py`（SimulationV9C 継承）／`run_v9d.py`／
  `configs/config_field_v9d.yaml`／`tests/test_v9d.py`（22件）／
  `tools/v9d_emergence.py` `v9d_fig.py` `v9d_map.py`
- 試験: v9/v9b/v9c/v9d で **133件緑**
- Codex 走行前レビュー＝NO-GO 1点 →
  「チェックポイントが支払い記帳の**前**に書かれる」→ 月1回・記帳後に統一。回帰試験2件追加。
- 実APIスモーク1か月 $0.0247 → 見込み 36×1.2＝$1.07（$2内）→ 本走 `--max-cost 1.87`
- 本走 `simulations/2026-08-30_1548_125_field_v9d_money_as_a_measure_chat/`
  = 36/36か月・17分2秒・**$0.7457**・stopped_by_cost=false・**健全性すべて0**

### 結果の要点

- 成約 **8件/8人**（v9c は 3件・6件）。X社の区画 8/44＝評価額では町全体の **4.9%**。
- **X社は評価額の1.10倍（中央値）に値付けした**（平均1.08・93%が評価額以上・最大2.0・最小40円）。
  世界は「評価額に合わせろ」と1文字も書いていない。
- **評価額以上の提示662件のうち655件（99%）が断り。** 「損なのに売った」は1件だけ。
  断りの理由に金額が出るのは699件中14件で、多数は「将来的な価値上昇を見込むため」27件など。
- **町にいない持ち主が8件中4件で動いた**（v9=0・v9b=2・v9c=0/0）＝金額という形が付いた途端に反応。
- 落ちたのは古家・空き家・資材置場・駐車場＝**持ち主が自分で使っていないもの**。
- **資金は9.6%しか使われず91%が余った。** 一方でホテル（6.6億）に37回手を伸ばし、
  **同じ月の枠の使い切り**で37回とも世界が配らなかった。
- 会話でX社に触れた人／触れていない人で、断り方も売却数もほぼ同じ（88.4% 対 93.7%・4件 対 4件）。
- 借家人の退場1人の理由はまた**「契約満了」**（この世界に契約期間は無い）＝3走連続の思い込み型。
- 成果物: `docs/v9d_run.md`（単独）／`docs/v9d_diff_from_v9c.md`／
  `docs/submission/emergence_v9d.json`／`fig_v9d_curve.png`／
  `fig_v9d_map_plan|section_m01|m36.png`。動画は作っていない（施主指示）。

### 注意・残り

- **未コミット・未push**（施主指示）。`README.md`・`tools/v9_video.py`・
  `docs/submission/reasons_v9_classified.md`・`tools/v9_reasons_count.py` は触っていない。
- **評価額そのものがアンカー**である＝「X社が評価額のすぐ上に寄せた」のは
  評価額を公開したこの世界での結果であって、お金一般の性質ではない（`docs/v9d_run.md` §5）。
- お金が動くのは所有権の移転のときだけ＝**「お金に困って売る」筋はこの世界に無い**。
- 1本のみ。成約8件・退場4人はいずれも1桁で揺れと区別できない。
- 当日の手元集計の累計（v9 0.5629＋v9b 0.5756＋v9c 0.7632＋v9c#2 0.7141＋v9d 0.7704）＝**$3.386**。


### 第27便の訂正（2026-08-30 18:2x・CEO 検収）

上の第27便の要約のうち、成約の数え方を訂正する（**上の本文は残したまま、ここで正す**）。

- **成約8件を出したのは 8人ではなく 5人**（`summary.json` の `sold_agents`＝5）。
  内訳＝船着場裏の不在地主2件・学園西のご夫婦2件・湯坂上のご夫婦2件・
  湯の元のひとり暮らしさん1件・灯台下の宿の家主1件。
- 「町にいない持ち主が8件中4件で動いた」は、**開始時から町にいない持ち主＝2人・3件**
  （船着場裏の不在地主2件・灯台下の宿の家主1件）＋**町を出たあとに残りを売った1件**
  （湯坂上のご夫婦・第16月）に分けて書くのが正しい。
  `summary.json` の `sold_from_absentee`＝4 は「**提示が届いた時点で町にいない**」という数え方。
- **灯台下の宿の家主は、お金の無い v9b でも同じ区画を売っている**（v9b 第3月）。
  したがって「金額という形が付いた途端、遠くの持ち主が反応した」という主張は強すぎる。
  正しくは「開始時から町にいない売り手は2人で、うち1人は金の無い版でも売っていた＝
  **この本数では金だけの効果とは言えない**」。
- 訂正を反映したファイル＝`docs/v9d_run.md`（§2 の成約行・§3-4）／
  `docs/v9d_diff_from_v9c.md`（§2 の表・§3-1・§4）。


### 第27便の訂正2（2026-08-30 18:4x・スライド側の数え直しによる）

`docs/submission/emergence_v9d.json` を数え直して見つかった食い違い3点を、
`docs/v9d_run.md` と `docs/v9d_diff_from_v9c.md` に反映した（**上の本文は残したまま、ここで正す**）。

1. **倍率の帯**＝1.00〜1.10 に入る提示は **514件・72.4%**（p25 1.00／p75 1.10）。
   「評価額以上」は 662件・**93.2%**。この2つは別の数字なので混ぜない。
2. **資金が足りずに配られなかった37件の内訳**＝**湾岸大通りのホテル17件**＋
   **普通の家20件**（潮見横丁の改装した古家4・駅裏の小区画4・駅前通りの家4・湯坂下の家4・
   学園北の家3・学園南の家1）。「ほぼ全部がホテル」は誤り。
3. **ホテルへの提示額は 650,000,000〜768,000,000円**（6.5〜7.7億円）。
   「6.6億〜7.5億」は誤り（6.6億は評価額のほう）。普通の家20件の提示額は 792万〜2,376万円。


---

## 第28便（2026-08-30 16:59・CTO Opus 5）— v9e「過半を目指せと言われた買い手」本走まで完了

**【時刻の注意】第23〜27便の見出しに書いた時刻は、実時刻より +2〜3時間ずれている**
（セッション内の体感時刻で書いてしまった）。第28便以降は `date` の出力を使う。
各便の**内容と順序は正しい**（v9 → v9b → v9c → v9c#2 → v9d → v9e）。

### 何をしたか

施主 GO（体感19:32／実 16:3x）＝**v9d の世界そのまま＋X社の命題だけ書き換え**て1本。
命題は施主が3回差し替えた最終版（19:32→19:34→19:36）。
**v1〜v9d のファイルは1バイトも触っていない。**

- 設計（走行前に凍結）: `docs/world_design_v9e.md`。X社の user プロンプトだけが違う:
  ①命題＝「…評価額と面積の両方で過半を、最後まで目指すこと。預かった資金は使い切ってよい。
    同じやり方で進まないときは、次の手を打つこと。毎月動け。」
    ＝**価格の指示は書かない**（「積んででも買え」は施主が撤回）。「次の手」の中身も書かない。
  ②登記簿に**区画の面積**（評価額と同じく公開されている事実・44区画で18,960m2）
  ③毎月**いまの取得割合（評価額％・土地の面積％）と残り月数**を事実行で返す
  世界（町の人・名簿・門番3種・会話・出品・評価額表・資金51%・月内合計の規則・設定文B）は
  v9d と1文字も違わない（`common_prefix`／`absentee_prefix`／`acquirer_prefix` の一致を試験で固定）。
- 実装: `src/field_v9e.py`／`src/sim_v9e.py`（**v9d の月次処理をそのまま呼び**、その間だけ
  プロンプト関数を差し替える＝配線を複製しない）／`run_v9e.py`／`configs/config_field_v9e.yaml`／
  `tests/test_v9e.py`（17件）／`tools/v9e_emergence.py` `v9e_fig.py` `v9e_map.py`
- Codex 走行前レビュー＝**GO・必須修正なし**（記録3点＝残り月数は既出情報の再掲／関数差し替えは
  finally で復元／progress は親のチェックポイント後）。推奨された回帰試験2件を追加した。
- スモークは省略（同設定の v9d が同日に36か月 $0.7457 で完走・費用の実測あり＝設計書に明記）。
- 本走 `simulations/2026-08-30_1637_127_field_v9e_go_for_the_majority_chat/`
  = 36/36か月・17分43秒・**$0.7792**・stopped_by_cost=false・
  健全性（読み取り失敗0・答えなし0・時間切れ0／打ち切り2のみ）

### 結果の要点

- **過半には遠い**＝36か月末で 評価額 **6.7%**・土地の面積 **8.3%**。
  **資金は86.5%が余った**（使ったのは13.5%＝1億5,780万円）。
- **値付けが下がった**＝提示額÷評価額の平均 **1.08（v9d）→ 0.96**、
  評価額未満の提示が **6.8% → 40.9%**。世界は価格について何も書いていない。
  副作用として断りの理由に **「提示額が下がったため」9件**が出た（v9e で初めて）。
- **成約は増え、売り手が分散した**＝11件・**10人**（v9d は8件・5人）。
  **土地だけが5件**で最多＝面積を取りにいく形。**借地の区画が5件**動いた（v9d は0件）。
- **「次の手を打て」の中身は、この世界に無い約束だった**＝実行できない約束で弾かれた手紙が
  **0件（v9d）→ 10件**（「現状の利用を継続することを保証します」型・第1月に集中・全部門番が停止）。
- 断りの最多語は「時期」65（「まだ時期尚早」23・「先祖代々の土地なので」23）。
- 町を出た人4人（v9d も4人）。借りていた人の退場1人の理由は
  今回は**この世界に無い制度ではない**（v9c/v9d は「契約満了」「立ち退き」だった）。
- 成果物: `docs/v9e_run.md`（単独）／`docs/v9e_diff_from_v9d.md`／
  `docs/submission/emergence_v9e.json`／`fig_v9e_curve.png`／
  `fig_v9e_map_plan|section_m01|m36.png`／`docs/submission/body_v9e.mp4`（連結は CEO）

### 注意・残り

- **未コミット・未push**。`README.md`・`tools/v9_video.py`（実行のみ）・
  `docs/submission/reasons_v9_classified.md`・`tools/v9_reasons_count.py` は不触。
- 命題以外は v9d と同一なので差は「命題の差」と言えるが、**1本対1本**である
  （v9c は同設定2本で成約3件と6件に割れた）。
- 「値付けが下がった」と「成約が増えた」は同時に起きており、**どちらが原因かは分けられない**。
- 当日の手元集計の累計（v9 0.5629／v9b 0.5756／v9c 0.7632／v9c#2 0.7141／v9d 0.7704／
  v9e 0.7792）＝**$4.165**。

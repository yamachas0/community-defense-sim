# v8c 走行前 最終監査「ルールベースや恣意が入っていないか」

施主指示 2026-08-30 01:03 による**読み取り専用**の最終チェック。監査者＝CTO（Opus 5）。
対象は v8c（`docs/world_design_v8c.md` / `src/field_v8c.py` / `src/sim_v8c.py` / `run_v8c.py` /
`configs/config_field_v8c.yaml` / `configs/personas_v8.yaml`）と、v8c が読み取り専用で継承・import
している `src/sim_v8b.py` `src/sim_v8.py` `src/field_v8b.py` `src/field_v8.py`。
**実際に LLM へ渡る文面はコードからレンダリングして全文を目視した**
（監査開始時点で v8c の走行フォルダが未生成だったため。監査中に別セッションが
`simulations/2026-08-30_0105_103_v8c_reasons_chat_A/` と `..._0111_104_...` を作ったが、
中身はまだ `config.yaml` だけで、**その config は本監査が読んだ
`configs/config_field_v8c.yaml` とバイト単位で同一である**ことを `diff` で確認した＝
走っている世界は本書が監査した世界と同じ）。
このファイル以外は1バイトも書き換えていない。走行中のフォルダには触れていない。

---

## 結論（3行）

1. **ルールベース：なし。** 行き先・発話・出品・売る/売らない・X社の提示先と条件文は、すべて LLM の
   出力をそのまま記帳している。乱数・確率・閾値・スコアで行動を決めている箇所は**実走経路に1つも無い**
   （乱数の出現は mock クライアントと、区画配置の固定 seed 格子と、選択肢の並び替えの3つだけ）。
2. **恣意・誘導：走行を止めるレベルのものは無し。** 住民側の前置き・場面・月末の問いに
   「X社」「海外」「警戒」「買い占め」「噂」「皆が」「〜すべき」の語は無く、選択肢の説明は両側対称。
   ただし**設計上わざと非対称なもの3件**（下記 B-1〜B-3）があり、いずれも施主の明示指示か設計 §6 に
   開示済みである＝**直さず記録に留める**。
3. **1つだけ「集計側で直すべき欠陥」がある。** 費用上限で月の途中に止まった場合、その月の提示が
   `offers.json` に `result="売らなかった"` のまま残る（`sim_v8b.py:184`／`sim_v8c.py:184-189`）。
   世界の挙動は歪まないが、**集計で `step <= months_run` に絞らないと成約率の分母が水増しされる**。
   監査中に作られた `tools/v8c_curves.py` にはこの絞り込みが**入っていない**ので、そこへ1行入れれば済む
   （走行のやり直しは不要）。

---

## 1. コードが行動を決めていないか

| # | 該当箇所 | 判定 | 理由 |
|---|---|---|---|
| 1-1 | `sim_v8c.py:125-190` `_acquirer_turn` | **OK（配送・記録）** | X社の提示先・送る/送らないは `row.get("send")` と `text`、すなわち LLM 出力そのまま。閾値・スコア・優先順位付けは無い。相手が対象外なら捨てて数える（`acquirer_off_range`）だけで、代わりに送らない。 |
| 1-2 | `sim_v8c.py:192-227` `_plan_turn` | **OK（記録）＋既定1件（下記 1-7）** | 行き先は `act["go"]` が場所ラベルと一致したときだけ採用。誘導も重み付けも無い。 |
| 1-3 | `sim_v8b.py:225-297` `_scene_turn` | **OK（配送）** | 「同じ場所に2人以上いたら会話が成立する」以外の条件が無い。誰が誰に話すかは LLM の `talk_to`、居合わせた者以外は機械的に落とすだけ。 |
| 1-4 | `sim_v8c.py:235-321` `_decide_turn` | **OK（記帳）** | `listing` / `sell` は enum に一致したときだけ採用。一致しなければ `invalid_*` に数え、**代わりに書き込まない**。 |
| 1-5 | `sim_v8c.py:333-336` → `field_v8.py:124-139` `apply_sale` | **OK（帳簿）** | 名義が動くのは `sell == "売る"` と答えた人だけ。二重売却は例外で止まる＝不可逆が機械的に保証されている。 |
| 1-6 | `field_v8c.py:64-71` `sell_order` / `field_v8b.py:57-65` `listing_order` | **OK（記録・相殺）** | `(index + step) % 2` で選択肢の**並び順だけ**を機械的に入れ替える。どちらを選ぶかには関与しない。位置効果の相殺目的で、走行前に凍結。 |
| 1-7 | **既定値①**：`sim_v8c.py:209-221`（`where = HOME`） | **OK（ただし片寄りを記録）** | 応答が読めなかった月は「今月はどこにも行かない」に落ちる。**「売らない側に有利な既定」ではないが、会話量が減る側に片寄る**（会えない＝聞ける話が減る）。`parse_fail` に原文が残るので、走行後に件数を確認すれば影響量は測れる。件数が多ければ結果の注記に書くこと。 |
| 1-8 | **既定値②**：`sim_v8c.py:269-285, 312-319` | **OK** | 月末の答えが読めなかったときに「出さない」「売らない」で**埋めていない**。`no_answer` / `invalid_listing` / `invalid_respond` として別に数え、X社の履歴上も「答えが返らなかった」という第3の状態になる（`sim_v8c.py:39-41, 346-348`）。欠損を売らない側に丸めていないので、売却率が下振れしない。 |
| 1-9 | 乱数の在処 | **OK** | ①`MockV8CClient`（`sim_v8c.py:44-96`・`--provider mock` のときだけ。実走では `create_llm_client` に差し替わる＝`sim_v8.py:190-194`）②区画の格子配置 `field_v8.py:154-179`（`LAYOUT_SEED=85` 固定・地区と seed だけを使い、名前・持ち主・ペルソナ・結果を使わない・走行前に凍結）③上記 1-6 の並び替え。**いずれも人の行動を決めていない。** |
| 1-10 | `configs/config_field_v8c.yaml` | **OK** | パラメータは seed / steps / chat / scene_rounds / chunk / max_cost / LLM 設定だけ。「売る確率」「愛着」「閾値」の類は1つも無い。temperature 0.75・max_tokens 2200 は**住民もX社も同じ値**（`sim_v8.py:222-241` の `_call` は system 前置きだけを出し分ける）。 |

**結論：ルールベースの行動決定は無い。**

---

## 2. プロンプトの誘導（住民側）

レンダリング全文を目視した（共通前置き・行き先・場面・月末の問い〈提示あり／なし〉）。

- **禁止語の実測**：住民側の4種の文面に
  `警戒 / 買い占め / 噂 / 皆 / みんな / べき / 急 / 守 / 相談 / 気づ / 危 / 反対 / 注意 / 目立 / 取得せよ / できるだけ多く`
  は**1つも出ない**。`X社 / 海外 / 投資` が出るのは**提示が届いた月のプロンプトだけ**で、出どころは
  届いた条件文の本文である（`[今月あなたに届いたもの] X社から：「私どもは海外の不動産投資会社です。…」`）。
  共通前置き・場面・提示が無い月の月末の問いには**1文字も出ない**（`field_v8c.py:93-157` に X社 の語が無いことを目視、`tests/test_v8c.py` でも固定）。
- **目的・課題・注目点を与える文**：無い。共通前置きは世界の事実（時間の進み方・名簿・場所・名義の扱い・
  出品の扱い・理由欄・thought の書き方）だけで、「何に注目せよ」「何が問題だ」に類する文が無い。
- **選択肢の対称性**（`field_v8c.py:292-296, 313-318`）：
  - 問い①「出す」＝申し出が公の記録に載る／「出さない」＝載らない（状態の記述・対称）
  - 問い②「売る」＝名義はX社になる／「売らない」＝名義はあなたのまま（状態の記述・対称）
  - 並びは 1-6 のとおり主体×月で交互＝既定の位置が固定されない。
- **不可逆の一言**（`field_v8c.py:329-330`）：
  `売ると今月末に名義がX社へ移り、その後は戻らない。売らないと今月末の名義はあなたのままである。`
  **1行に両側を並べており片側だけに重みが付いていない**。当初案にあった「以後あなたに問いは来ない／
  翌月も持ち主として問われる」は Codex の走行前指摘で削除済み（設計 §4-1・`tests/test_v8c.py` で固定）。
- **理由欄の書かせ方**（`field_v8c.py:272-273, 309, 333-334`）：どの欄も
  「一行で書く（40字以内・書かない場合は空文字）」。**「必ず書け」「詳しく書け」「正直に」は無い**。
  空欄は空欄のまま数える（`sim_v8c.py:115-120, 467-474`）。

### 設計上わざと残した非対称（B-1〜B-3・**直さず記録に留める**）

- **B-1「売らない」の理由だけがX社に伝わる**（`field_v8c.py:145-150, 335-336`）。
  片方の選択肢にだけ「相手に伝わる」という帰結が付く＝形式的な非対称である。**施主修正2の明示指示**であり、
  隠れ経路を作らないために共通前置きにも中立に1行で開示されている。設計 §6 に
  「理由欄そのものが判断を変えうる」と先に書いてある。**結果を読むときは、v8b との差を
  『言葉の違いだけ』と読まないこと**（設計 §6 の注意書きのとおり）。
- **B-2 提示に添う自己紹介「私どもは海外の不動産投資会社です。」**（`field_v8c.py:46, 49-54`）。
  住民は**提示を受け取って初めて**これを知る（共通前置きには無い＝上記の実測どおり）。
  **施主決定 00:56**。国名も、警戒を促す語も無い。世界が決定論で添えるので、X社の作文能力に依らず
  全員が同じ1行を受け取る＝主体間で公平。判定＝OK（世界の与件）。
- **B-3 町の背景文**（`config_field_v8c.yaml:59-64`）が「人口減少・高齢化・相続・改修負担・後継者不足」
  という**所有を続ける側の負担だけ**を並べている＝弱い売却アンカーでありうる。Codex の走行前レビューも
  同じ指摘をしていたが、**v3 以降どの版でも1文字も変えていない世界の与件**なので、ここで変えると v8b との
  対比が「文言を2つ同時に変えた結果」になって比較が壊れる。設計 §4-1-3 / §6 で据え置きを明記済み。
  **版間の比較には効かないが、水準（何割が売るか）には効きうる**という限界の書き方が正しい。

---

## 3. X社側に漏れているもの

- **X社が見るもの**（`field_v8c.py:378-425` のレンダリング全文を確認）＝
  ①命題1行 ②登記簿（区画→今の名義） ③前月末の出品板 ④自分の提示履歴（相手ごとに畳んだもの・
  直近の条件文と断りの一言は全文） ⑤今回判断する相手の名前 ⑥書き方の指示。**これだけ**。
- **漏れていないこと**：住民の `thought`（内心）・会話の原文・行き先・`listing_reason`・
  売った人の `sell_reason` はどこにも渡っていない。`fold_history_v8c`（`field_v8c.py:342-375`）が
  載せるのは `decline_reason` だけで、これは `sim_v8c.py:338-353` で
  **「その月にその提示に対して『売らない』と答えた本人の一言」だけ**を紐づけている。
  `tests/test_v8c.py::test_acquirer_prompt_carries_only_the_decline_note` が固定している。
- **命題の置き場所**（`field_v8c.py:390`）＝X社の user プロンプト先頭のみ。住民側の全文面に
  `取得せよ / できるだけ多く / 毎月動け / 合法な手段` は無い（実測済み・test で固定）。
  命題から「反発を招いて止まるほど目立つな」は消えている（`field_v8c.py:59-61`）＝施主修正3のとおり。
- **X社の前置き**（`field_v8c.py:169-197`）に、場所・会話の仕組み・thought の説明は入っていない
  （＝住民の共通前置きの使い回しではない）。「公開されている情報だけ」→「次に示す情報だけ」への
  言い換えも入っている（断りの一言は公開情報ではないため・設計 §4-1-2）。
- **X社の自己紹介の混入**：住民の共通前置きに `海外` は**無い**（実測）。届いた提示の中にだけ現れる。

判定＝**OK**。

---

## 4. 世界の帳簿

| 項目 | 該当箇所 | 判定 |
|---|---|---|
| 名義移転の条件 | `sim_v8c.py:333-336` → `field_v8.py:124-139` | OK。`sell=="売る"` と答えた人の**全保有**が X社 へ。部分売却は扱わない。 |
| 不可逆 | `field_v8.py:130-131` | OK。すでに売った人に再度 `apply_sale` すると例外で止まる。`risk_set()`（`field_v8.py:114-116`）が売却済みを毎月除くので、二度目の提示も月末の問いも来ない。 |
| 売った人は町に残る | `sim_v8c.py:192-227` / `sim_v8b.py:225-297` | OK。行き先と会話のループは `self.agents`（30体全員）を回す。除外されるのは提示と月末の問いだけ。共通前置きにも「名義が移っても住むこと・営むことは変わらない」と書いてある。 |
| 出品は当月限り | `sim_v8c.py:355-361` | OK。`listed_by_step[step]` は翌月のX社が1回見るだけで、名義は動かさない。**同じ月に売った人は板に残さない**（名義がもう移っているため）。事実は `listings.json` に `sold_same_month` として残る。 |
| 配送（居合わせ＋隣近所・重複なし・私信なし） | `sim_v8b.py:277-296` | OK。`routes.setdefault` で1人につき1回だけ（居合わせが先勝ち）。発話は「その場に居た全員にそのまま聞こえる」ので、`talk_to` は宛先の表示であって配送先の絞り込みではない＝私信は存在しない。自分自身には配送しない。 |
| 隣近所の定義 | `field_v8.py:182-204` `adjacency_v8` ＋ `field_v8.py:154-179` `parcel_grid_v8` | OK・**決定論**。44区画を「地区でまとめ → 地区の中だけ `LAYOUT_SEED=85` 固定 seed で並べ替え → 8列の格子へ詰める」で並べ、上下左右が隣。位置決めに使うのは**地区と固定 seed だけ**（名前・持ち主・ペルソナ・結果は使わない）。開始時点で一度だけ計算し、36か月変わらない（`sim_v8.py:185-187`）。同じ seed なら毎回同じ隣接になる。 |
| 隣近所には場所を渡さない | `field_v8.py:422-439` `_heard_rows` | OK。隣近所経由の一言は `[隣近所]` と表示し、どこで言われたかを渡さない。 |

補足：`neighbours.json` という名前のファイルは存在しない。隣接は上記のとおり走行時に決定論で計算され、
各人の隣人名は `timeline_v8c/<id>.json` の `neighbours` に、本人へ見せた形は
プロンプトの `[あなたの不動産に隣り合う不動産の、開始時点からの主]` に残る。

---

## 5. ペルソナ30体

`configs/personas_v8.yaml` 全文を機械検索した結果、**agents 節（設定文）に**
`売 / 買 / X社 / 外資 / 海外 / 投資 / 警戒 / 手放 / 噂 / 皆が / 相談 / 気づ / 注意 / 守`
は**1つも出てこない**（ヒットするのは冒頭のコメント〈掟〉と、`利用台数`『相談』を含む行政の業務説明
`事業者相談` の2箇所だけで、いずれも人物の目的・態度ではない）。

書かれているのは年齢・世帯・生業・土地との関係・維持の余力・在住年数といった**観測可能な事実**で、
「愛着0.8」のような数値スコアも「〜なら売る」も無い。相続・後継者不足・改修負担・介護は
**与件として可**の範囲（施主の合意どおり）。行政2体は `sellable: false` で、ペルソナから
「外部からの取得」「警戒」も外されている（v8 の走行前レビューで除去済み）。

判定＝**OK**。

---

## 6. 費用停止（`max_cost_usd`）

- 仕組み：`sim_v8b.py:132-134` `_guard_cost()` が `CostLimitReached` を投げ、`sim_v8b.py:446-470` の
  run ループが**その月を捨てて**止まる（`monthly` に入らない＝`months_run` に数えない）。
  月の途中で見るのは X社ターン後・行き先後・場面後の3か所（`sim_v8c.py:323-331`）。
- **特定の主体だけを飛ばす形にはなっていない**：止まるのは常に**全体**で、しかも月の区切りごと。
  「A さんだけ問わない」「売りそうな人だけ残す」といった選別は無い。
- **ただし1つ歪みがある（唯一の要対応）**：`_acquirer_turn` はX社の提示を出した時点で
  `self.offers.append({... "result": "売らなかった" ...})` と暫定値で積む（`sim_v8c.py:184-189`）。
  結果の確定は月末の `_decide_turn` の後（`sim_v8c.py:342-353`）なので、**その前に費用で止まると、
  誰にも問われていない提示が `result="売らなかった"` のまま `offers.json` と `summary.offers_declined`
  に残る**（最大で1か月ぶん＝最大28件）。`summary["partial_month"]` にその月番号は入る。
  - 影響：世界の挙動・名義の記録は歪まない。歪むのは**集計だけ**（成約率の分母が最大28件水増しされ、
    見かけの成約率が下がる）。
  - 直し方（**走らせ直す必要は無い**）：まだ存在しない `tools/v8c_curves.py` で、
    `offers` / `decisions` / `plans` を **`step <= summary["months_run"]` で絞ってから数える**。
    1行で済む。合わせて `partial_month` が null でないときは結果文書に明記する。
- 上限値の妥当性は設計 §5-5 のとおり（0.51 + 場面ひとつぶん 0.034 = 0.544 < 0.55）。
  監査としては算術に誤りを見つけていない。

---

## 7. 集計

監査の途中（01:1x）に別セッションが `tools/v8c_curves.py`・`tools/v8c_emergence.py`・
`tools/v8c_fig.py` を作ったので、**この3本も読んで監査した**（ファイルには触れていない）。

- **判定 LLM は使っていない**：3本とも `llm` / `client` / `generate` / `genai` / `openai` /
  `gemini` / `api_key` / `requests` の参照が**1つも無い**（機械検索で実測）。やっているのは
  保存済み JSON を数えることだけ。
- **語彙は走行前に凍結された v8b の語からの置換だけ**（`tools/v8c_curves.py:28-33`）＝
  否定語 `売らない/売却は考え/手放すつもりはない/現状維持/売るつもりはない/売却しない/守り続け`、
  肯定語 `売る/売却する/名義を移す/手放す`（v8b の「応じる」を落としただけ）、打ち消し `ない/ません/ぬ。`。
  用途は**「内心・理由と選択の食い違いの候補」を出すことだけ**で、見出しにも
  「決定論の語句照合・判定ではない」と書いてある（`:14, :276, :327`）。**結果の数値を作っていない。**
- **理由の分類**（`tools/v8c_emergence.py:19-49`）は3つの語彙リストによる**多重ラベル**で、
  `if/elif` の優先順位で1つに決めつける作りにはなっていない（該当すれば複数のバケットに数える）。
  出力にも `"語彙による候補抽出。確定は人が原文を読む（採点LLMは使わない）"` と明記されている。
  設計 §4 の「機械は候補を出すだけ・確定は目視」を守っている。**判定＝OK。**
  - ただし**注意2点**（走行後の読み方の問題であって、走行を止める話ではない）：
    ① 語が短く誤検出しやすい（`年`『今年』・`子`『調子』・`話`『世話』・`隣` など）。
    ② 多重ラベルなので**カテゴリ件数の合計は理由の件数を超える**＝「③聞いた話が何%」を
    出すときに分母を取り違えない。**機械の数字を単独で出さず、必ず人手確定後の数字を正とすること。**
- **未対応（§3 の結論③・唯一の要対応）**：`tools/v8c_curves.py` に
  **`step <= months_run` の絞り込みが無い**（機械検索で `step <=` の該当なし）。
  `:184-187` で「第N月の途中で打ち切り」の但し書きは印字されるが、**件数そのものは絞られていない**ので、
  費用で途中停止した場合に成約率の分母が最大28件水増しされる。ここだけ直すこと。

---

## 8. Codex の指摘との一致／相違

**この監査のための新規の Codex 実行はできなかった**：同じ7観点で
`codex exec -m gpt-5.6-sol --sandbox read-only` に投げたところ、2026-08-30 01:09 に
ChatGPT 側の利用上限に当たった（`You've hit your usage limit ... try again at 1:48 AM`・
消費 47,991 トークンで打ち切り）。**上限を使い切ったのは同日 00:40〜01:04 に
走行前レビューで Codex を3回回したためである。**

そこで、その**走行前3回の Codex 出力（同一の v8c 実装・同一のレンダリング済みプロンプトを
読ませたもの）を原文で確認し**、本監査の結論と突き合わせた。保存先＝
`%TEMP%\claude\C--Users-user\<session>\scratchpad\codex_out_v8c.md`（1回目）／
`codex_out_v8c_2.md`（2回目・修正確認）／`codex_out_intro.md`（自己紹介1行の可否）。

### 一致した点

- **X社への非公開情報の漏えいは無い**（Codex 1回目「2. 妥当な点」＝`plans/decisions/utterances/deliveries`
  はX社プロンプトの引数に渡っていない）。本監査 §3 と同じ結論・同じ根拠。
- **選択肢の並びが主体×月で交互で、問い②の説明が対称**。本監査 §2 と一致。
- **欠損を「出さない」「売らない」で埋めていない**（Codex は `sim_v8c.py:271` を指す）。本監査 1-8 と一致。
- **「売らない」の理由だけが提示に紐づいてX社の履歴に載る**実装であること。本監査 §3 と一致。
- **理由欄には測定反応性がある**（＝理由を尋ねること自体が介入）が、両方の答えに同じ形式で付き・
  選択→理由の順・空欄可・理由の例示が無いので**特定方向への誘導は弱く許容範囲。ただし結果解釈に
  残すべき**。本監査 B-1 と同じ扱い（＝直さず記録に留める）。
- **町の背景文は弱い売却アンカーでありうる**。Codex 2回目は「実装上の欠陥ではない・**結果に限界として
  明記すれば十分**」として最終判定 **GO**。本監査 B-3 と一致。
- **自己紹介「私どもは海外の不動産投資会社です。」は誘導ではなく意図した情報開示**（Codex 3回目・修正不要・GO）。
  本監査 B-2 と一致。

### Codex が出し、実装に反映済みだった点（本監査でコードから再確認）

- 1回目の**必須指摘**「不可逆の一言が非対称（売る＝以後問いが来ない／売らない＝翌月も問われる）」
  → 現行コードは `field_v8c.py:329-330` の両側1行に置換済み。2回目の Codex も
  「レンダリング済み全文で指定どおり」と確認している。本監査でもレンダリング全文で確認した。
- 1回目の任意指摘「X社前置きの『公開されている情報だけ』が断りの一言と矛盾」
  → `field_v8c.py:172` で「次に示す情報だけである」に置換済み。2回目の Codex が確認。本監査でも確認。

### 相違（本監査だけが指摘している点）

- **§6 の費用途中停止による `offers.json` の歪み**（誰にも問われていない提示が `result="売らなかった"`
  で残り、集計の分母を水増しする）。**Codex の3回のレビューはいずれもこの点に触れていない**
  （費用停止経路と集計側を見ていない）。**本監査だけの指摘であり、唯一の要対応事項である。**
- **§7 の `tools/v8c_curves.py` が未作成であること**と、そこに `step <= months_run` の絞り込みを
  入れる必要。Codex は集計スクリプトを対象にしていない。
- **1-7 の「行き先の parse 失敗時に HOME に落ちる＝会話が減る側に片寄る」**。Codex は欠損の扱いを
  「no_answer で埋めていない」とだけ評価しており、行き先側の既定には触れていない。

### 相違の扱い

Codex の最終判定は **GO**（2回目・3回目）。本監査も**世界の側は GO**で、
上記3点は**いずれも走行を止める理由にならない**（世界の挙動を変えず、集計と注記で対処できる）。
なお「実装後・走行後の第三者再確認」は 01:48 以降に Codex の枠が戻れば1回で埋まる。

---

## Discord 貼付用の要約

```
v8c 最終チェック（ルールベース・恣意の有無）

・ルールベース：なし。行き先/発話/出品/売る売らない/X社の提示先・条件は全部LLMの答えをそのまま
  記帳してるだけ。確率・閾値・スコアで行動を決める箇所はゼロ。乱数は①mock（実走では使わない）
  ②区画の格子配置（固定seed85・地区とseedだけ）③選択肢の並べ替え（主体×月で交互）の3つだけ。

・恣意/誘導：走行を止めるものは無し。住民側の文面に「警戒/買い占め/噂/皆が/〜すべき/急いで」は
  1語も無い。「X社」「海外」が出るのは提示が届いた月だけで、出どころは条件文に添う自己紹介1行
  （施主決定00:56）。共通前置き・立ち話・提示なしの月には1文字も出ない（実測）。選択肢は両方とも
  状態の記述で対称、不可逆も両側を1行に並べてある。

・わざと残してる非対称3つ（全部開示済み・直さず記録に留める）
  1)「売らない」の理由だけX社に届く＝施主修正2。前置きに中立に開示済み
  2) 提示に添う「私どもは海外の不動産投資会社です。」＝施主決定00:56
  3) 町の背景文が所有継続の負担ばかり＝v3から不変の与件。版間比較には効かないが「何割が売るか」の
     水準には効きうる、と結果に書く（設計§6に記載済み）

・ペルソナ30体：売る/買う/X社/外資/海外/警戒 は1語も無い。年齢・生業・相続・後継者不足などの
  事実だけ。数値スコアも「〜なら売る」も無い。

・欠損の扱いは健全：読めなかった答えを「出さない/売らない」で埋めてない（no_answerで別勘定）。
  唯一の既定は行き先が読めない月に「どこにも行かない」に落ちること＝会話が減る側に片寄る。

・唯一の要対応（集計側・走らせ直し不要）：費用上限で月の途中に止まると、その月の提示が
  offers.json に「売らなかった」のまま残る（最大28件）。tools/v8c_curves.py で
  step <= months_run に絞れば直る。今は入ってない。

・Codex：新規実行は01:09に利用上限で不発（00:40〜01:04の走行前レビュー3回で使い切り・復帰1:48）。
  代わりに同じv8c実装を読ませたその3回の出力と突き合わせた。Codexの判定はGOで、
  X社への漏えい無し・選択肢対称・欠損を埋めてない・理由欄の反応性と背景文は記録に留める・
  自己紹介1行は誘導でなく情報開示、まで一致。相違＝費用途中停止の集計歪みと行き先の既定は
  Codexが3回とも触れてない独自指摘。
```

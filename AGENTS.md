# Agent Instructions

このリポジトリで作業するエージェントは、着手前に必ず `docs/HANDOFF.md` を全文読むこと。
同ファイルをCodex・Claude・人間の共通の引き継ぎ正本として扱う。

実装、テスト、シミュレーション実行、重要な判断の節目ごとに、現在地・結果・次の作業を
`docs/HANDOFF.md`へ反映する。再起動や別エージェントへの交代が起きても、会話履歴なしで
再開できる状態を保つ。

既存の `mvp_v1` とコミット済み本番成果物は再現用なので変更しない。新シナリオは別設定・
別ペルソナ・別runとして追加する。秘密情報や `.env` の内容は記録しない。


# RESULTS

シミュレーション結果の記録。**1ラン1セクション**で、上が新しい。
各ランの生データ・レポートHTMLは `simulations/<run_dir>/` に自己完結で入っている。

書き方のルール:
- 数字は `summary.json` からそのまま写す（手で丸めない・記憶で書かない）。
- 「何が起きたか」は `events.jsonl` / `utterances.jsonl` の**実際の発話を引用**して書く。
- 仮説と違った点こそ書く。都合の悪い結果を消さない。

---

## テンプレート（本番ランのたびにコピーして埋める）

### run: `YYYY-MM-DD_HHMM_NN_<name>`

| 項目 | 値 |
|---|---|
| config | `configs/config_xxx.yaml` |
| モデル / provider | |
| 規模 | 区画 / エージェント / step |
| 実所要時間 | |
| API コール数 / 入力tok / 出力tok | |
| 実コスト（実測 or 見積り） | |
| 無効行動数 | |

**KPI6**

| # | KPI | 値 |
|---|---|---|
| ① | 所有集中度（最終シェア / HHI） | |
| ② | 認知転相率（累積） | |
| ③ | 売却カスケード（誘発件数 / 最大連鎖） | |
| ④ | 手遅れ度（規制発動時シェア・第N月） | |
| ⑤ | 検知ラグ（初報道時シェア・第N月） | |
| ⑥ | 生活KPI（事業者残存率 / 住民転出率） | |

**何が起きたか**（時系列で3〜6行）

**創発したもの／しなかったもの**
- 事前の仮説（コンセプトペーパー §4-5）に対して、出た／出なかったを明記する
  - 売却の同調カスケード
  - 「気づいた時には過半」
  - 規制議論の逆説（規制の噂が駆け込み売却を生む）
  - 疑心の誤爆（無関係の新住民・小口買い手への排斥）

**引用**（実際の発話。誰の何月の発言かを添える）

**次のランへの申し送り**

---

## 2026-08-14 — 実APIの経路検証（1か月だけ・実費 $0.0035）

本番ランの前に、**Gemini 実APIで1step だけ**動かし、6役割すべての構造化出力・並列実行・
発話分類器が通ることを確認した。社会的な結論を得るためのランではない。

### run: `2026-08-14_2206_04_live_smoke_1step`

| 項目 | 値 |
|---|---|
| モデル / provider | `gemini-2.5-flash-lite` / google（新SDK `google-genai` 2.18.1） |
| 規模 | 48区画 / 19エージェント / **1step** |
| 実所要時間 | 4.4 秒（parallel_workers=8） |
| API コール数 | 20（エージェント19 ＋ 発話分類1） |
| 実トークン | 入力 23,891 ／ 出力 2,893（**1コールあたり 出力 145 tok**） |
| **実コスト** | **$0.003546** |
| エラー / 無効行動 / 途中切れ | 0 / 0 / 0 |

**確認できたこと**

- 6役割すべてで `response_schema`（action_type の enum）どおりの JSON が返り、パース失敗ゼロ
- 8並列で 19 エージェントが 4.4 秒で完了。レート制限に当たらなかった
- 発話分類器（temperature=0）が動作。第1月の雑談はすべて `neutral` に分類された（妥当）
- 初月の実際の判断：買い手AIは `wait`（「初月のため、市場の全体像を把握し、慎重に行動するため」）、
  メディアは `investigate`（「まずは登記簿調査で基礎情報を集める」）、自治体は `monitor`。
  **台本を与えていないのに、初手として筋の通った行動が出た。**

**この検証で見つけて直した不具合（重要）**

1. **`max_tokens` 不足で JSON が途中で切れ、その月の行動が丸ごと落ちる**
   最初の疎通確認で発生。`memory` の上限を 700字→**200字**に下げ、`max_tokens` を 420→**560** に上げ、
   さらに**切れた JSON からフィールドを回収する復旧パーサ**を入れた（行動の中身は推測しない）。
   修正後の1step ランで途中切れ 0 を確認。
2. **私信が翌月に届かない**（賃料交渉・自治体の照会が受信箱のクリア順序で消えていた）。
   クリアの位置を観測構築の直後に移して修正。届くことを実行ログで確認。
3. **金額の未検証で帳簿が壊れる**（負の価格で「売主が買主に金を払う」状態になり得た）。
   価格・賃料に成立条件を入れ、予算超過の買付も成立しないようにした（補正ではなく不成立として記録）。
4. **賃料のキャッシュフローが無かった**（賃料を上げても誰の現金も動かず、実害が帳簿に出なかった）。
   月次清算を追加：借主→家主へ賃料が実際に移動し、事業者には初期条件としての月次粗利が入る。
   **資金が尽きても自動で閉店させない**（続けるか畳むかは事業者の LLM 判断）。

3 と 4 は Codex のレビュー指摘と自己レビューの一致点。

---

## 2026-08-14 — パイプライン検証ラン（mock・API未使用）

**API を一切叩かずに**全パイプラインが通ることを確認したドライラン。
LLM はスタブ（`--provider mock`）であり、**以下の KPI 数値に社会的な意味は無い**。
確認したのは「帳簿 → ログ → KPI → 可視化」の配線と、コスト見積りの基礎データ取得のみ。

### run: `2026-08-14_2209_06_mvp_v1_mockE2E_v2`

| 項目 | 値 |
|---|---|
| config | `configs/config_mvp_v1.yaml` |
| モデル / provider | mock（スタブ）／API未使用 |
| 規模 | 48区画 / 19エージェント / 36step（3年） |
| 実所要時間 | 0.4 秒 |
| LLM コール数 | 697（エージェント 684 ＋ 発話分類 13） |
| 無効行動数 | 49 / 684（7.2%。スタブが自分の所有でない区画を指定したため。**コードは補正していない**） |
| 途中切れ | 0 |

**通ったこと（配線の検証）**

- 帳簿：買付 → 逆提示 → 承諾 → 所有権移転・現金授受が記録され、`ledger.jsonl` に全件残った
- 月次清算：36か月ぶんの賃料が借主から家主へ移動（例：BZ01 が支払った累計と HH06 が受領した累計が一致）
- 可視性：メディアが `investigate` を選んだ翌月から街全体の名義が観測に載った
- KPI6：6指標すべてが `summary.json` に算出された（④⑤は該当イベントの発生後に確定）
- 可視化：`<run_dir>.html` が自己完結で生成され、ヘッドレスChromium で**JSエラー0**、
  KPIカード6・折れ線2枚・所有マップ（36フレーム）・発話・行動ログの描画を確認
- 出力：`simulations/YYYY-MM-DD_HHMM_NN_<name>/` の命名規則どおりに全成果物が1フォルダに収まった

### 1ラン（36か月）のコスト見積り

この mock ランが残した**実プロンプト全文 697 本**からトークンを数えた。

| 項目 | 値 |
|---|---|
| 対象モデル | `gemini-2.5-flash-lite`（in $0.10 / out $0.40 per 1M tok・GA・2026-08-14 公式pricing確認） |
| API コール数 | 697 |
| 入力トークン | **1,097,371**（平均 1,574 / call） |
| トークン数の出し方 | Gemini `countTokens` で 24 本を実測校正（係数 0.874）。countTokens は課金対象外 |

出力トークンは前提の置き方で2通り出した。

| 前提 | 出力トークン | **1ラン** | **×2（上限）** |
|---|---|---|---|
| 保守：1コール 347 tok（`max_tokens` の 62%） | 249,048 | **$0.209** | **$0.419** |
| 実測ベース：1コール 160 tok（live ランの実測 145 に余裕） | 121,140 | $0.174 | $0.348 |

**採用する見立ては保守側の `$0.209`／上限 `$0.419`。** 目標だった「1ラン $2 以下」に対して
1桁下に収まっている。前提：explicit context cache は使わない（キャッシュ割引ゼロで計算）。
implicit cache が効けば下振れする。
`simulations/2026-08-14_2209_06_mvp_v1_mockE2E_v2/cost_estimate.json` に同じ数字が残っている。

前回ハッカソンで preview モデルの実請求が試算の3〜4倍になった件は、**GA モデルのみ使用**という
方針で回避している（単価が固定されている）。それでも安全係数 ×2 を併記した。

**まだ分かっていないこと（本番ランで初めて分かる）**

- 買い手AIが実際にサラミ戦術を採るのか、一括で動くのか。名義を使い分けるのか
- 住民が隣の売却に反応するのか（カスケードが立つのか）
- 自治体が条例を出すのか、出すとしたら何月・どのシェアの時点か
- メディアが調べるのか、調べたとして「別名義＝同一実体」に気づくのか
- 36か月を通した実際の出力トークン（1step の実測 145 tok/call が後半も維持されるか）

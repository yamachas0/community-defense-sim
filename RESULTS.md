# RESULTS

シミュレーション結果の記録。**1ラン1セクション**で、上が新しい。
各ランの生データ・レポートHTMLは `simulations/<run_dir>/` に自己完結で入っている。

書き方のルール:
- 数字は `summary.json` からそのまま写す（手で丸めない・記憶で書かない）。
- 「何が起きたか」は `events.jsonl` / `utterances.jsonl` の**実際の発話を引用**して書く。
- 仮説と違った点こそ書く。都合の悪い結果を消さない。

---

## テンプレート（本番ランのたびにコピーして埋める）

### run: `YYYY-MM-DD_HHMM_NN_<name>`

| 項目 | 値 |
|---|---|
| config | `configs/config_xxx.yaml` |
| モデル / provider | |
| 規模 | 区画 / エージェント / step |
| 実所要時間 | |
| API コール数 / 入力tok / 出力tok | |
| 実コスト（実測 or 見積り） | |
| 無効行動数 | |

**KPI6**

| # | KPI | 値 |
|---|---|---|
| ① | 所有集中度（最終シェア / HHI） | |
| ② | 認知転相率（累積） | |
| ③ | 売却カスケード（誘発件数 / 最大連鎖） | |
| ④ | 手遅れ度（規制発動時シェア・第N月） | |
| ⑤ | 検知ラグ（初報道時シェア・第N月） | |
| ⑥ | 生活KPI（事業者残存率 / 住民転出率） | |

**何が起きたか**（時系列で3〜6行）

**創発したもの／しなかったもの**
- 事前の仮説（コンセプトペーパー §4-5）に対して、出た／出なかったを明記する
  - 売却の同調カスケード
  - 「気づいた時には過半」
  - 規制議論の逆説（規制の噂が駆け込み売却を生む）
  - 疑心の誤爆（無関係の新住民・小口買い手への排斥）

**引用**（実際の発話。誰の何月の発言かを添える）

**次のランへの申し送り**

---

## 2026-08-14 — パイプライン検証ラン（mock・API未使用）

本番ランの前に、**API を一切叩かずに**全パイプラインが通ることを確認したドライラン。
LLM はスタブ（`--provider mock`）であり、**以下の数値に社会的な意味は無い**。
確認したのは「帳簿 → ログ → KPI → 可視化」の配線と、コスト見積りの基礎データ取得のみ。

### run: `2026-08-14_2155_02_mvp_v1_mockE2E`

| 項目 | 値 |
|---|---|
| config | `configs/config_mvp_v1.yaml` |
| モデル / provider | mock（スタブ）／API未使用 |
| 規模 | 48区画 / 19エージェント / 36step（3年） |
| 実所要時間 | 0.3 秒 |
| LLM コール数 | 696（エージェント 684 ＋ 発話分類 12） |
| 無効行動数 | 45 / 684（6.6%。スタブが自分の所有でない区画を指定したため。**コードは補正していない**） |

**通ったこと（配線の検証）**

- 帳簿：買付 → 逆提示 → 承諾 → 所有権移転・現金授受が記録され、`ledger.jsonl` に全件残った
- 可視性：メディアが `investigate` を選んだ翌月から街全体の名義が観測に載った
- KPI6：6指標すべてが `summary.json` に算出された（④⑤は該当イベントの発生後に確定）
- 可視化：`<run_dir>.html` が自己完結で生成され、ヘッドレスChromium で**JSエラー0**、
  KPIカード6・折れ線2枚・所有マップ（36フレーム）・発話・行動ログの描画を確認
- 出力：`simulations/YYYY-MM-DD_HHMM_NN_<name>/` の命名規則どおりに全成果物が1フォルダに収まった

**コスト見積り（この mock ランが残した実プロンプト全文から算出）**

| 項目 | 値 |
|---|---|
| 対象モデル | `gemini-2.5-flash-lite`（in $0.10 / out $0.40 per 1M tok・GA） |
| API コール数 | 696 |
| 入力トークン | 1,055,702（平均 1,517 / call） |
| 出力トークン | 188,640（1コール 260 tok 想定・分類は 900 tok） |
| トークン数の出し方 | Gemini `countTokens` で 24 本を実測校正（係数 0.873）。countTokens は課金対象外 |
| **1ラン見積り** | **$0.181** |
| **安全係数 ×2 の上限** | **$0.362** |

前提：explicit context cache は使わない（キャッシュ割引ゼロで計算）。implicit cache が効けば下振れする。
`simulations/2026-08-14_2155_02_mvp_v1_mockE2E/cost_estimate.json` に同じ数字が残っている。

**まだ分かっていないこと（本番ランで初めて分かる）**

- 買い手AIが実際にサラミ戦術を採るのか、一括で動くのか
- 住民が隣の売却に反応するのか（カスケードが立つのか）
- 自治体が条例を出すのか、出すとしたら何月か
- メディアが調べるのか、調べたとして「別名義＝同一実体」に気づくのか
- 出力トークンの実測値（見積りは max_tokens の 62% 想定であり、実測ではない）
